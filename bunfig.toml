[install]
# Use exact versions in lockfile by default; allow caret in package.json.
exact = false

[test]
# Root `bun test` discovers backend tests only. Shared tests must run through
# `bun run --filter @agent-workflow/shared test`; frontend tests use Vitest.
root = "packages/backend/tests"
