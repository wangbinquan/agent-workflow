/** Runtime-only nominal key for the Intent context transaction capability. */
export const intentContextResourceAuthorizationSessionBrand: unique symbol = Symbol(
  'intent-context-resource-authorization-session',
)

/** Runtime-only nominal key for the provider-owned demo seed capability. */
export const demoResourceCatalogSeedParticipantBrand: unique symbol = Symbol(
  'demo-resource-catalog-seed-participant',
)

/** Runtime-only nominal key for the Skill Catalog boot capability. */
export const skillCatalogBootParticipantBrand: unique symbol = Symbol(
  'skill-catalog-boot-participant',
)

/** Runtime-only nominal key for the Agent launch integrity capability. */
export const agentLaunchResourceIntegrityParticipantBrand: unique symbol = Symbol(
  'agent-launch-resource-integrity-participant',
)

/** Runtime-only nominal key for the tx-bound skill version commit capability. */
export const skillVersionCommitParticipantInTxBrand: unique symbol = Symbol(
  'skill-version-commit-participant-in-tx',
)
