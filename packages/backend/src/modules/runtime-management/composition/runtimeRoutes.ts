/** Runtime Management HTTP adapters are mounted only by the application root. */
export { mountRuntimeRoutes } from '../infrastructure/http/runtimeRoutes'
export { mountRuntimesRoutes } from '../infrastructure/http/runtimesRoutes'
