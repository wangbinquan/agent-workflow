# gitleaks 历史误报豁免（fingerprint = commit:file:rule:line）。
#
# gitleaks detect 扫全历史：源码里已加 `gitleaks:allow` 行内注释只对新 commit
# 生效，进了历史的匹配必须在这里按 fingerprint 精确豁免。只登记确认过的误报，
# 每条注明为什么不是 secret；真 secret 一律轮换+清史，不进本文件。

# RFC-310 PR-3（66a79be2）：测试字面量撞 generic-api-key 规则——
# `idempotencyKey: 'idem-forged-actor-1'` / `deliveryKey: 'journey-delivery-1'`
# 是幂等键/投递去重键的测试值，非凭据。
66a79be2d746820786ae5260c607fb574744ead6:packages/backend/tests/rfc310-pr3-upload-security.test.ts:generic-api-key:195
66a79be2d746820786ae5260c607fb574744ead6:packages/backend/tests/rfc310-pr3-journey.test.ts:generic-api-key:614
