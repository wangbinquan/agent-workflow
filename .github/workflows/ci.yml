name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint + Typecheck + Test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          # Lockfile format was changed in 1.2+; keep CI on the same major as
          # what developers run locally. Bump as the project upgrades.
          bun-version: '1.3.x'

      - name: Install opencode (required by daemon/doctor tests)
        # Daemon `start` refuses to run without opencode >= MIN_OPENCODE_VERSION
        # (1.14.0); cli `doctor` asserts opencode is in PATH. Several test
        # files spawn the daemon end-to-end so the binary must be installed.
        run: |
          bun install -g opencode-ai
          # setup-bun already puts ~/.bun/bin on PATH for subsequent steps,
          # but be explicit so the smoke check below works in this step too.
          export PATH="$HOME/.bun/bin:$PATH"
          opencode --version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Format check
        run: bun run format:check

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: Test (backend + shared, bun:test)
        run: bun test

      - name: Test (frontend, vitest)
        run: bun run --filter @agent-workflow/frontend test

  build-binary:
    name: Build single-binary (smoke)
    needs: check
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.x'

      - name: Install opencode (start --version probe needs it for log lines)
        # The smoke test invokes `agent-workflow version`, which does not touch
        # opencode — but the start path does, and we want the binary to be
        # ready to run an end-to-end smoke locally if anyone reuses this job.
        run: |
          bun install -g opencode-ai
          export PATH="$HOME/.bun/bin:$PATH"
          opencode --version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build single-binary
        run: bun run build:binary

      - name: Show artifact size
        run: ls -lh dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: agent-workflow-${{ matrix.os }}
          path: dist/agent-workflow-*
          if-no-files-found: error

  e2e:
    # Playwright happy-path against the freshly-built single binary. We
    # depend on `build-binary` and re-run the build here rather than
    # downloading the artifact because download-artifact strips +x and
    # the binary needs to stay executable. The build is incremental
    # against the same workspace so the second run is fast.
    name: Playwright e2e
    needs: build-binary
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.x'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build single-binary
        run: bun run build:binary

      - name: Install Playwright browsers
        run: bunx playwright install --with-deps chromium

      - name: Run e2e
        env:
          CI: 'true'
        run: bun run e2e

      - name: Upload Playwright trace on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: playwright-trace-${{ matrix.os }}
          path: |
            test-results/
            playwright-report/
          if-no-files-found: ignore
