# RFC-319 任务分解：用户面 system-mock e2e 覆盖加固

读之前先读同目录 `proposal.md`（背景 / G1–G6 / AC-1–AC-8）与 `design.md`（层次落位规则 / 分流 /
四层棘轮）。逐条缺口在 `findings.md`，机器索引在 `findings.json`。

本仓**只在 `main` 上开发、不建分支、不开 PR**（CLAUDE.md 硬规则），所以下文的「批」= 一次或数次
提交，不是 PR。每批都必须独立过 `bun run gate:local`、独立可 revert。

## 0. 开工前置（必须先做，否则后面全部计数不可信）

| 任务 | 内容 | 依赖 |
| --- | --- | --- |
| **T1** | 在**干净的 exact SHA** 上重采分母：`allRouteMeta()` 端点数（落档时 462）、`router.tsx` 路由数（58）、`e2e/*.spec.ts` 用例数（340）、`startSystemMockSuite` 文件数（6）。落档基线是 `92478e636`，且当时工作树上有并发 session 的大批未提改动（RFC-318 数字员工工具合同等） | — |
| **T2** | 若 T1 与 `findings.md §1` 的数字有出入，**只订正分母**，不重跑 820 条审计；出入写进本文件 §7 变更记录 | T1 |
| **T3** | 确认 `design/RFC-318-minimal-digital-employee-tool-contracts/`（并发 session 的 RFC）与本 RFC 无交叉改动面；若他们的实现会新增用户面能力，在 R3 账本里给它们留 `gapSince: RFC-318` 的位置而不是替他们补测 | T1 |

## 1. B1 — 分档工装与 CI 拓扑（design §2）

| 任务 | 内容 |
| --- | --- |
| **T10** | `e2e/` 引入 `@nightly` tag 约定；在 `e2e/README.md` 写清「不带 tag = PR 档」与选 tag 不选分目录的理由 |
| **T11** | `ci.yml` 的 `e2e` job 增加 `--grep-invert`；**windows 腿必须与既有 `$AW_E2E_WINDOWS_EXCLUDE` 合成一条正则**（`(@nightly)\|(<既有>)`）——Playwright 只认一个 `--grep-invert`，写两次后一个静默覆盖前一个 |
| **T12** | 新建 `.github/workflows/e2e-full-nightly.yml`：cron + `workflow_dispatch`，**不加任何 grep 过滤**跑全量，分片 4 起；产出 `route-hits/` artifact |
| **T13**（未做，留待第一条 nightly 档后端用例落地时一起做）| `packages/backend/tests/nightly/` 目录 + `scripts/test-backend-sharded.ts` 与 `bun run test:backend` 的排除；`e2e-full-nightly` 增加一步跑它 |
| **T14** | 守卫：①排除清单与目录两向钉死（目录存在但没被任何腿跑 ⇒ 红；排除项指向不存在的目录 ⇒ 红）；②windows grep 合成正则里必须同时含 `@nightly` 与既有排除项 |
| **T15** | `gate:local` 增加**可选** Playwright 车道（默认关，显式开），并在 `packages/backend/tests/local-gate-runner.test.ts` 的车道断言里登记 |

## 2. B2 — 四层棘轮（design §3–§5）

棘轮**先于**补测落地：账本初值 = 今天的实测缺口集合，之后每个域批把它单调压小。
这样「进度」本身是机器可见的，且任何一批中断都不留半截状态。

| 任务 | 内容 |
| --- | --- |
| **T20** | `e2e/harness.ts`：`logLevel: 'info'` → `'debug'`（`harness.ts:556`）；`stop()` 在 `rmSync` 临时 home **之前**提取 `<home>/daemon.log*` 的 `req` 行 → `test-results/route-hits/<spec>-<pid>.jsonl`。覆盖 `keepHome=true` 的两次起停分支 |
| **T21** | 归一器：具体路径 → 路由模式，**method 一并比对**、最多字面量段获胜；与 `api-contract-coverage.test.ts:275` 的逐段匹配器**共用一份实现**，不得各写一套 |
| **T22** | `architecture/e2e-endpoint-coverage.json` 初值 = 全量 nightly 实测的未命中集合；汇总 job `route-coverage-ledger`：下载全部分片 artifact → 求并集 → 逐条相等断言 + 反方向 zombie 断言（日志里出现了注册表没有的端点） |
| **T23** | R1 的 fail-closed：分片数 < 预期 / 并集为空 / 某分片无 journal / 认不出任何一行日志格式 —— 四种都必须红 |
| **T24** | **R2-static**：从 `routes/*.tsx` 的 `createRoute({ path })` + `router.tsx` 派生路由集；`architecture/e2e-route-coverage.json` 初值 = 今天的 18 条未直达（其中 12 条零字符串命中）；守卫进 `gate:local` |
| **T25** | **R2-runtime**：新建 `e2e/test.ts` 共享入口（`test.extend` 包 `page`，监听 `framenavigated` 写 journal）；62 个 spec 的 import 机械迁移；源码守卫「`e2e/**` 不得直接 import `@playwright/test`」。**本任务侵入面最大，单独一批、可独立 revert** |
| **T26** | **R3**：`architecture/e2e-capability-ledger.json` 从 `findings.json` 播种 820 行；141 条 `covered` 的证据要**逐条归一**成 `{file, test}` 并验证 test 标题逐字存在——这一步会顺带照出 `findings.md` 里写歪的证据，照出来的就地订正 |
| **T27** | R3 守卫四条断言（证据可达 / 状态与证据互斥 / gap 只减不增 / 总数可增但须有出处） |
| **T28** | **入网**：三份账本注册进 `architecture/ledger-baselines.json`；R1–R4 全部守卫注册进 `architecture/guard-manifest.json`，按既有字段契约填 `corpusScanner` / `minCorpusFiles` / `assertsAbsence` / `negativeFixture` |
| **T29** | **R4 变异实证**：逐条守卫注入真实事故形态 → 确认转红 → 撤销 → 确认转绿；证据记进本文件 §6 验收清单。**没做过变异实证的守卫不算交付** |

## 3. B3 — 8 条空洞绿修复（design §6）

逐条见 `findings.md §2`。口径：先写会红的断言并**确认它红**，再恢复被测行为；
恒真成因写进 test 上方注释。

| 任务 | 条目 | 处置要点 |
| --- | --- | --- |
| **T30** | `AGENT-31` | `rfc099-ownership-acl.spec.ts:188` 的 `acl-panel` 计数恒为 0（carol 从未打开该弹窗）。改成断言「详情页与不存在同形」，并先注释掉私有化那步验证会红 |
| **T31** | `WF-49` | `workflow-editor.spec.ts` 的删除用例只开框跑 axe 点 Cancel；全仓无一处对 `/api/workflows` 发过 DELETE。补真删除 + 名字二次确认 + 版本 CAS |
| **T32** | `AGENT-35` | 功能断言被误关在 `rfc250-visual-states.spec.ts:530` 的 `test.skip(!RUN_VISUAL_REGRESSION)` describe 里。**搬出去**到 PR 档功能 spec，不是给 visual describe 解 skip |
| **T33** | `EVENT-46` | `rfc232-owner-list.spec.ts` 全程 admin 身份，peer 只用来播种。补真正的非 admin 会话可见性边界 |
| **T34** | `TASK-32` | 任务成员面板对话框从未被打开、`members-save` 从未被点、转让所有权从未跑过 |
| **T35** | `DE-10` | 泳道拖拽重排发生在未保存草稿里且以 Cancel 收尾，「刷新后仍生效」在源码里不存在。补 publish + reload + 序断言 |
| **T36** | `OPS-002` | harness 把同一个 bindPort 既写进 config.json 又传成 flag，两者恒等，「flag 压过 config」不可分辨。构造两者不同的场景 |
| **T37** | `UX-19` / `WF-23` | 覆盖面清单写错（前者漏记三处已有整页 axe 扫描，后者 `workflow-camera-focus-selection` 全仓零命中）。订正账本 + 补 focusSelection 断言 |

## 4. B4–B18 — 逐域补测（design §1 层次落位）

**同一个域的 P1 / P2 / P3 一起做**——它们大量共用 fixture 搭建（例如「建端点 → 取 secret」
既是 P1 前置也是 P2 rotate 用例前置），分开做会逼出重复代码。档位靠 tag 区分：
P1 不带 tag（PR 腿），P2 / P3 带 `@nightly`。

按 P1 密度排序，先做风险最集中的：

| 批 | 域 | 缺口 | P1 | P2 | P3 | 备注 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| **B4** | 身份、认证、用户与资源权限 | 38 | 15 | 18 | 5 | P1 密度最高；`/setup/admin` 首屏、退出登录、会话失效、越权写他人 public 资源 |
| **B5** | 技能 / 插件 / MCP | 50 | 11 | 30 | 9 | 技能详情整页零 e2e（Save All / 文件树 / 版本回滚 / 删除拒绝 / 可见性）；PAT 脱敏 |
| **B6** | 事件自动化 | 47 | 10 | 23 | 14 | `scripts:author` 五处拒绝分支全仓零命中（违反 CLAUDE.md 硬规则）；rotate secret、去重、熔断、replay |
| **B7** | 记忆与蒸馏 | 52 | 8 | 28 | 16 | 四类 scope 的 ACL 边界、候选行只对资源管理员可读、蒸馏任务详情页零直达 |
| **B8** | 仓库、仓库组与 Git | 41 | 7 | 25 | 9 | 凭据脱敏、推送拒绝、镜像 GC、worktree 清理 |
| **B9** | 代理 | 45 | 6 | 12 | 27 | 富字段保存往返（`agents.detail.tsx:315-353` 四条事故墓碑）、409 冲突、能力引用不丢 |
| **B10** | 意图构建器与融合 | 62 | 6 | 41 | 15 | 融合可见性隔离、跨用户 approve/reject 拒绝形状 |
| **B11** | 数字员工 / 开发任务 | 36 | 5 | 20 | 11 | UI 面为主（后端 journey 已有 6 个 system-mock 套件）；`/code/missions/*` 零直达；`retireTool` 零校验（可能是真缺陷，按 §5 处理） |
| **B12** | 工作流与画布编辑器 | 50 | 5 | 34 | 11 | wrapper 归属拖放、连线弹窗提交、冲突三条恢复动作、code-host-call 检查器（全 e2e 零命中） |
| **B13** | 任务生命周期 | 33 | 3 | 25 | 5 | 删除终态任务、成员面板、启动前置权限门的绕 UI 直打接口分支 |
| **B14** | 前端横切 | 35 | 3 | 16 | 16 | 退出登录、WS 4401/4403 语义、WS 推送让**已打开的**页面自更新（今天全部是「先收 WS 再 goto」） |
| **B15** | 人机交互门 | 42 | 2 | 28 | 12 | 引擎侧已扎实；缺的是浏览器里走完门 + 任务问题看板整条产品线（e2e 零字符串命中） |
| **B16** | 设置、运行时、文档与总览 | 51 | 2 | 31 | 18 | 全仓没有一条 e2e 点过设置页的 Save；备份/恢复的「选文件 ≠ 上传」护栏 |
| **B17** | 运维面 CLI / daemon / 备份恢复 | 48 | 2 | 22 | 24 | 备份→改数据→恢复→重启的完整往返（五组路径 e2e 全零命中）；`auth password-login` 零测试 |
| **B18** | 工作组 | 49 | 1 | 30 | 18 | 自动保存的冲突处置（工作流侧有 5 条弱网用例，工作组侧 0 条） |

每批的**收口检查项**（写进提交前的自查，来自 `docs/dev-gotchas.md` 的实测教训）：

1. `grep -o "getByTestId('[^']*')" <新增/改动的 spec>` 逐个回 grep `packages/frontend/src` 确认存在；
   动态拼接的按前缀比对。**RFC-310 T140 实测：一条 21 个 testid 的 spec，头三个在源码里根本不存在。**
2. 新增 / 改动的 spec **在本机真跑过一次**（`bun run build:binary:e2e` + `bunx playwright test <spec>`）。
   「本机跑不了、CI 会替我跑」通常是错的。
3. 改了选择器 / 等待条件的，必须真浏览器跑——源码层守卫只能证明两处字符串一致，**它看不见 DOM**。
4. 跑**根级** `bun run lint`（`e2e/` 只被 `lint:repo-ui` 覆盖，`--filter <pkg> lint` 漏它）。
5. `bun run gate:local` 全绿；推完立刻按**本人 exact SHA** 查 CI。
6. 账本单调性：本批把 R1/R2/R3 的 gap 计数**压小了多少**，写进 commit body。

## 5. 补测过程中发现真缺陷时的处置

审计已预判若干条（例如 `retireTool` 对「工具正被已发布岗位模板绑定」零校验、
`forcePasswordChange` 后端发信号前端零消费）。口径：

1. 先写一个**能稳定复现**的用例（红），再写修复（绿）——CLAUDE.md §Test-with-every-change。
2. 生产改动**单独 commit**，与测试批分开，commit message 写清是本 RFC 补测时照出来的。
3. 在本文件 §7 逐条登记；若缺陷涉及产品行为判断（不是明显 bug），**停下来问用户**，不自行裁决。

## 6. 验收清单（对齐 `proposal.md` AC-1–AC-8）

- [ ] **AC-1** 679 条缺口逐条有用例，且在 R3 账本里指向具名证据；账本守卫验证证据可达
- [ ] **AC-2** 8 条空洞绿修复完毕，每条附变异实证记录（注入→红→撤销→绿）
- [ ] **AC-3** R1/R2/R3/R4 四条棘轮全绿；每条自带负 fixture 能自证会红；每条断言语料非空
- [ ] **AC-4** 三份账本注册进 `architecture/ledger-baselines.json`，条目数只减不增
- [ ] **AC-5** PR 腿单片墙钟不超过今天的水平；`e2e-full-nightly` 能完整跑完
- [ ] **AC-6** 每条新增 / 改动 spec 本机真跑过；testid 逐个回 grep 过
- [ ] **AC-7** `bun run gate:local` 全绿；hosted CI 按 exact SHA 全绿
- [ ] **AC-8** `e2e/CAPABILITY_COVERAGE.md` 改写为导读，删除被本次审计证伪的宣称，指向三份机器账本

**两向对账**（RFC-317 收口时自查出的定式，写在这里免得重蹈）：收口前拿**编号**做一次
双向核对——①`proposal.md` 的每条 AC 都能在本文件找到承载它的任务；②本文件的每个任务都能
回指一条 AC 或一条 `findings.md` 条目。RFC-317 正是在这一步发现「52 条 P1/P2 逐条修复」
与「C1–C9 各有拒绝分支覆盖」两条 AC 在「看起来做完了」的状态下静默不成立。

## 7. 变更记录

| 日期 | 内容 |
| --- | --- |
| 2026-08-25 | **B72（数字员工 P1 + 仓库列表与导入 + MCP 管理三域并行批，20 条能力；并把 B69 的 ACL 那份迁到 RFC-324 新契约）**：新建 `e2e/rfc319-digital-employee-p1.spec.ts`（DE-07/17/18/27/X1，6 条——**数字员工域的 5 条 P1 至此清零**）、`e2e/rfc319-repos-list-and-import.spec.ts`（REPO-01/02/03/08/09/10/11/33，9 条）、`e2e/rfc319-mcp-management.spec.ts`（RES-16/17/21/22/23/37/X3，7 条）。**ACL 那份是被迫重写**：RFC-324（并发 session）在 B69 之后落地并连推三笔，把授权改成 read/write 两档、`/acl` 的 wire 从 `userIds` 换成 `grants[{userId,level}]`、拒绝码从一个 `forbidden` 拆成三个。迁移后判据**只增不减**：新增 write 档的完整分野（PUT 内容 200 且真落库 / body 夹带改名 403 `resource-rename-owner-only` / DELETE·rename·PUT-acl 403 `resource-govern-owner-only`）、界面加人默认落 `read` 的服务端 wire、提档后对方真的拿到内容写入口、以及 `expectNoAclWriteControls()` 里新加的第五条（能用加人搜索框就能给同伙发授权）。四个拒绝点**逐个实跑确认过不是同一个码**——`PUT /acl` 走的是 `resourceAcl.ts:776` 的 `requireResourceGovern`，不是 `:823` 那条只在竞态下够得着的 `forbidden`。**工装缺陷自查（重要）**：本批发现我的变异脚本用 `-g` 单跑一条，而本仓 7 份 spec 里有 5 份是 `mode: 'serial'`，单跑等于抽掉前置用例——**无论有没有变异都会红，于是每条变异都报 BITES，等于工装在自证**。已修：一律跑整份文件，并在变异前先跑一次基线要求全绿（这类错误以后会自己暴露）。用修好的工装复验后，`task-feedback-distill` 的「把冷却从 3s 改成 6s」**确实是 NO-BITE**——那份 spec 在运行时从生产源码读 `RATE_LIMIT_MS`，期望值跟着变异一起变，对这个常量自证。这不是可以糊过去的：已补一条**区间约束**（500ms–10s），判的不是「等于 3 秒」而是「还在防手抖的量级上」；低于 500ms 挡不住真实连点，高于 10s 就是把人锁在门外。如实记下：在这个区间内改冷却值仍然不咬——那是有意的，冷却时长是可调项而非契约。本批四条抽样变异（ACL 拒绝码退回 `forbidden` / 仓库搜索退回前端 / MCP 的 401·403 不再判 `auth-required` / 启动请求不带 `executionOptions`）**全部咬中**。起草方另捎回 **9 条真实产品缺陷**（含一条**空洞绿**：`rfc310-digital-employee-journey.spec.ts` 的联调用例一直绿，而 `analyze-implement` 其实每轮都在失败重试——它只断言了「第一步存在」，那是平台节点），已逐条登记 `docs/audit-backlog.md`。能力 gap 439 → **419**，其中数字员工域 P1 归零。 |
| 2026-08-25 | **B71（CI 上暴露的三条自有红，逐条查到确定性根因后修掉；顺带立一条通用规律）**：**①`clarify-detail-states` 只在 macOS 腿红、648ms 就失败**——不是超时是竞态。实测探针定因：详情页一次冷加载会打**两次** `GET /api/clarify/{id}`（第二次来自 WS 握手后的 `reconcileOnOpen`，`useClarifyWs.ts:75` → `useWsInvalidation.ts:117-124`），断言只要第一次就满足，**正文结束点只比第二个 handler 收尾晚 29ms**；机器一忙就翻成负数。而 Route 动词里 `fulfill`/`continue`/`fallback`/`abort` 全包在 `_raceWithTargetClose` 里、页面关掉时静默放弃，**只有 `route.fetch()` 走 `_wrapApiCall` 没这层 race**——所以红永远落在那一句，且把 handler 拖慢时报错归属会漂到下一条用例。两把锁：handler 里不再 `route.fetch()`（要回源的数据在 Node 侧预取），`afterEach` 加 `unrouteAll({behavior:'wait'})`（必须 wait，`ignoreErrors` 只是把错吞掉＝「重跑就过了」）。已把这条立成 `docs/dev-gotchas.md` 的通用规律，并给自有的另两处 `route.fetch` 站点补上锁 B。**②`rfc319-settings-runtimes` 只在 Windows 腿红**——夹具值 `/bin/echo` 在 win32 上 `isAbsolute()` 为真（无盘符根路径也算绝对）但 `resolve()` 会补盘符换反斜杠，过不了 `validateBinaryPath` 的 canonical 闸（`runtimeRegistry.ts:567-588`）⇒ 422 ⇒ **弹窗按设计就不该关**（`RuntimeList.tsx:445-449` 只有 onSuccess 才关窗）。不是慢。改用 `process.execPath`，并把那条断言拆成「PUT 必须 200」+「弹窗必须关」两条，让「服务端拒了」与「关窗逻辑坏了」不再共用同一条红；另加 `beforeAll` 夹具自检，下次换平台时红在夹具处而不是伪装成产品缺陷。**③`intent-commit-details-and-conflicts` 在 macOS 腿红**——工作区页签初值是 once-per-session 定的（`intent.detail.tsx:254-258`），取决于**首读时**的 `journey.reason` 是否 ∈ `REVIEW_FIRST_REASONS`；本机首读赶在生成完之前拿到 `generation-running` ⇒ build，CI 慢一步拿到 `draft-invalid` ⇒ review。原用例把本机那一支当成了契约。改法是**两支都锁**（严格强于原来只锁一支）：用 hold 文件挂住这一轮保证首读必然在草稿之前 ⇒ 初值必为 build 且草稿落地后不得抢焦点；`reload()` 使首读必为 `draft-invalid` ⇒ 必须直接落评审栏（这一支此前 e2e 零覆盖）。两条变异（把分档去掉恒 build / 拆掉 once-per-session 守卫）**都当场咬中**。三条红的复现方式各不相同但都做到了**确定性**：①人为延后第二次 callback 稳定复现同一行号；②用 `path.win32` 实测 + 在 macOS 上用 `/bin/./echo` 仿真同一机制；③注入首个详情 GET 延迟 1.5s，本机 100% 复现出与 CI 逐字一致的失败。 |
| 2026-08-25 | **B70（代理端口/删除 + 记忆面板/蒸馏 + 画布检查器，三域五份并行批）**：新建 `e2e/rfc319-agent-ports.spec.ts`（AGENT-14~20/X5，9 条）、`e2e/rfc319-agent-delete-and-refs.spec.ts`（AGENT-08~13/40/X2，7 条）、`e2e/rfc319-memory-distill-jobs.spec.ts`（MEM-25~33/47，10 条）、`e2e/rfc319-memory-panels.spec.ts`（MEM-02/03/06/07/11/12/20/23/X6/X7，11 条）、`e2e/rfc319-canvas-inspectors.spec.ts`（WF-26~32/X3，11 条），共 48 条用例覆盖 43 行能力。**变异实证 12 条：10 条直接咬中，2 条 NO-BITE 各自查明并各带出一处真缺口**——①端口那份的文件头声称覆盖「后端拒收未声明的 branchPort（`agent.ts:973-985`）」，但拆掉那道闸整份仍全绿：**没有任何断言覆盖它**。补了保存期 422 `branch-port-not-declared` + `details.notFound` 点名 + 整笔回退三条断言，再拆当场红。删端口忘了摘 `branchPorts` 这条漂移在编辑器里不可见，不在保存时拦住就要等到运行期才炸，而那时报的是「你确信自己声明过的端口」。②删除那份的确认名用例送的 typo 是 `${name}-2`（**超串**），于是把 `!==` 松成 `startsWith` 照旧被拦、变异不咬——而注释写的却是「同名前缀」。补了**截断前缀**输入（`name.slice(0,-3)`），前缀匹配与大小写不敏感两种松法现在都当场红。这两条都不是断言弱，是判据**没覆盖到最容易被松开的那个方向**。起草方另捎回三条真实产品缺口（未写成断言，因为那会是红用例）：`removeOrphanOutputSidecars` 返回值缺 `branchPorts`，清理任意一条孤儿映射会把草稿里的分支声明整体归零，下一次动任何分支开关就把原声明永久覆盖掉（`agent-ports.ts:424-447` + `OutputsEditor.tsx:81-83,97-99`）；记忆表单 repo 档下拉每行空白——`MemoryDialogShell.tsx:194-196` 仍读 RFC-204 早已摘掉的明文 `url`，全仓只剩这一个消费方；`?focus=` 是空载参数，`CandidatesList.tsx:55` 用 `search={{focus}}` 整体替换 search 导致 `tab` 回落，「在审批队列中打开」实际落在 All Approved。能力 gap 482 → **439** |
| 2026-08-25 | **B69（工作组 ACL 补上 B68 扣下的那一份，并修掉「干净沙箱」本身的污染）**：`e2e/rfc319-workgroup-acl.spec.ts` 落地，6 条用例覆盖 WG-19a/19b/20/21/22/X1，账本 5 行（WG-19/20/21/22/X1）转 covered。**B68 记的方法论少了一步，这一批把它补全**：那份「`git archive origin/main` 解包出的干净构建」其实**不干净**——解包目录里的 5 个 `node_modules` 全是指向脏仓的软链（含 `packages/*/node_modules`）；本仓是 bun workspaces，`packages/*/node_modules/@agent-workflow/*` 本该指向**同棵树内**的兄弟包，经软链一解析就回到了**脏工作树**的 `packages/shared`，而当天脏树里正躺着别人 RFC-324 改过的 `resourceAcl.ts` / `scheduledTask.ts` / `taskCollab.ts` 三个 schema。得到的二进制是个杂交体：后端/前端是 `origin/main`，schema 是 RFC-324。**后果可实证**：该二进制上 `PUT /acl {userIds:[…]}` 被 RFC-324 的 zod 判 422，`{grants:[…]}` 回 200 且 `aclRevision` 前进、但回读 `users` 恒空——**「给资源授权」整体是一次静默 no-op**，任何「先授权再验证」的用例要么红要么假绿，测试侧无从绕过。处置：解包后先 `rm` 掉那 5 条软链（只删链不碰目标），在沙箱内 `bun install --frozen-lockfile`（991ms/1546 包）拿到自己的 workspace 链接，再 build；并给出两秒可跑的判据（`readlink -f` 出来的 `@agent-workflow/shared` 必须落在沙箱内）。**修正后复验的结论是好的**：B68 那 41 条、B67 那 37 条（含 ACL 6 条）、wave-1 那 27 条，**105 条在真·干净构建上全绿**——先前提交的那批不是假绿；而 B68 判给 ACL 那份的 3 条红确实是脏二进制造成的，起草方「别改断言、先修沙箱」的判断成立。方法论修正已回填 `docs/dev-gotchas.md`（原配方的软链写法就地作废，并记下「在软链进去的沙箱里 `bun install` 会写穿软链改到主仓」这条附带风险）。能力 gap 487 → **482** |
| 2026-08-24 | 落档。审计基线 `92478e636`。原拟编号 RFC-318，与并发 session 的「数字员工工具最小合同」撞号，改为 RFC-319 |
| 2026-08-25 | **B65-DE（真实数字员工复验场景收口）**：以真实 GitLab 环境跑过的 ISSUE 自动接入、人工方案评审迭代、流水线红灯分类与修复、多数字员工跨仓协同、外部审批、MR 多轮检视、目标分支前进与冲突修复为输入，按最新分层规则回收到既有确定性 E2E：外部系统与生命周期继续由 `rfc310-digital-employee-system-mock-e2e.test.ts` / `rfc310-digital-employee-conflict-system-mock-e2e.test.ts` 承担，人工方案闸由真实 TaskEngine 子进程 E2E 承担。新增事故判据：人工闸在 `awaiting_review` 与 `done` 两个时点都必须把内部 TaskEngine task 冻结为 `catalogVisibility=internal`，公共任务目录两次都不得出现它，防止数字员工 Round 再次冲击用户任务列表。R3 同批把 EVENT-07、HUMAN-28、HUMAN-29 从散文证据归一为逐字可达的测试标题，`covered-unverified` **141 → 138**；本批自身不新增 UI 覆盖、不改变 capability gap，当前整数批次 B65 后总 gap 仍为 **553**。按用户约束不启动/停止任何服务，本地只做静态与不监听端口的定向检查，完整 E2E 由 exact-SHA hosted CI 执行。 |
| 2026-08-25 | **B68（工作组 + 设置双域并行批，39 条一次落地）**：新建五份 spec —— `e2e/rfc319-workgroup-crud.spec.ts`（WG-01/02/03/04/16/17/18）、`e2e/rfc319-workgroup-editor.spec.ts`（WG-07/10/11/12/13/14/23）、`e2e/rfc319-settings-runtimes.spec.ts`（CFG-10~17）、`e2e/rfc319-settings-sections.spec.ts`（CFG-01/02/05/07/09/23/26/27/35/36）、`e2e/rfc319-overview-and-docs.spec.ts`（CFG-20/34/39/40/41/42/43），共 41 条用例。**这一批最重要的产出不是用例，是验证方式的修正**：`build:binary:e2e` 编的是**工作树**，CI 编的是 `origin/main` 干净 checkout，本仓常年有并发 session 的在制品（当天 58 个 ` M`），两者差着一整批未提交的生产代码。**双向都实撞了**：①**假绿**——第六份 ACL spec 按「testid 源码实读」写了 `acl-level-*` / write 授权档，在工作树构建上 5/5 全绿，而 `origin/main` 上那些 testid 出现 0 次、`resourceAccessPolicy.ts` 根本不存在，提交上去必红；②**假红**——首页/文档那份在工作树构建上 5 失败 / 5 通过，在干净构建上 **10/10 全绿**，照着假红去「修」等于把别人的在制品固化进断言。处置：**验收一律用 `git archive origin/main` 解包出的干净构建**（普通目录，非 worktree，不触犯禁 worktree 硬规则），并行跑时各自钉一份该构建的副本。全部 6 份在干净构建上跑出 **43 通过 / 3 失败**，3 条全在依赖 RFC-324 的 ACL 那份——**该份整体扣下不提交**，已退回起草方按 main 现状重写。方法论已落 `docs/dev-gotchas.md`。**变异实证**：本批抽验 5 条全部咬中；另有 1 条我自己选错锚点（改到注释上）、换成真实产出点后咬中。起草方还捎回一条真实产品缺口：运行时表的 `probe` / `setDefault` / `toggleEnabled` 三个 mutation **没有错误渲染面**，失败时用户什么都看不到。能力 gap 526 → **487** |
| 2026-08-25 | **B67（意图构建器域并行批，26 条一次落地）**：新建四份 spec —— `e2e/fusion-review-surface.spec.ts`（INTENT-50/51/52/55/56/57/X9）、`e2e/rfc319-intent-access-boundaries.spec.ts`（INTENT-11/27/43/44/45/X1）、`e2e/rfc319-intent-create-and-list.spec.ts`（INTENT-03/04/05/06/07/08/09）、`e2e/intent-commit-details-and-conflicts.spec.ts`（INTENT-30/35/36/37/38/41），共 31 条用例。**流程换法**：四个能力簇并行起草（只读研究 + 出稿，不 build 不跑），我串行做 build / 跑 / 变异实证 / 落库；共享二进制的争用用「钉一份副本 + `AGENT_WORKFLOW_E2E_BINARY`」解开，起草方跑用例、我同时做变异 build 互不干扰。**14 条变异咬中**（融合 5、权限 3、创建与列表 3、提交明细 3）。**一条不咬中并查明原因**：拆掉 `submitPendingRef` 单条防重复提交门不红——旁边还有 `pending` 状态兜着；两道一起拆当场红，属双重守卫而非断言弱。**一条变异是我自己选错**：把 `intent-name-conflict` 的**错误码**改名不红，因为用例断的是消息原文；改成拆掉 `occupied?.has(...)` 判定后立刻红。**两处起草期的错误由实跑纠正**：①按钮 pending 时可及名从 `Start building` 变 `Creating…`，按名字定位当场失配；②卡片 meta 三格之间无空白，`/\b2 turns?\b/` 的词边界永远匹配不上——两处都改成更严的逐格 `toHaveText`，判据只紧不松。**如实记的覆盖边界**：secret / secretWaiver / humanBinding 三类槽现行 stub 造不出（只签发 `finalName`），本批用请求层注入锁的是**明细步的用户面闸门**，不是服务端那三条规则（后者已有单测）；要真链路覆盖需给 stub 加三个变体，方案已写进用例头。能力 gap 552 → **526** |
| 2026-08-25 | **B66（同一个节点上还有别人在等，你得看得见）**：新建 `e2e/clarify-shard-switcher.spec.ts`，一条 P3（HUMAN-15），**人机交互门整域缺口至此清零**。fan-out 把活切成 N 份、每份各自反问时，人是从收件箱点**某一条**进来的；这一屏不摆出兄弟分片，剩下那几份就没有任何入口——任务一直停着，而他以为自己答完了。**fixture 换了一条路**：工作流 fan-out 那条走不通（见 B62 实测），仓内唯一能造出「同一 clarify 节点、多分片待答」的是**工作组**——worker park 的 `askingShardKey` 是任务卡 id，而 `WG_CLARIFY_NODE_ID` 全组共用一个。为此给 `mode-workgroup-matrix` 加了一条 **`WG_WORKER_CLARIFY` 环境变量门控**的 worker 反问支线：默认完全不生效，因此 rfc254 差分基线仍 155/155、既有 `workgroup-matrix.spec.ts` 仍 6/6。**一处实测纠正**：判「这一卡自己答过没有」不能用 `## Clarify Q&A`——leader 那一轮的 Q&A 会被灌进全组上下文，三条 member run 的首轮提示词里全都已经带着它，拿它当判据会把整条支线静默抑制掉（第一版正是如此，0 个分片轮）；改用**本卡问题的原文**作判据才对。**三条变异全部咬中**：切换器恒不渲染、只列自己（兄弟分片进不去）、阈值 2→1。能力 gap 553 → **552** |
| 2026-08-25 | **B65（一份文档都没有时，别把人困在门口）**：新建 `e2e/review-empty-list-auto-approve.spec.ts`，一条 P3（HUMAN-43）。「审计输出为空」是 Code→Audit→Fix 的**成功**档，但下游那道人工评审门是按「逐份看」设计的，零份时既无从渲染也无从决策——RFC-202 T1 记的事故形态是：停一个**从每个用户入口都够不着**的空轮（收件箱看不见、详情 404、画布导航 null、API 对零条待决行回 409），任务永远停在 awaiting_review 且不报错。三段判据：任务真的跑完、**从未进过待办**、发布的 accepted + approval_meta 与「人工勾了空集」逐字一致且带 `auto=empty-list`（下游据此分辨是人批的还是框架判的），并顺带锁住 approval_meta 不带决策人身份（RFC-099）。**三条变异全部咬中**：拆掉自动放行分支（任务当场 failed）、丢掉 auto 标记、把条件放成恒真（对照用例红——这条正是防「把人工门整个废掉」）。**fixture 取巧但不改 stub**：`workflow-matrix` 按提示词标记分支、`iteration=` 也从提示词读，于是 promptTemplate 里写死 `MATRIX_LOOP_EMPTY` + `iteration=1` 就能拿到一个**空端口**，写 `iteration=0` 拿到非空的一份，两条工作流除这一处外逐字相同。能力 gap 554 → **553** |
| 2026-08-25 | **B64（首页那块「等你处理」）**：新建 `e2e/home-inbox-preview.spec.ts`，一条 P3（HUMAN-X3）。这块预览是多数人每天第一眼看到的东西，它错了不会报错、只会让人以为「今天没什么事」，而任务正停在等人回答上。三段判据：两条 feed（评审 + 澄清）都要合进来（少合一条＝那一类待办整类消失）、点一行要落到**它自己**的详情页（落错页面比不给点更糟，人会以为已经处理过）、一条 feed 挂了要说清是哪条并能重试而**另一条照常显示**。**三条变异全部咬中**：合并时丢掉澄清那条 feed、点击一律按评审跳转、一条挂了就整块当错误处理。同一个 daemon 里同时造出「等人回答」与「等人评审」两类真实待办（clarify stub 第二次调用同一 agent 时出 `design`，正好喂评审节点）。能力 gap 555 → **554** |
| 2026-08-25 | **B63（写给未来的自己，到底进没进蒸馏）**：新建 `e2e/task-feedback-distill.spec.ts`，两条 P3（HUMAN-47 / HUMAN-X6）。任务反馈这一屏的全部价值在「写下去之后会怎样」，而差别在界面上只有一个小徽标：徽标要是写死的，人会以为每条都排进了蒸馏、实际可能一条都没排上。四段判据：写入真的 enqueue 并置位（服务端对账 `distilled` + `distillJobId`）、3 秒连点闸（且**服务端不许多出一行**——只挡界面不挡请求等于没挡）、正文 trim/长度校验、以及「看不见的任务」与「不存在的任务」404 **逐字节同形**（能分辨就等于给了个存在性探针）。外加 HUMAN-X6 的深链定位：断**移焦**而不是滚动位置（滚动因视口高度而异，焦点不会），并补一条认不出锚点时不许乱移焦的负向断言。**四条变异咬中**：不置 distilled、拆掉节流闸、可见性中间件改成可区分文案、深链不移焦。**一条不咬中并如实记**：`routes/taskFeedback.ts` 自己那句「不可见 ⇒ 404」改成 403 文案**照样绿**——外人走不到路由自己那一行，真正兜住这条保证的是 `routes/tasks.ts:1204-1208` 的中间件（改它当场红）；路由里那句是双保险的第二层。**一处测试自身的坑**：只改 hash 属于同文档导航、页面不重载，上一步落下的焦点会原样留着，负向断言测的就成了残留——先手动 blur 才问得对。能力 gap 557 → **555** |
| 2026-08-25 | **B62（澄清详情页的三件事 + 一条可达性实测）**：新建 `e2e/clarify-detail-states.spec.ts`，三条 P3（HUMAN-16 / HUMAN-48 / HUMAN-X5）。截断告警（agent 超发题目时页面不说，人看到的就是一份「看起来完整」的问卷，第 6、7 题从此没人知道存在过）、两条查询各自的失败与重试（主查询挂了整页报错可重试；同伴查询挂了**不许**把整页拖垮）、提交归属（HUMAN-48 至此两半齐了：评审半在 B61）。**顺带加了两个 testid**（`clarify-session-error` / `clarify-peers-error`，ErrorBanner 本就有 testid 直通），否则两条查询的失败在断言上分不开。**四条变异咬中**：截断条不渲染、整页错误态删掉、切换器阈值 2→1、提交人不渲染。**三处方法论坑**：①泛选择器 `.error-box` 会被别的错误条满足，导致重试还没跑完就往下走；②注入的 message 不能用来锚——错误条渲染的是解析后文案，原文只进折叠详情；③「同伴挂了不拖垮整页」这条必须**当场判**（短超时）——注入「同伴失败也整页顶掉」后，那个页面会随 10s 轮询在错误视图与正常视图之间来回摆，长超时断言总能逮到一帧正常的、照样绿。**HUMAN-15 实测判定仍为缺口**：把 clarify 节点接到 wrapper-fanout 内层 agent 上，工作流能过校验、三个分片也都跑完，但渲染出的提示词里**根本没有 clarify 邀请**（实测三份 promptText 只列了 design 端口），一轮都不会被 mint，任务直接 done；与 `e2e/clarify.spec.ts:442-450` 那段被 skip 的旧用例注释一致（per-shard clarify 自 RFC-060 PR-D 推迟至今未接线）。本批锁住它的**负向半边**（只有一份待答时不出切换器），正向半边只剩工作组 worker park 一条路，已记进 audit-backlog。能力 gap 560 → **557** |
| 2026-08-25 | **B61（单文档评审页上三件「小」事）**：新建 `e2e/review-single-doc-keyboard-download.spec.ts`，两条 P3（HUMAN-36 / HUMAN-42）。**A 在没有评论时不弹窗、直接批准**——这一屏上有个「按一下就不可逆」的键，于是必须挡住三类误触：组合键（源码注释逐字点名 Cmd+A）、输入框里打字（写驳回理由时一句话就带 a/i/r 三个字母）、历史版本。下载则是：标题带 `/` `:` 时不清洗，点击会在部分系统上**静默失败**；正文空时不禁用，存下来一个 0 字节的 .md。**五条变异咬中**：文件名不清洗、空正文仍可下载、拿掉修饰键守卫（Cmd+A 当场把评审批准了）、拿掉 activeElement 守卫、把决策人显示名塞进重跑提示词。**三处方法论坑，都是变异照出来的**：①`fill()` 不产生 keydown——用它写的「输入框里打字」断言在守卫被整个拆掉时照样绿，换 `pressSequentially` 才有效；②`?version=` 指向**当前**版本会折回当前视图，所以历史态那一档只能放在驳回产生 v2 之后；③历史态有**两道独立守卫**（keydown 一道、渲染一道），只拆一道不红——如实记在用例头，免得后人看到「拆了没红」误判没覆盖。另修一处自己的 flake：`page.route` 里 `route.fetch()` 再 fulfill 的异步窗口会与 8s 轮询、测试收尾撞出 `Route is already handled`，改成**导航前**装路由 + 从预取载荷造响应。本批只做了 HUMAN-48 的评审一半（决策人 + 角色 + 不进 prompt）；澄清那半的「谁提交」徽标仓内仍无断言，HUMAN-48 **仍留在缺口表**。能力 gap 562 → **560** |
| 2026-08-25 | **B60（多文档评审的「轮」这一面）**：新建 `e2e/review-multidoc-round-history.spec.ts`，一条 P2（HUMAN-40）+ 一条 P3（HUMAN-X4）。**仓内此前没有任何 e2e 打开过多文档评审页**。四条单文档没有的用户面契约：①列表里看得出是多文档（看不出就会按单文档心智点进去，漏审的几篇带着未审状态走完流程）；②展开的是**轮**不是版本（多文档 versionIndex 逐篇编号 v1,v1,v1,v2…，铺版本行人根本对不上第几轮）；③历史轮只读且写清那一轮为何被打回（还能点接受＝对作废稿子做决定；不写理由＝不知道哪没过）；④上一轮接受过、这一轮内容变了的要打「已变更」（RFC-129：选择跨轮继承，不打标就会有人直接批准，等于**没人审过**新内容就发布）。外加 HUMAN-X4 的 q / w 快捷键与「带 ctrl/meta/alt/shift 一律不接管」守卫。**造了一个真的单文档对照组**（另起一条 input→quality-gate→review 的工作流），否则「徽标无条件渲染」「一律铺版本行」两条都测不出来。**六条变异全部咬中**：徽标无条件、多文档也铺版本行、`inheritSelection` 恒不 stale、历史轮照渲染写入按钮、快捷键不再排除 ctrl/meta、重新表态不清 stale。**第六条一开始不咬**——原写法把「表态后徽标消失」断在了**本来就没徽标**的那一篇上，属于自证伪断言，已挪到真正带徽标的那篇（这类断言只有靠变异才照得出来）。顺手更正 HUMAN-X4 标题：审计原文写 A / N 键，源码实为 q / w。能力 gap 564 → **562** |
| 2026-08-25 | **B59（把最后一个人撤走之后，那条反问怎么收场）**：新建 `e2e/clarify-round-dismissed-no-human.spec.ts`，一条 P2（HUMAN-17b）。管理员把工作组花名册里最后一个人类成员移出去，此时那条还开着的反问**不可能再有人答**（产品自己也这么认：`resolveWgClarifyAllowed` 无人类成员时恒 false）。三件事必须同时发生：轮次被撤销（不撤则任务永远停在「等人回答」，而「人」已不存在——RFC-181 A2 的开关就为这个而设）、页面说清是**被撤了**而不是任务结束（两者界面同形、后续动作完全不同）、迟到的答案被拒。与 B40 配成一对：B40 锁 `task-canceled` 那一支，本条锁 `wg-clarify-disabled`——两支走不同文案，只有两条都在才挡得住「任挑一条文案糊上去」。API 面上逐字锁 `dismissed 1 open clarify session(s)` 这句回执，否则 canceled 也可能是终态清扫造成的、文案分支就白锁了。**三条变异全部咬中**：把 `errorMessage` 写成 `task-canceled`、前端砍掉 `wg-clarify-disabled` 文案分支、把 configActions 里的撤销触发条件关掉。**覆盖边界如实记**：本 fixture 的 leader stub 兜底分支恒发提问信封，而无人类成员时提问会被拒，所以第二条只能断言「离开等人回答这个态」，不能断言「继续把活干完」——后者要给 stub 加一条无人可问时改走正常出参的分支，会动到 RFC-254 的差分基线，留后续批次。能力 gap 565 → **564** |
| 2026-08-25 | **B58（答完一家就以为交差了）**：新建 `e2e/cross-clarify-multi-source.spec.ts`，一条 P2（HUMAN-05）。两个提问者反问同一个 designer，用户先答完一家。若此时框架立刻放行，后果两层且都不报错：designer 拿**半份**反馈改稿（另一家的意见还没到，可能直接冲突），第二家答完再触发一次、前一次连同烧掉的模型调用作废；而先答完那个人**看不到任何提示**，以为可以走了。判据三段：横幅出现、**点名到具体那一家**（只说「还有别人」等于让人猜）、两家答完后任务真的往前走。**两处真实契约实撞**：①cross-clarify stub 按 agent 名硬分支（`designer` / `questioner`），自定义名会落进 `other` 分支直接出 output 并被强制追问模式拒掉——第一版两个提问者全部 failed；改为两个提问**节点共用同一个 `questioner` agent**（stub 计数按 agent 名分键，两节点首轮恰好 count 1/2 都发起追问），从而不必改 stub、也就不动 RFC-254 的差分基线。②`stop` 作答按契约**只算结清、不喂反馈**，所以两家都 stop 时 designer 本就不该重跑——原本写的「答完就重跑」是误读，改成锁真正的用户面契约并把这条反直觉的语义写进注释。**两条变异咬中**（横幅不点名、横幅整个不渲染）。**一条不咬中并如实记**：服务端那道 readiness 栅栏在 stop-only fixture 下不可观测（拆掉它不红：没有可喂的 source，且界面横幅另有 `crossPeers` 这条独立来源）；要打中它需要 `continue` 作答，而现有 stub 的问答序列做不到、改它又会废掉差分基线，留给后续批次。能力 gap 566 → **565** |
| 2026-08-25 | **B57（diff 是人据以做决定的那块内容）**：新建 `e2e/review-diff-granularity.spec.ts`，一条 P2（HUMAN-37）。三种静默失效：①**段选了但渲染没跟着换**——pill 高亮「词」而屏幕上仍是行级 diff；整行标红时人看不出实际只动了一个标识符，会把一次无害改名读成一次重写；②**快捷键把不该触发的也触发了**——`Cmd/Ctrl+1/2/3` 切粒度而裸的 1/2/3 不许，这一页上 A/R/I 是**单键决策**，键盘处理一旦放宽就可能顺手按下不可逆的一下；③**上一版正文取不到时假装无事**——diff 的一侧来自上一版，那一侧失败若被吞掉，人看到的是一份「什么都没改」的 diff，据此通过一版其实改了很多的稿子。判据都取**渲染出来的粒度**（`.diff-view[data-granularity]`）而不是 pill 的高亮态，因为「段变了但渲染没跟着换」正好落在后者的盲区里。失败态各有各的 testid，共用一条会让「服务端挂了」与「这份文档确实只有一版」无法区分，所以两档分别断言且互斥。**三条变异咬中**：粒度写死成 word、裸数字键也切、上一版失败只转圈不报错。**一档如实留白**：第三种失败态 `review-diff-body-stale-error` 只在「已有缓存数据 + 随后刷新失败」时渲染，而本仓 QueryClient 关掉了 `refetchOnWindowFocus`，外部没有可确定性触发那次刷新的把手——硬造只能靠计时器抢跑，那种用例本仓不写；账本标题据此收窄，这一档留给能拿到 QueryClient 句柄的组件级测试。能力 gap 567 → **566** |
| 2026-08-25 | **B56（面板提交时多带一题，就等于把它「已答成空」封掉）**：新建 `e2e/centralized-answer-pane.spec.ts`，一条 P2（HUMAN-25）。面板比澄清页多两件事，而这两件事**都发生在请求里、界面上完全看不见**：①**只提交真填了的那些题**——面板会把当前所有待答问题一次列出，其中大多数你并不打算现在答；提交时若把没填的也送上，它们会被当作「已答成空」封存，从待答池里消失而没有人回答过，事后翻看板只看到「已处理」；②**重答已封存的题必须显式声明**（`resubmitQuestionIds`）——服务端封存是 exactly-once 的，漏掉声明的表现是「点了提交没反应」。还锁一条边界：面板走**控制通道**（`defer:true`），提交完任务仍停在等人上。判据因此打在请求层（沿用 B27/B52 的教训）。**一处真实契约实撞**：面板会把 **staged** 的题整个排除（`CentralizedAnswerDialog.tsx:107-125`，防「半新半旧」的题被重复下发），所以重答那一档针对的是「已封存但仍待指派」——用例因此先把题从待下发**撤回**再重答，这也是一条真实路径（在面板里答完、又想改）。**两条变异咬中**：提交丢掉子集上限、丢掉重答声明。能力 gap 568 → **567** |
| 2026-08-25 | **B55（看板的界面面：人怎么在这块板上找到自己该干的那张卡）**：新建 `e2e/task-questions-board-ui.spec.ts`，一条 P2（HUMAN-20）。B49 锁的是看板四个动作在服务端是否真生效；这一条锁的是**找得到**：五列分阶段（列错位 ⇒ 人在「待指派」里找不到那张真的待指派的卡，会以为没有待办）与按承接节点过滤（坏掉时人在一堆卡里挑，挑错的那张属于另一个节点的上下文）。**这两件事只有多个提问节点才验得出来**——单节点 fixture 下「过滤器点了什么都不做」与「过滤正确」完全同形，所以特意造了**两条并行的 designer+clarify 分支**（两个 agent，clarify stub 的轮次标记按 agent 名分键）。判据：五列存在且顺序逐条相等；四张卡起初全在待指派、别的列为空；封存 A 之后两张换到「待下发」而 B 那两张不动（顺带断言 B 那一轮仍在等人——控制通道不该顺手推进另一个提问节点）；点节点 chip 后**另一边必须消失**、再点「全部」两边都回来。**两条变异咬中**：每列都渲染全部卡、节点过滤空转。能力 gap 569 → **568** |
| 2026-08-25 | **B54（这一屏显示的是一份已经不存在的现实）**：新建 `e2e/clarify-realtime-collab.spec.ts`，一条 P2（HUMAN-12）。澄清页最危险的状态不是「显示错了」，而是**显示的是一份已经不存在的现实**：同事改了某题的草稿而我毫不知情，两人各自以为在协作、实际在互相覆盖；更糟的是同事**把整轮提交了**，我这一屏还是可编辑表单，继续打字、点提交，然后拿到一个 409——在那之前我完全没有理由怀疑任何事。这一段**只有真浏览器 + 真 daemon 才验得出来**：单测里 WS 是假的，组件测试里「不刷新也能变」这个前提根本不存在。两条判据都要求不刷新页面（页面上留哨兵变量验证确实没重载）：①提示条自己冒出来且远端值被采纳进表单；②同事提交后两个提交键自己变灰。**一处真实契约实撞**：草稿提示条对**自己**的编辑是刻意抑制的（`clarify.detail.tsx:182-184`），第一版用同一身份在「别处」写草稿，界面正确地什么都不显示、当场红；因此本条造了**真正的第二个人**（建用户 → 登录拿会话 token → 以任务管理员身份 PUT 成员；成员维护端点 `tokenAccess: never`，API token 用不了）。**两条变异咬中**：草稿帧被忽略、answered 帧被忽略。能力 gap 570 → **569** |
| 2026-08-25 | **B53（协作草稿写成「整轮一份」，先写的人就凭空消失）**：新建 `e2e/clarify-collab-draft.spec.ts`，一条 P2（HUMAN-10）。澄清页是多人共用的一屏，草稿因此不是「我这台机器上的临时状态」，而是**服务端**逐题共享的东西。三种静默失效：①**粒度写错成整轮**——甲在 q-db 打字、乙在 q-lang 打字，后写的把整轮覆盖掉，先写那位的内容凭空消失、刷新才发现；整轮存一份 JSON 是最自然的实现，所以这是最容易写错也最贵的一处；②**归属没记 / 记成第一个人**——「谁最后动过这一题」是「两条互相矛盾的答案该听谁的」的唯一线索，记错了讨论就从问事实变成猜是谁；③**提交后草稿没冻**——服务端草稿是载入时的协作事实源、会压过本地副本，封存后它若还活着，下次打开就会用一份**没被提交**的内容盖住真正提交出去的那份。判据都带对照：写第二题时断言第一题原样在；同题重写时断言**另一题的归属戳不许跟着刷新**（整轮刷新会在这里露出来）；提交后草稿清空**而归属保留**（归属是审计线索，方向相反）。归属只进审计与界面、绝不进提示词（RFC-099 D7 隔离不变量），所以只从 API 断言。**三条变异全部咬中**：草稿改整轮、归属只记首次、提交后不清草稿。能力 gap 571 → **570** |
| 2026-08-25 | **B52（别处已封存的题，不能被这次提交带走）**：新建 `e2e/clarify-cross-channel-lock.spec.ts`，一条 P2（HUMAN-26）。同一道问题有两条可作答的通道（澄清页快速通道 / 看板·集中面板控制通道），两个人同时用时某题很可能已被别处封存。澄清页若照旧渲染成可答：人会**再答一遍**（他不知道同事答过了）；提交时若把它也带上，要么覆盖掉同事的答案、要么被服务端 exactly-once 守卫整批拒掉（他自己那几题也白填）。两种后果都不报错。判据三段：**看得出**（`data-locked`）、**动不了**（输入禁用）、**不带走**（提交载荷里没有它）——第三段最关键也最容易漏，前两段只改观感，真正决定同事答案生死的是提交时带不带它，而那一段在界面上完全看不见（沿用 B27 学到的教训：断言打到请求层）。外加第四段：提交完之后同事那条答案原样还在。正向对照先做（没被动过时两题都可答），否则「所有题永远变灰」也能让每条成立。**两条变异咬中**：提交不再排除 locked、界面不再变灰/禁用。**一条变异不咬中并如实记进用例**：`lockedQuestionIds` 里的**轮次范围限定**（`originNodeRunId !== nodeRunId` 跳过，防兄弟轮同名 id 误锁）在单轮 fixture 下是空转，去掉它不会红——要打中得在同一任务里造出两轮共存的澄清，clarify stub 现有轮次标记做不到，留给后续批次（与 HUMAN-25 集中回答面板同批更顺手）。能力 gap 572 → **571** |
| 2026-08-25 | **B51（两个「停止反问」的闸，以及一次没有预言力的断言被追到底）**：新建 `e2e/clarify-stop-controls.spec.ts`，两条 P2（HUMAN-07 / HUMAN-27）。「停止反问」是一条**单向门**：把节点从强制追问模式里放出来，下一轮它就不再提问、直接出稿；误按的代价是把「本来还能问清楚」的机会关掉，而后果要到产出错了才看得见。两个入口各锁一道：画布开关的守卫是「目标必须是快照里的**提问 agent 节点**」（挂到 clarify / io 上既无意义、又让人以为自己关掉了什么而其实什么都没发生），且**被拒后指令表一条不留**；澄清页的「提交并停止澄清」必须先弹二次确认，取消时**一个请求都不发**、这一轮原样还在，确认时恰好发一次。画布那条还锁**行为**：开关置 stop 后，即使用户在澄清页按的是「继续追问」，下一轮提示词里也不再有追问协议块，且拿到的是 STOP 而不是 KEEP——正反两面都断言，才排除「碰巧两边都没写」。**一处判据的预言力被追到底并如实记进用例**：`nodeStopOverride` 与 `contextDirective` 按源码注释本就**冗余**，单打一项另一项顶上（纵深防御，不是覆盖空洞）；两项一起打掉**仍不红**——因为 STOP 那句话根本不从那条 oracle 来，而来自 `shouldInjectStopNotice` 的独立 trailer 路径。打掉 trailer ⇒ 红；强行把追问通道打开 ⇒ 红在等待里（节点继续提问、永远不 done，正是用户看到的形态「开关按了它还在问」）。四次变异的结论逐条写进注释，免得下一个人以为这条断言比实际更锋利。**另一处判据的取材也被修正**：裸的 `<workflow-clarify>` 子串**也出现在**「释放」那句话里（`do NOT emit another <workflow-clarify> envelope`），拿它做否定断言永远不会红；改取协议块独有的 `<workflow-clarify nonce="`。账本口径顺带收窄：HUMAN-07 原标题写「自澄清 / 跨节点文案不同」，本条只覆盖自澄清那一支。能力 gap 574 → **572** |
| 2026-08-25 | **B50（改派的两道守卫防的都是「任务安静地卡死」）**：新建 `e2e/task-question-reassign.spec.ts`，一条 P2（HUMAN-22）。改派是「这题该由谁来答」的唯一调整手段，它的两道守卫防的都是死锁，而死锁在这个产品里的表现是**任务安安静静地永远不动**：①目标必须是工作流里的 **agent 节点**——改派到 clarify / review / io / wrapper 看起来毫无问题（都在图上、都有 id），它们没有的是提示词与产出契约；守卫失守时界面上一切正常：改派成功、条目换了承接人、任务继续等着，等到天荒地老。②**人工提问的目标必须至少跑过一次**——人工提问靠「重跑承接节点」落地，改派到从没跑过的节点，§18 的 park gate 会把任务停在一个下发永远铸不出重跑的目标上。两条判据各补一句**被拒之后条目原样不变**（先落库再报错同样返回 4xx，那种实现会把条目留在非法承接人上），外加一条正向对照（改派到跑过的 agent 节点必须通过——否则「对任何目标都拒」也能让上面两条成立，而那等于功能不存在）。fixture 特意加了一个**下游** agent 节点 `spare`：任务停在澄清门上时它一次都没跑过，正好用来打第二道守卫。**两条变异咬中**：`canReassign` 绕开、never-run 守卫绕开。**账本口径同样收窄**：原标题捆着 added-designer / removed-designer / moved-manual 三条子路径，本条只覆盖 moved-manual 与两道守卫，标题改写成「（added/removed-designer 另计）」。能力 gap 575 → **574** |
| 2026-08-25 | **B49（任务问题看板：下发那一步空转，任务就永远不动了）**：新建 `e2e/task-questions-board.spec.ts`，三条（HUMAN-21 / 23 / 24，P2·P2·P3）。看板把一轮反问拆成逐题条目，让人分批决定谁答、何时下发；每一段都可能静默卡死整条任务，而**这一段的所有失效形态都返回 200**，所以判据一律取「任务状态真的动了没有」。五条判据：人工提问进得了看板且**把任务扣住**；空 entryIds 的下发被拒且状态不变（「下发了个寂寞」与「下发成功」在界面上同形）；真下发之后任务必须离开等人态；跨任务的条目 id 在 confirm/stage 上都 404 且不动到对方任务；承接 run 跑完后条目进「已处理待确认」，确认只关掉被点名那一条。**两处真实契约在写的过程中被打脸**：①人工提问**故意**建成 `staged` 而不是 `pending`（`services/taskQuestions.ts:1383-1387`，§15：必须有 handler ⇒ 建出来就待下发，由 park gate 扣住任务），第一版按直觉断言 `pending` 当场红；②**没答过的题不许推进待下发**（`stageTaskQuestion` 的 `isEntrySealed` 前置，409）——放行的话，一道没人答过的问题会被下发给 agent，它拿到空答案、产出自然错，而看板上写着「已处理」。这条前置本身补成了一条判据。还锁了 unstage 的**题级**语义：它按 (originNodeRunId, questionId) 级联，但不许波及另一道题——源码里记着那次用户报障「回答问题的按键又没了」，半 staged 的题会从答题面板消失且无法重答。**五条变异全部咬中**：人工提问不 staged、不校验条目归属、下发不释放门、未封存放行进待下发、unstage 放大成轮级。**账本口径顺带订正**：HUMAN-21 标题里的「复制问题」是一个**已不存在的功能**（`TaskQuestionList.tsx:166-167`：2026-07-02 按用户拍板移除），标题改写，不让账本继续散播它。HUMAN-20（按来源节点过滤 + 五列看板）**保持 gap**——本条没碰那两个界面面，不整行认领。另：本条真跑到了 5 个此前从未被 e2e 命中的端点（manual / confirm / reassign 之外的 stage / dispatch 等），R1 端点账本会在下一次 e2e-full-nightly 的实测对账里自然下降，这里不手改。能力 gap 578 → **575** |
| 2026-08-25 | **B48（人正看着任务，怎么知道该做什么）**：新建 `e2e/task-detail-human-gate-entry.spec.ts`，一条 P2（HUMAN-X1）。前面几条锁的是「待办清单对不对」（B42/B43）与「角标亮不亮」（B41）；这一条锁的是**另一条真实路径**——人正在看某个任务，想知道「它卡在哪儿、我该做什么」。断掉的形态很朴素也很致命：节点表里那一行只显示一个状态 chip，没有任何可点的东西，人得自己猜 `awaiting_human` 是什么意思、再绕到收件箱里翻。多数人不会绕，任务就那么停着。两条判据都带**负向对照**：跳转按钮只该出现在真正卡人的那一行上——每行都挂一个「Answer」在功能上等于没有指示，人会按错、落到与他无关的节点上；答完之后那一轮的入口必须**收回去**，否则人点进一个只读页面还以为自己有事没做。fixture 用一条同时挂澄清通道与评审节点的工作流，一个任务连着走完两道门（先等人回答、答完重跑再等人评审），两道门因此在同一页上先后出现。**两个方向的变异都咬中**：跳转按钮整个去掉、以及每一行都挂。**账本口径顺带收窄**：原行标题捆着「画布待答问题角标」，本条只覆盖节点表那两个按钮，标题改写成「（画布待答角标另计）」，不让整行 covered 把没测的那半抹掉。能力 gap 579 → **578** |
| 2026-08-25 | **B47（决策前那个弹窗是止损闸，不是礼貌）**：新建 `e2e/review-decision-confirm-dialog.spec.ts`，一条 P2（HUMAN-35）。三种被它挡住的损失在没有它时全部静默：①带着意见按「通过」——意见只在迭代/驳回时才进重跑提示词（B45），通过意味着它们一个字都不到 agent 手里，而人刚写完它们、会以为「我提了所以会被处理」；②一条意见都没有就按「按意见修改」——agent 收到**空**清单，重跑出几乎一样的东西，B45 里那个没有报错面的死循环起点常常就在这里；③驳回不写理由——必须**当场拦下、一个请求都不发**，把空理由送到服务端再吃 4xx 在界面上同形，但用户已经以为自己驳回了。另加一条正向：弹窗要预告**哪些节点会回滚重跑**，人据此判断这一下要扔掉多少工作。**反向对照是这条用例的地基**：没有意见也没有草稿时按通过**不该弹窗**——少了它，「永远弹窗」也能让上面每条成立，而永远弹窗会训练人闭眼点确认，等于把这道闸拆了。还锁了「取消就是取消」：按下取消后这一轮必须仍待评审（弹窗关了但决策已发出去，是这类确认闸最恶劣的坏法）。**三条变异咬中**：approve 不再问、iterate 的空清单警告被抹掉、空理由改由服务端去拒。能力 gap 580 → **579** |
| 2026-08-25 | **B46（看旧版本时，决策按钮不能还在那儿）**：新建 `e2e/review-historical-version-view.spec.ts`，一条 P2（HUMAN-38）。评审页有两种截然不同的用途却共用同一个 URL 形状：**决策当前这一版**，与**回看某个旧版本**。分不清的后果只有一种，而且不可逆——人以为在看当前稿于是按下「通过」，实际通过的是三轮以前那一版；或者反过来，看着旧版的问题去驳回一份早已修好的新稿。两种都不报错，因为决策本身完全合法。所以判据不是「横幅显示了吗」，而是**写操作到底还在不在**：RFC-013 的做法是历史模式下决策按钮**根本不进 DOM**，而不是 disabled——disabled 的按钮仍在讲「这里可以决策，只是现在不行」，而真相是这一版早已决策完毕。另两条同样静默：非法版本号必须**说出来**（静默回落比看错版本更坏，因为人有明确的「我已经切过去了」的预期）；拿别条评审的版本 id 来问不能把那份文档渲染出来（既是越权读，也是一次张冠李戴的误导）。正向对照先做——当前版上三个按钮都在，否则「这三个按钮任何情况下都不渲染」也能让断言成立。**三条变异咬中**：历史模式照样渲染按钮、未知版本静默回落、不校验版本归属直接进历史模式。fixture 需要「v1 已决策 + v2 待评审」两版历史，因此先迭代一轮，并复用 B45 学到的「按 id 认新起那一轮」。能力 gap 581 → **580** |
| 2026-08-25 | **B45（评审意见有没有真的到 agent 手里）**：新建 `e2e/review-comments-to-rerun-prompt.spec.ts`，两条 P2（HUMAN-33 / HUMAN-34）。评审意见是「人告诉 agent 该改什么」的唯一通道，迭代之后 agent 凭什么改——全靠这批意见被渲染进重跑提示词。断掉的形态完全静默：决策 200、任务确实继续跑、agent 确实重跑了一遍，只是**没收到任何意见**，于是产出与上一版基本一样；评审的人看到「又是这个」再迭代一次，一个没有报错面的死循环，每一轮都烧真实的模型调用。与 B39 是同一条判据在评审侧的镜像：判据取**提示词本身**，因为「写进库了吗」在库那一层与「渲染错了」完全同形。三条：意见增/改/删（改与删都断言「另一条不受影响」，只断言新值在的话把两条一起改掉也能过）；迭代后两条意见的正文**与锚点**都进提示词并归档进那一版的 `commentsJson` / `decisionReason`；驳回理由必填（空理由被拒且**状态原样不变**——先落库再报错同样返回 4xx）且理由同样进重跑提示词。**三条真实契约实撞**：①默认 stub 只发 `answer` 端口，声明 `design` 会得到一份**空文档**，评审无正文可锚——退化 fixture；②迭代一落库重跑行就以 `pending`/`startedAt=null` 出现，按行数等会读到空壳；③换成「数 status=done」同样不行——迭代会把原来那轮回滚成 `canceled`，done 的行始终只有一条。最终按 **id 认人**：记下决策前的行 id，等一条不在其中且已 done 的行。**一条断言被自己证伪后删掉**：原打算锁「决策后行侧 review_comments 被清空」，变异（去掉那句 delete）**不红**——决策后读的是新那一版的 id，陈旧行永远不会重现，「否则下一轮重复渲染」是编出来的危害。与其留一条空断言冒充覆盖，改成锁归档快照（该变异当场咬中），并在用例里写清为什么不锁前者。能力 gap 581 → **579** |
| 2026-08-25 | **撤回一处「提前发布」的覆盖声明（DE-18 / DE-39）**：CI 在 `90fa0eef8` 上红在 RFC-319 R3 的「证据必须逐字可达」——账本里 DE-18 / DE-39 标着 covered，证据指向 `e2e/rfc310-digital-employee-journey.spec.ts` 里一条**只存在于并发 session 工作树**的用例标题。那两行是 RFC-323 写的，但**是我发布的**：B38 提交整份能力账本时把它们一起带上了主干（同一个 pathspec 陷阱，见 dev-gotchas 新条）。撤回不是删别人的工作——他们的改动原样留在工作树，主干只是不该在用例落地前先声称覆盖。两行改回 gap，配合本批新增的两条覆盖，gapIds 净持平 581，基线不动。RFC-323 落地时把这两行连同用例一起提交即可。同一次 CI 还有一条 `Markdown link check (design/)` 红——`design/plan.md` 的 RFC-323 索引行指向尚未进主干的目录，来自并发 session 自己的提交 `1e3cdf1e4`，内容归属明确，留给他们（替别人写 RFC 目录不是我该做的决定） |
| 2026-08-25 | **B44（未落盘的草稿被拦下时，那三个按钮各自兑现什么）**：新建 `e2e/clarify-unsaved-draft-guard.spec.ts`，一条 P2（HUMAN-46）。这道闸只在一个很窄的窗口里有意义——本地草稿写下去了、还没落盘确认（本地写**不带防抖**，防抖只加在服务端队列上）。窗口平时几毫秒，一旦变宽（私密模式 / 配额吃紧 / 机器忙）用户正好点了别处，刚打的字就直接消失且界面毫无提示，是「无声丢工作」的标准形态。更要紧的是「保存并离开」的**失败关闭**语义：必须先确认草稿真落了盘才放行；先跳转再去存的话，存失败时人已经在别的页面上，损失与没有这道闸一样，却多了一句「已保存」的假回执。**确定性前提**是这条用例的核心设计：把 `clarify-drafts` 写事务的 `oncomplete` **扣住不发**（数据照常落盘，只是不告诉页面）。这不是伪造状态——它就是 `dirtyRef` 的定义（`latestGeneration > localAckGeneration`），也正是这道闸存在的理由；不扣就只能靠抢那几毫秒，那种「本地绿 CI 红」的用例本仓不写。三条判据：拦下时**不能已经跳走**（弹框 ≠ 拦住）、留下后答案还在（「拦住了但把表单清了」是换一种方式丢工作）、「保存并离开」在回执未到时必须停在原地、回执一到就走完那次被拦下的导航。**实撞一处真实契约**：三条用例复用同一轮次，而 RFC-099 D8 的服务端逐题草稿是协作事实源、会被下一条用例载入为基线——再「改」成同一个值时 `recordChange` 判为无变化直接返回，页面根本不脏，于是整条用例会在一个**没有前提**的状态上断言（第三条首次运行即红在「指示器应为 saving」）。改为每条用例用各自唯一的自定义文本。**两条变异咬中**：保存改成先跳转后保存（fail-open）、脏态跟踪整个关掉。能力 gap 582 → **581** |
| 2026-08-25 | **B43（评审收件箱列表页：五态过滤与逐行版本史）**：新建 `e2e/review-inbox-list.spec.ts`，一条 P2（HUMAN-41）。与 B42 同形，但评审这一侧代价更高——澄清答错还能再问一轮，评审是**放行或打回**的那一下，而这一页决定了人去看哪一份、看的是第几版。三种静默失效：①过滤没接上（待评审混进已决策 ⇒ 点进去动不了；反方向 ⇒ 「上一版为什么被打回」查无对证，而那正是下一轮 agent 的修改依据）；②**展开取错了行**——每行历史按 `nodeRunId` 懒加载，键写错时页面照样展得开，只是给你看**另一份文档的版本史**，人据此判断「这版比上版改了什么」，而两份根本不是同一条链，且没有任何报错面；③把「读不到」画成「没有」。判据同样靠双 fixture（两个任务、两条评审、各自不同的评审节点 id）：单条 fixture 下「任何页签都返回全部」也能过，所以「已驳回里两条都不在」这条负向断言必须在。展开那条还断言：展开前谁也没有历史面板（否则「永远展开」也成立）、另一行仍是收起的（展开态是 per-row 不是全局开关）、收起后面板消失。**三条变异咬中**：filter 不进 query、展开态改成全局、历史数据被掺进第二份。能力 gap 583 → **582** |
| 2026-08-25 | **B42（澄清收件箱列表页：给出的清单是不是对的）**：新建 `e2e/clarify-inbox-list.spec.ts`，一条 P2（HUMAN-13）。这一页是「我该去回答哪一条」的唯一入口，它坏起来不报错，只**给出一份错的清单**：①过滤没接上 ⇒ 待回答页签把答过的也列出来（人点进去发现是只读的，几次之后就不信这一页），反方向更糟——已回答空着，「这题当初怎么答的」查无对证；②分组串了 ⇒ 甲任务的问题挂在乙任务名下，而回答的人正是照着任务上下文判断的，串组等于让他在错的上下文里作答，答完也没有任何提示；③把「读不到」画成「没有」⇒ 人直接下班了，而真实情况是有待办在等他。**关键是用两个任务而不是单任务 happy path**：单任务下分组永远只有一节，分组键写成常量也照样绿——第二条变异（分组键换常量）正是靠双任务才咬中的。三条判据各带负向断言：甲的行不许出现在乙那一节里；只答 alpha 留 beta 作对照（否则「页签根本没生效、永远返回全部」也能让「已回答里有 alpha」成立）；故障态可见**且空态必须不在**，重试要真的把列表拉回来（只有按钮存在不够）。**三条变异咬中**：filter 不进 query、分组键写成常量、错误被画成空态。能力 gap 584 → **583** |
| 2026-08-25 | **B41（待办角标接的是不是真的待办）**：新建 `e2e/inbox-badge-live.spec.ts`，一条 P2（HUMAN-14）。角标是这个产品里唯一一个**不需要人主动去找**的通知面，任务停在等人回答时全靠它把人叫回来；它的失效因此天然静默——不亮就是不亮，没有报错没有日志，任务就停在那儿直到有人碰巧点开收件箱。**与既有覆盖的差别是这一条的全部理由**：`a11y` / `nav-redesign` / `visual-regression` 三条用例都开过这个抽屉，但喂的是 `page.route` 造的假数据（`routePopulatedInbox`）——它们证明的是「给它数据它画得出来」，不是「真有一条待办时它拿得到」；三条端点接错、权限过滤把自己的任务滤掉、查询条件写反，在假数据下全都照样绿。所以这里全程用真 daemon 跑出来的真轮次。两条：①任务停在等人回答 ⇒ 角标亮出真实计数、抽屉列得出那一行（行 testid 用轮次 id、导航却用 intermediary node_run id，写混时抽屉照样画得出来、只是点进去 404）、点进去落在那一屏且题目真的在；②待办没了（任务取消 ⇒ 终态清扫封存）⇒ 角标**自己**消失，不刷新页面——页面上留哨兵变量验证确实没重载。第②条是唯一 e2e 才验得出的部分，也是最容易坏而没人发现的：轮询掉了角标会一直挂着一个早就不成立的数字，人点进去发现什么都没有，下次就不信它了；它同时是第①条的反向对照，证明那个「1」不是一块永远亮着的死牌子。**三条变异咬中**：角标不算 clarify 那一路、`refetchInterval` 被掐掉、行的 navigationId 写成轮次 id。能力 gap 585 → **584** |
| 2026-08-25 | **主干二次红：同一个 git 陷阱**：B39 那笔本想只提自己那一处基线改动，为此先用 `git hash-object -w` + `git update-index --cacheinfo` 造了只含自己 hunk 的 blob，还 `git show :<path>` 验过——**然而 `git commit -- <路径>` 读的是工作树、不是 index**，那两步对它毫无作用，并发 session 尚未落地的降幅（216/13）第二次被提上主干，T16 再红。已 30 秒最小复现实证并落 `docs/dev-gotchas.md`：带 pathspec 的提交会重新从工作树读取这些路径，于是仓规里「提交必须带 pathspec」（防的是别人**已 add** 的在制品）反过来必然带走别人**未提交的工作树改动**，而 `git diff --cached --stat` 恰恰验不出这件事——它看的是被忽略的那一半。可用定式：能不碰就不碰；必须碰时「写入 → 提交 → 写回并 `cmp` 验字节」；**提交后一律 `git show HEAD:<file>` 复验**（两次事故缺的都是这最后一步）。本次按此定式把端点 / 路由基线复位到主干实测值并重挂具名 allowGrowth |
| 2026-08-25 | **B40（轮次被系统封存之后：只读、说明原因、不再收答案）**：新建 `e2e/clarify-round-sealed-readonly.spec.ts`，一条 P2（HUMAN-17）。澄清页是**多人同时看着的一屏**，任务在别处走到终态时，那些开着的页面上不会有任何东西自己变——除非服务端封了轮次、页面据此转成只读并说明原因。三种失效全静默：①封存**没跑** ⇒ 收件箱与角标里永远挂着一条死任务的待办，谁点进去回答都是白费（2026-07-16 UX 审计 §1 R8 逐字记着「answering was pointless (or worse, the answer committed and then errored)」）；②封了但页面仍是**裸的只读表单** ⇒ RFC-202 T6 点名的形态，footer 还在说「草稿已保存，可安全关闭标签页」；③封了也只读了但**不说为什么** ⇒ 人分不出「任务结束了」与「工作组把反问撤了」，而两者后续动作完全不同。判据五段：轮次转 `canceled`、`sealedCause` **逐字**等于 `task-canceled`（取自 park 载体 run 的 errorMessage，**不是**任务此刻的可变状态——按当前状态反推会把「取消后又重试」的历史轮标成工作组撤销，这条在后端与前端各写了一遍注释）、此后作答被 409 拒绝、界面两个提交键都 disabled 且 footer 改口、原因横幅是任务终态那一条而非通用兜底或工作组版本。正向对照不能省：封存**之前**先断言这一屏确实能答，否则「页面永远只读」也能让上面每条成立。**四条变异全部咬中**：`sealedCause` 不外露、横幅退回通用文案、终态清扫钩子未接线（轮次永远停在 awaiting_human）、外层拒绝守卫放开。**顺带两件事**：（a）第四条变异暴露出「能不能答」其实是**双保险**——外层放开后更深的 `sealRoundQuestions` 仍以 `clarify-round-terminal` 拦住；但也因此看清外层那个码**把原因说反了**：被取消的轮次拿到的是 `clarify-already-answered`，而没有人答过它。取消前就打开页面的那个人点提交，会去找一个并不存在的同事。不是安全问题、只是措辞，且改它要连三条调用路径的判据一起过，超出「补 e2e 覆盖」的范围 ⇒ 记进 `docs/audit-backlog.md`，并在用例里注明「这条锁的是当前实际行为，改了会红」。（b）**账本拆行**：HUMAN-17 原本把两个封存来源捆在一行，而工作组自主撤销走的是 `workgroupLifecycle.ts` 的**另一条实现**（`terminalSweep.ts:24-28` 注明刻意不合并）。整行标 covered 会把没测的那支从视野里抹掉，故拆成 HUMAN-17（任务终态，covered）+ **HUMAN-17b**（工作组撤销，仍 gap，附下一步 fixture 方案），`findings.json` / `findings.md` 同批更新，分母 820 → **821**（T2 口径：只订正分母、不重跑审计）。gap 计数因此净持平：585 → 584（-HUMAN-17）→ **585**（+HUMAN-17b），基线不变 |
| 2026-08-25 | **B39（澄清答案的类型矩阵：用户点的东西有没有原样穿到 agent 手里）**：新建 `e2e/clarify-answer-type-matrix.spec.ts`，一条 P2（HUMAN-X2）。这条链上真正决定成败的不是「点得动」，而是答案落进**下一轮提示词**时的形态——这一段没有任何报错面：作答 200、轮次照样封存、任务照样重跑，中间捏错了 agent 就拿着一份「看起来完整的错答案」继续干活，而界面上写着「已回答」。判据一律取**提示词本身**（`GET …/node-runs/:id/session` 的 user 消息，即 RFC-311 externalize 后经 `readNodeRunPrompt` 双读解析的正文），那是 agent 真正读到的字节。两条用例：①走真实界面——单选题点「其他」行打字、多选题勾满两项**再**补一段说明，断言提示词里是 `User chose custom answer: "…"` 与 `User selected: "TypeScript", "Python" with additional note: "…"`，且不许冒出并未选中的候选项；②走端点——空答 + 越界 index + **客户端伪造的 `selectedOptionLabels`**，断言伪造标签一个字节都不进提示词（服务端按 index 重算，`services/clarify/service.ts:849-850`）、两题各自如实上报「没答」而不是把题吞掉。**顺带订正一处账本口径**：标题里的「必填校验」在产品上已废弃——`shared/schemas/clarify.ts:69-71` 明写「submit accepts empty answers」，所以正确契约是「如实上报没答」而不是「拦住不让提交」，账本标题按源码改写。取证一律带 `path:line`。**五处变异全部咬中**：多选改成覆盖而非并入、`summariseClarifyAnswer` 多选只留首个标签、服务端信任客户端标签、未答题目整条从提示词里消失、单选自定义文本被当成没答。自证两条防「断言打在第一轮上」：提示词必须同时含 `## Clarify Q&A` 与 `User directive: STOP CLARIFYING`。能力 gap 586 → **585** |
| 2026-08-25 | **两条 CI 红的收尾（都不是新功能，但都是本 RFC 的推带出来的）**：①**基线被提前提交**——B38 那笔为记自己的账本改动提交了整份 `ledger-baselines.json`，把共享工作树里并发 session（RFC-323）**尚未落地**的两处降幅（端点 220→216、路由 16→13）一起推上 main，于是主干上「基线」低于「实测账本」，RFC-317 T16 五个 backend 分片一起红。复位到主干实测值并按 T17 写具名 `allowGrowth`；留着 216/13 会让 `e2e-full-nightly` 的实测对账反过来红——那条腿拿真跑出来的命中集合逐条比对账本，账本说 216 而实测 220 时它没办法判断哪边说谎。**教训**：共享 index 下即使精确 `git add`，整文件提交仍会把别人的在制品一起带走；混文件的处置见下一条——**这里写的「造 blob 进 index」是错的**：`git commit -- <路径>` 读的是工作树、不是 index，照这个做第二次又把同样两行提了上去。②**`rfc253-script-node` 在 macOS 分片间歇红**——本地单跑与 `--repeat-each=8 --workers=2` 全绿，典型「重跑就过了」形状，因此没按 flaky 处理而是翻 trace：命中测试那一帧两个节点的 style 都是 `visibility: hidden`，此前 60 多帧一直 `visible`，翻转就发生在点相机概览（fitView）之后。xyflow v12 在节点被测量出来前渲染成隐藏，而 fitView 会把已渲染节点**打回未测量态**；窗口里 `boundingBox()` 照样给坐标、`elementFromPoint` 却跳过隐藏元素，于是稳定器 settle 在一个**永远命中不到**的点上。修法：稳定器同时等「可见」与「几何不动」；命中断言改成报出实际拦截者（裸 `false` 分不出「浮层遮挡」与「未测量态」）。红→绿由本地确定性复现验证（注入 `visibility:hidden` 把窗口钉死）。通用规律已落 `docs/dev-gotchas.md` |
| 2026-08-25 | **B38（决策落库但踢不动时不许假装成功）**：新建 `e2e/review-resume-failure.spec.ts`，一条 P2（HUMAN-18）。提交评审决策做两件事：**记下决策**（本地事务、几乎不失败）与**把任务踢回去继续跑**（碰工作树、起进程，真会失败）。两者失败语义不同，不能合并成一个成功/失败：决策确实落库了（2xx 是对的），但任务并没有继续——响应若对此只字不提，reviewer 看到「已通过」就走了，任务从此停在原地，**没有任何人会再回来看它**。这正是审计 R4 点名的「伪成功」：真失败沉进 daemon 日志、HTTP 那头一片祥和。触发真失败的方式是把任务工作树从磁盘删掉——`resumeTask` 的 410 前置检查（`task-worktree-missing`）守的就是这个形态，也是源码注释里点名的第一个例子。判据三段：决策**确实**落库（不该因为踢不动就回滚）、响应带 `resume.ok===false` 与可归因的 code、任务**没有**被说成 done。正向对照不能省：工作树健在时不带 `resume` 字段，否则「永远报警告」也能让上面三条成立——这条正是第二个变异打红的。两条变异咬中。能力 gap 589 → **588**，covered 90 → **91** |
| 2026-08-25 | **B37（RES-28 的根因修复：那条用例一直在赌）**：CI 上 `mcp-acl-session-termination` 又红成 `session-unusable`。这次不猜时序，取证：给 daemon 传自己拥有的 home、跑完读 `db.sqlite` 的 `mcp_runtime_test_turns`——`status=failed / exit_code=3 / stderr=「prompt is missing the RFC-200 envelope nonce」`。**根因不是慢，是 stub 压根没在跑**：MCP runtime-test 是独立 feature、提示词**不带 RFC-200 信封**，而 `mode-slow` 一进来就 `requireOutputOpen`，子进程 76ms 自己退出。撤权那条事务只处理 `status=active` 的会话（只有回合在飞时才是 active），于是本机赢竞态就绿、CI 输就红。**此前两次「修 flaky」都只是在把窗口调宽**——换 `slow` 模式、加 `STUB_OPENCODE_SLEEP_MS=60000`，而那个 sleep 排在信封检查后面，一次都没执行过。修法是把生命周期**观测化**而不是赌：`mode-slow` 新增 `STUB_OPENCODE_HOLD_FILE`（stub 起来先落 `<hold>.started`，调用方轮询到它才动手，然后一直挂着直到调用方删文件），且该块**必须排在 `requireOutputOpen` 之前**。改后实测：turn 变 `interrupted / exit_code=143`（被 SIGTERM 掐断）、stderr 为空，`--repeat-each=3` 稳定绿；`slow` 的 155 条差分 golden 不受影响（无该环境变量时字节不变）。通用坑与排查手法已落 `docs/dev-gotchas.md` |
| 2026-08-25 | **B36（澄清的 defer 控制通道）**：新建 `e2e/clarify-control-channel.spec.ts`，一条 P2（HUMAN-19）。同一个端点上挂着两条语义完全不同的通道：快速通道回答**整轮**、封存即下发、任务继续跑；控制通道（`defer:true`）只封存被点名的几题、**不铸重跑不推进任务**。两个只在控制通道有意义的字段（子集上限 `questionIds`、重答声明 `resubmitQuestionIds`）若被允许配在快速通道上，后果是**静默丢题**——服务端按子集作答却仍把整轮判为已回答并铸出重跑，没被点名的问题从此永远停在那儿，而调用方收到 200。判据三段：两条非法组合各自**被点名拒绝**（两个不同 code——共用一个的话界面无法区分「不能带子集」与「不能重答」）且看板逐字不变；控制通道真的只封存不下发（被点名那题进 `staged`、另一题不动、任务仍 `awaiting_human`）。三条变异咬中（两条拒绝各自短路、控制通道忽略子集上限）。能力 gap 590 → **589**，covered 89 → **90** |
| 2026-08-24 | **B35（人工门的两把乐观锁）**：新建 `e2e/human-gate-optimistic-locks.spec.ts`，两条 P2（HUMAN-08 / HUMAN-31）。人工门是**多人同时看着同一屏**的地方：任务停在澄清或评审上，任务成员都能打开它。没有这把锁，后到的提交会**静默覆盖**先到的——两边界面都显示「已提交」，而 agent 只拿其中一份重跑；评审侧更隐蔽：驳回理由与重跑节点都挂在决策上，覆盖掉的不只是一次点击，是下一轮 agent 的**修改依据**。两条判据同形：拿过期轮次号提交必须 409，且**被拒之后服务端状态逐字不变**——只断言状态码不够，「先落库再返回 409」同样能让状态码正确；收尾各跟一次正确轮次号的提交作正向对照。两条变异咬中（两把锁分别短路）。**两处实测契约**：①评审决策值是 `approved` 而非 `approve`（`shared/schemas/review.ts:134`），写错的话 422 会在**乐观锁之前**拦下请求、判据打不到目标——第一版正是如此；②`clarify` stub 的轮次计数是个标记**文件**，缺省落在 `/tmp/aw-e2e-clarify-state`，比 daemon 和整次运行都活得久：不给每次运行独立的 `CLARIFY_STUB_STATE`，第二次跑时 stub 认为「已经问过了」直接出最终输出、任务根本不停在等人上，表现为「第一次绿、第二次在轮询上超时」的假 flaky。`--repeat-each=3` 连跑 6/6 稳定。能力 gap 592 → **590**，covered 87 → **89** |
| 2026-08-24 | **B34（推送被拒的两条出路）**：新建 `e2e/commit-push-rejection.spec.ts`，一条 P1（REPO-18）。自动提交推送是一条**用户不在场**的链路，推被拒时只有两种可接受结局、且都必须保住那次提交：非快进 ⇒ 取回、合并、再推，**绝不能力推覆盖**（那会悄无声息删掉别人刚推上去的提交，而任务照样报成功）；认证被拒 ⇒ 不再重试，改动**留在本地提交里**并如实报出降级结局（最坏的不是「没推上去」，是「连本地提交都没留下」——那次产出就此消失，工作树随后会被回收）。判据两边都读：node_run 的结局 + **远端仓库里真实的树**，只断言 `pushOutcome` 不够，力推同样能让它显示 `pushed`。两条变异咬中（非快进后跳过 merge、auth 改判 repairable）。**前提做了两轮才做对，两次都记进注释**：①先建好分支再跑，任务会直接基于远端分支建工作树，`repairAttempts` 是 0、一次就推上去——根本没有非快进，判据空洞；②改用服务端钩子 `update-ref` 把分支挪过去，被 git 当场拒绝（`ref updates forbidden inside quarantine environment`）。最终形态是**钩子在第一次推送上阻塞、测试侧趁窗口把别人那笔真的推上分支、再放行钩子回绝**——于是「取回 → 合并 → 重推」合的是真的别人的提交；注入的只有回绝文案，而那正是 `classifyPushFailure` 唯一消费的输入。能力 gap 593 → **592**，covered 86 → **87** |
| 2026-08-24 | **B33（wrapper 归属拖拽）**：新建 `e2e/canvas-wrapper-membership.spec.ts`，一条 P1（WF-16）。wrapper 归属不是装饰——它决定这个节点**跑几次、在谁的工作树里跑**：拖进 loop 的节点每一轮都重跑，拖进 git wrapper 的节点它的改动才被算进那次 diff。而归属只由**落点落在谁的矩形里**决定，事后从静止的画布上看不出「它到底进组了没有」——压在 wrapper 上面和真的属于它长得一模一样。判据落在保存后的 `wrapper.nodeIds` 上，且**两个方向都测**：只测「拖进去」的话，一个「只增不减」的实现同样能过，而那意味着节点一旦沾过 wrapper 就再也出不来。两条变异分别咬中两条用例（`leaveWrapperId` / `joinWrapperId` 各置空）。一处操作细节：相机动画结束前抓到的坐标会在 `mouse.down` 之前失效、拖拽根本不会开始，取全图视角后要等一拍。`--repeat-each=2` 连跑 4/4 稳定。能力 gap 594 → **593**，covered 85 → **86** |
| 2026-08-24 | **B32（提交策略步：原地修改 vs 复制一份）**：新建 `e2e/intent-apply-mode.spec.ts`，一条 P1（INTENT-39），并给 e2e stub 加了 `intent` 的 `update` 变体。这一步问的问题只有一个，但它决定的是**谁被改了**：「修改」就地改掉那个资源、改动立刻对所有引用它的工作流 / 工作组生效；「复制一份」铸新的、原件一个字节不动。选反的后果完全不对称且静默——想复制却改成了原地，别人的资源被覆盖，界面只显示「已提交」。判据因此**两边都读**：改动去了哪个 id、以及**另一个**有没有被动过（只断言「新的那个对了」不够，原地修改同样能让它成立）。两条变异咬中（applyMode 恒定 modify / 恒定 copy，各自打红对面那条用例）。**这一步此前根本走不到**：所有 intent stub 变体产出的都是 create，而策略步只在存在 update 操作时出现；新变体按用户消息里的 `rfc319-target:<name>` 从工作目录清单里认出会话句柄——真实模型读的是同一份东西。搭这条链路时撞出**三处产品契约**，逐条记进注释：①update 的目标必须**已挂载**，只在清单里可见会被判`intent-target-not-mounted`（因此入口改走资源详情页的「Modify via intent」）；②intent 的 update **不支持改名**（`intent-rename-unsupported`，改名要走副本上的 finalName 槽），所以「谁被改了」只能靠 description / bodyMd 区分；③会话正文不在 CLI 提示词里而在工作目录的 `INTENT.md`——stub 第一版去提示词里找目标名，找不到、当场退出 4。能力 gap 595 → **594**，covered 84 → **85** |
| 2026-08-24 | **B31（code-host-call 检查器）**：新建 `e2e/code-host-call-inspector.spec.ts`，一条 P1（WF-36）。这个节点是平台里**唯一一个由框架自己直接调用外部代码平台写接口**的东西——没有 agent、没有模型、没有子进程，凭据也不进任何 prompt；检查器里的每一格都直接决定「平台会用管理员配好的令牌去真实 GitLab / GitHub 上做什么」。两条最危险的形态各锁一条：①破坏性方法若不需要显式授权就能选中，一次手滑就删掉真实分支 / 评论，而这类操作**没有撤销**；②参数绑定 `{{port}}` 若没原样落库，节点看着配好了、跑起来打的是另一条 MR，外部平台上的副作用是真的。第三条锁的是**状态层**：取消授权时已经选好的 DELETE 会被改回 GET——只把开关关掉、把 DELETE 留在定义里是最坏的一种，因为界面显示「未授权」而定义里躺着一条破坏性调用，下一个人读界面读不出来。服务端那层（`code-host-method-forbidden`）已有单测，不重复。三条变异咬中，**其中一条第一次没咬中并因此改了变异点**：参数字段 `mr`/`body` 是 text/textarea 控件，我先变异的是 select 分支——打错分支的变异等于没做，改打 textarea 分支后立刻转红。能力 gap 596 → **595**，covered 83 → **84** |
| 2026-08-24 | **B30（连线弹窗）+ REPO-35 销账**：新建 `e2e/canvas-connection-dialog.spec.ts`，一条 P1（WF-19）。画布上一条线看起来只是一条线，但它落进定义里的形态由弹窗里的几个选择决定，而这些选择**事后在画布上看不出区别**：「新建输入」与「复用已有输入」画出来一模一样，区别是后者会**顶掉**原来接在那个端口上的边；分片扇出边界上的「分片 / 广播」决定每个分片拿到的是**一项**还是**整份**，接错时任务照样跑完、每个分片处理了全量数据、零报错。判据一律落在**保存后的定义**上（读回 `/api/workflows/:id`），弹窗里的替换预告 / 降级预告只作为「按下确认前被告知过」的补充。三条变异全部咬中：目标端口名被忽略、复用被当成新建、广播被当成分片。四处实测契约：①`inspector-connect-next` 只在紧凑检查器里渲染，宽视口下检查器是常驻侧栏、根本没有它——入口要走**节点右键菜单**；②目标节点选项的标签是 `<agentName> (<nodeId>)`；③分片源必须声明 `kind: list<T>`，否则弹窗以「Choose one list<T> input as the shard source before adding broadcasts」拒绝提交（**产品把原因直接写在界面上**，省了一轮排查）；④画布是 transform 视口，视口外的节点在 DOM 里存在却点不到而 `scrollIntoView` 无效——先取全图视角。同批**REPO-35 销账**：它此前因「测了并发 session 尚未上库的端点」退回 gap，现在 `routes/accountRepositoryTransportCredentials.ts` 已进库，而 RFC-321 自己的 `e2e/rfc321-code-push-credentials.spec.ts` 已完整覆盖（掩码 / Alice-Bob 隔离 / 最大权限 PAT 四路全拒），账本记的是**证据**不是作者，直接标 covered。能力 gap 598 → **596**，covered 81 → **83** |
| 2026-08-24 | **B29（修掉那个缺陷 + 补完融合后三环）**：用户拍板在本 RFC 内直接修，于是 RFC-319 出现**第一处也是唯一一处生产改动**。先红后绿：新建`packages/backend/tests/rfc319-fusion-manifest-merge-back.test.ts` 断言融合任务把结果清单登记进 force-include 名册（`tasks.platform_input_paths_json` + 消费端 `forcedPortPathsForTask`），**当场红**（名册为空）。修法两处：`services/fusion.ts` 的两处 `startTask` 加 `platformInputPaths: [MANIFEST_REL]`；**以及一处落档时没料到的**——`taskPlatformInputPaths.ts` 的 `ALLOWED_ROOTS` 是**封闭文法**，原本只放行 `PLATFORM_INPUTS_DIR` / `PLATFORM_PIPELINE_DIR`，光传路径会被判 `task-platform-input-paths-invalid`，故 `PLATFORM_FUSION_DIR` 一并入册（该模块的文件头注释恰好写明了这套机制正是为「agent 节点跑在隔离工作树里、`.agent-workflow/` 又被有意 ignore」而存在——**融合只是从来没被登记进去**）。修完融合第一次真的走通：`e2e/fusion-lifecycle.spec.ts` 补回 INTENT-53 / INTENT-54 / MEM-50 / MEM-X1 四条 P1，真实 agent 跑完发起 → 反问 → 审阅 → 批准 → 记忆转 fused 且**不再被注入**（同一个探针任务批准前后各跑一次，比 node run 的 `injectedMemories` 快照）。三条变异全部咬中：**抽掉那两行 `platformInputPaths`，单测与 e2e 双双转红**（这条最关键——它证明这两层防护真的锁着那个缺陷）、reconcile 丢掉 skipped 列表、`fuseMemoriesTx` 不改任何记忆。`docs/audit-backlog.md` 的条目已改判「已修」并写明多出来的那处文法改动。端点账本无新增命中，维持 **220**。能力 gap 602 → **598**，covered 77 → **81** |
| 2026-08-24 | **B28（融合发起）+ 一个真实产品缺陷**：新建 `e2e/fusion-lifecycle.spec.ts`（INTENT-48）与 e2e stub 的 `fusion` 模式。本批**只到「发起」为止，原因写在明处**：为了覆盖后面三环（审阅 / 批准 / 记忆转终态）给 stub 加了一个能真正跑完融合的模式，用它跑真链路时**挖出一个产品缺陷——记忆→技能融合在真实 agent 下必然失败**。根因链：merger 节点跑在逐节点隔离工作树（`nodeIsolation.ts:173`），产品契约要它把结果清单写进 `.agent-workflow/fusion/result.json`，而平台自己的排除档把整个 `.agent-workflow/` 写进了工作树 git ignore（`workspaceExcludeProfile.ts:28`），逐节点 merge-back 又是 git 驱动的，force-include 名册（`portArtifacts.ts:535`）只收 `platform_input_paths_json`，而 `fusion.ts:645` 的 `startTask` 没传——于是清单永远回不去，`reconcileFusion` 每次判 `agent did not write the fusion result manifest`。**同一次运行里的对照证据**最有说服力：stub 在同一个目录里既改了 `SKILL.md` 又写了清单，merge 回来的工作树里前者在、后者不在，差别只有「被 ignore」这一条（`git check-ignore` 指向 `.git/agent-workflow/excludes/v1:3:`）。既有的 `fusion-engine.test.ts:325` 照不出它——那条用例把清单**直接写进 task.worktreePath** 并把任务强制置 done，从不跨越隔离边界，**这正是本 RFC 立项要找的那类「缝隙上没有防护」**。详情与候选修法已落 `docs/audit-backlog.md`。两处实测契约同批记下：①`<aw-input>` 里的记忆标题带**零宽空格前缀**（框架给不可信输入里的 `#` 消毒），`/^### Memory /m` 一条都匹配不到——stub 第一版因此静默报告「吸收 0 条」；②轮次判定要读**提示词里的 STOP 指令**而不是按 cwd 记标记：重试会换 node-run id、也就换工作目录，标记版在第一次重试时又问了一遍、被框架以 `clarify-required-output-emitted` 判失败。变异两条咬中（对话框只送第一条记忆、全局 scope 注入过滤改取 fused）。INTENT-53 / INTENT-54 / MEM-50 / MEM-X1 仍留 gap，等缺陷处置定了再补。能力 gap 603 → **602**，covered 76 → **77** |
| 2026-08-24 | **B27（配置包导入）**：新建 `e2e/config-package-import.spec.ts`，一条 P1（RES-39）。这条链路此前**整条零 e2e**——`POST /api/resource-packages/preview`、`/commit` 与七个 `:id/export-package`，一条端点都没被任何 e2e 打到过；而它是本产品里**唯一一条会一次性写入多个资源**的用户动作。判据分三段，全部从服务端读回来核对而不看界面提示语：①预览是干跑；②两项都选「新建」⇒ 新建两份，且**新代理指向新技能**（依赖重新接线——指回旧技能的话，日后有人改旧技能新代理跟着变，而这层关系没有任何地方记录过）；③技能改选「复用既有」⇒ 只新建代理、它指向既有技能。②③用**同一个包、同一个界面**，只有那一格选择不同——这正是「逐项决策」本身的判据。**第一条中途被加固过一次**：原本只断言「资源集合逐字不变」（效果层），变异实测**没咬中**——把预览按钮改成 `runPreview().then(runCommit)` 之后 `runCommit` 因闭包里 `preview` 仍为 null 直接返回，于是「发过一次提交请求但恰好失败」会被记成通过。改成**动作层**判据（记录本次页面发往 `/api/resource-packages/*` 的全部请求，断言只有 preview 一条），同一条变异立刻转红。四条变异全部咬中：commit 里把 new 强改 reuse / 把 reuse 强改 new、`rewritePayloadRefs` 的键表去掉 `skills`、预览顺带发一次 commit 请求。用 R1 的运行期采集取正向证据销账，端点账本 **223 → 220**（本条真实打到 `preview` / `commit` / `agents/:id/export-package` 三条从未被触及的端点）。能力 gap 604 → **603**，covered 75 → **76** |
| 2026-08-24 | **B26（工作组自动保存的失败与冲突处置）**：新建 `e2e/workgroup-draft-recovery.spec.ts`，一条 P1（WG-08），五个用例。工作组编辑器**没有保存按钮**——用户唯一的凭据就是那两个状态徽标，所以这条能力的失效形态是「编辑器安静地把一次编辑丢了，而界面从头到尾显示已保存」。它复用工作流那套草稿引擎，工作流侧的弱网行为已被 `rfc199-save-reliability` 锁住，**但复用不等于接对了线**：`hooks/useWorkgroupAutosave.ts` 是一层独立适配器（把工作组快照编码进 `WorkflowDraftSnapshot.description`、自己逐项校验回执、自己把冲突负载映射回界面），这一层接错在工作流侧的用例里一条都照不出来，症状却同样是静默丢数据。五条对应能力条目里的三件事：①离线队列——发不出去的那次尝试必须**原样**重发（断言 `sent[1].clientMutationId === sent[0]`：换个 id 重发，服务端会把它当成第二次写），期间的新编辑排队不插队；②响应丢失重对账——服务端其实写成功了、只是回执丢在路上，恢复后必须按版本/哈希精确对账，断言落在 **`version === 2`** 上（重发一遍会推到 v3，而每一次多余的写都会把并发编辑者顶成冲突）；③冲突三选一，各自「先打开对话框再取消、断言两边都没动」再走确认路径。五条变异全部咬中：引擎侧重发换 id、settle-by-hash 分支短路；适配器侧两个二次确认改成点击即执行、副本改送服务端快照而非本地草稿。**中途自查改了一处**：第三条原本一个用例叫「三选一」却只走了覆盖的取消路径与副本，**标题名不副实**，当场拆成三条各自完整的用例。`--repeat-each=2` 连跑（与 B25 合并）16/16 稳定。能力 gap 605 → **604**，covered 74 → **75** |
| 2026-08-24 | **B25（草稿冲突后的三条恢复动作）**：新建 `e2e/draft-conflict-recovery.spec.ts`，一条 P1（WF-44）。冲突态本身已有覆盖（`rfc199-save-reliability` 的两标签页用例锁到「出现冲突且谁都没被覆盖」为止）——**没被覆盖的是它之后**：用户此刻握着一份没保存的工作，界面给三个按钮，每一个都会销毁一边的成果（载入远端丢我的、覆盖远端丢别人的、另存副本谁都不丢）。接错线的形态全是静默的：点了「载入远端」结果把本地推上去了，页面照样回到「已保存」，要等别人回来问「我的改动呢」才发现。因此判据**两边都读**：本地看到什么 + 服务端存的是什么。二次确认按判据对待而非装饰——它是这三个不可逆动作唯一的挽回机会，所以每条都**先打开对话框再取消**、断言两边都没动，再走确认路径；只测确认路径的话，「点按钮即执行、对话框只是通知」的实现同样能全绿（这条变异实测确认咬中）。本地改动刻意做成**两处**——改名（可从标题与服务端字段读出）+ 新增一个 review 节点（可从 `definition.nodes` 数出）：只改名的话「副本带走了本地工作」会退化成「副本被建出来了」，因为副本的名字本来就是我在对话框里填的。四条变异全部咬中（loadRemote / overwrite 各自短路、副本改送空 definition、两个二次确认改成点击即执行）。一处实测：宽视口下节点检查器是**常驻侧栏**而不是 `modalSurface==='inspector'` 那个 Dialog，`workflow-editor-inspector-surface` 在这个视口根本不存在，要收的是 `.inspector__close`。`--repeat-each=2` 连跑 6/6 稳定。能力 gap 606 → **605**，covered 73 → **74** |
| 2026-08-24 | **B24（OIDC 开户策略拒绝）**：新建 `e2e/oidc-provisioning-refusals.spec.ts`，一条 P1（IAM-08）。这条判据的不对称性很极端——**放行一个不该进的人，他就成了系统里一个正常的 active 账号**，之后所有 ACL 都建立在这个错误之上；而它**没有界面**可看：管理员改完 `provisioning` 之后，唯一能确认它生效的方式就是让一个不该进的人来敲门。因此用例走**真实授权码流**（system-mock 统一身份提供方、真 PKCE/state、真 id_token 签名与校验），让 IdP 把身份**验过之后**交回来，再看 callback 作不作数——只调 `decideProvisioning` 纯函数不够，它已有单测，而线上出事的形态是「纯函数判对了、callback 没把 reject 落成拒绝」。三条分支各锁三件事：状态码 403、对应的 friendly 文案、**以及账号列表逐字不变**（半个账号比放行更难查），外加「拒绝页上不许存在会话令牌」。第 4 条是正向对照，与第 3 条**共用同一份 provider 配置**（同样的 `@mock.test` 白名单、同样 `trustEmailVerified:false`），只换点哪个身份——没有它，前三条会退化成「这条链路本来就走不通」的假绿。四条变异逐一咬中（三条 reject 各自改成 create、allowlist 命中改成 reject）。两处实测契约：白名单条目是 **`@` 前缀的邮箱后缀**而非裸域名（`shared/schemas/oidcProvider.ts:19` 的 `EMAIL_DOMAIN_REGEX`，写 `corp.invalid` 会 422）；`POST /api/oidc/providers` 回 **201** 不是 200。本条打到的端点全部已被覆盖，端点账本如实维持 **223** 不动。能力 gap 607 → **606**，covered 72 → **73** |
| 2026-08-24 | **B23（保存结果未知）**：新建 `e2e/settings-outcome-unknown.spec.ts`，一条 P1（RES-08）。用 `page.route` 把保存请求**中途掐断**（不是回 5xx——错误码是「已知失败」，走另一条分支）精确复现「响应丢失」。**实测到的设计比审计条目描述的更强，判据按实际行为写而不是照抄描述**：审计写的是「页面锁死 +『重新判定』恢复」，实际上**连接级**的响应丢失走的是更保守的**写屏障**（`lib/config-receipts.ts:166` 的 `getWriteBlock`）——该连接的写入整体 fail-closed，并**刻意不给**自动重判入口，因为那次写可能仍在服务端跑，一个迟到的结果会覆盖掉别人后来的保存；恢复代价明确写在界面上（重启 daemon 后重载）。「重新判定」属于另一类**可判定**的歧义，不是这一条。用例因此锁四件事：进入结果未知、保存被禁用、出现写屏障文案且**不出现**重判按钮、屏障之后改值仍不许写且确实没再发出写请求。变异（`getWriteBlock` 恒空）咬中。另一处实测：这句提示在页面上不止一处（分区导航徽标 + 告警条），定位必须锚到 `.notice-banner` 本身。能力 gap 608 → **607**，covered 71 → **72** |
| 2026-08-24 | **相机缺陷修复 + 棘轮第一次完整闭环**：`e2e-full-nightly` 四个分片**首次全绿**，于是 R1 的覆盖账本对账 job 第一次真正跑完对比，并精确点出一条：`PATCH /api/auth/me/profile` —— RFC-320 落库的新端点，挂了却没有任何 e2e 打它。这正是那套 fail-closed 机制存在的目的：此前它一直在「上游没全绿就拒绝对账」，这次终于给出了实质结论。**补覆盖优于记债**，遂新建 `e2e/account-profile.spec.ts`：这条端点不是可有可无的资料页——账号邮箱是**任务能否启动的前置**（`getUserGitCommitIdentity.ts:31-36` 缺邮箱直接 `git-identity-email-missing` 拒绝建任务），且 displayName/email 会成为任务里每一次 git commit 的作者身份。判据四段：改完回读确实落库、邮箱被归一成小写（否则同一个人在 git 历史里留下两种大小写的作者身份）、一个人改自己不会改到别人身上、schema 是 strict 所以多塞 `role` 必须 422 而非静默丢弃（静默丢弃时调用方以为自己提权了）。变异（取消小写归一 + strict 放宽为 strip）咬中。端点账本因此维持 **223** 而不是涨到 224 |
| 2026-08-24 | **B22（设置保存回执）**：新建 `e2e/settings-save-receipt.spec.ts`，一条 P1（CFG-04）。设置页保存的是守护进程的运行参数（端口 / 保留期 / 限额），它的失败形态很特别——点了保存、界面毫无变化，用户**以为存上了**，带着一个从未生效的配置继续走，直到下一次真正需要那个值时才发现。判据分三段，缺一不可：①有**明确回执**（「没有报错」和「什么都没发生」长得一模一样）；②回执对应的值**真的落库**（只渲染一句 Saved 而后端没收到，比没有回执更坏）；③刷新后仍是新值（回执不该只对应一次内存态的乐观更新）。前置对照也不能省：未改动时既无回执、保存按钮也是灰的——没有它，「保存后出现回执」可能只是「回执一直都在」。变异（`routes/settings.tsx:3320` 的回执渲染短路）咬中。两处实测：`settings-webhook-body-retention` 在 **gc** 分区而非 limits；对应的 config 键是 `webhookDeliveryBodyRetentionDays`。能力 gap 609 → **608**，covered 70 → **71** |
| 2026-08-24 | **同类脆弱性自查（RES-28 之后）**：把本轮 12 个 spec 逐条扫一遍找同形隐患，改掉一处——`fusion-access` 结尾原本用白名单 `['running','awaiting_approval']` 断言「融合没被陌生人推进」，而融合引擎任务可能因它**自己的**原因失败（环境 / runtime），那与「陌生人有没有得逞」无关，白名单会让这条断言在一个与它无关的原因下变红。改成只排除**他那三个动作会产生的两个终态**（`done` / `canceled`）。随后把 12 个 spec 以 `--repeat-each=3` 连跑：**93/93 全绿、零 flaky**。定式记下来：断言要挂在「被测行为会产生的状态」上，不要挂在「我以为此刻应该处于的状态」上——后者会把环境噪声算进判据 |
| 2026-08-24 | **RES-28 时序缺陷修复（CI 归因产物）**：`26aa65967` 上 CI 报 `endReason` 期望 `access-revoked`、实得 `session-unusable`，本机稳定绿。缺陷在**用例**这边，不在产品：runtime-test 会话在模型回合自然结束后会转 `ending / session-unusable`（`services/mcpRuntimeTest.ts:1228`），而 `transitionMcpAclRuntimeTestsInTx` 只处理 `status='active'` 的会话——本机 ACL 变更恰好先到所以绿，CI 上回合先结束，撤权时已无活跃会话可终止，用例实际上变成了**空洞绿**再被 endReason 断言救回来打红。修法不是放宽断言（那会让它在 CI 上真的变空洞），而是把前提做成确定性的：改用 `slow` stub + `STUB_OPENCODE_SLEEP_MS=60000`，让模型回合在整条用例期间**始终在飞行中**，会话因此确定性地停在 `active`。`--repeat-each=5` 连跑稳定，变异（`endNow` 短路）重新咬中。教训与 REPO-35 那条同源但方向不同：那次是「测了库里还没有的东西」，这次是「测了一个只在本机时序下成立的前提」。两者都只有 hosted CI 能照出来 |
| 2026-08-24 | **R1/R2 账本对账（补 B4 遗留）**：用 R1 的运行期采集（`AW_E2E_ROUTE_JOURNAL`，零生产改动）跑一次完整 PR 档全量，收到 **111 份 journal / 1490 条去重请求**，据此对账两份账本：端点账本 **264 → 223**——本轮 B4–B21 新增的用例真实打到了 **41 条此前从未被任何 e2e 触及的端点**；前端路由账本维持 **17**（本轮用例打的都是已覆盖页面，如实不变，不粉饰）。**口径仍是「只按正向证据删除、绝不新增」**：这次全量跑并非全绿（两条并发 session 的 `rfc310-*` spec 红），新增会把「没跑到」误记成「没覆盖」。对账脚本是一次性的临时 test 文件，跑完即删——不往仓里留只用一次的工具。跑完 `packages/backend/tests/architecture/` 全套：296 pass / 4 fail，四条红逐条指向并发 session 的在制路径（`routes/accountRepositoryTransportCredentials.ts`、`modules/source-control/composition`、`modules/development-automation/*`），与本 RFC 无关 |
| 2026-08-24 | **B21（实时列表自更新）**：新建 `e2e/live-list-updates.spec.ts`，一条 P1（UX-30）。这条能力的价值全在「不导航」三个字上：用户停在列表页等结果，别处（另一个人 / 后台任务 / 一次自动化）改了数据。链路断掉的症状是**界面静止**——不报错、不空白、不转圈，只是永远停在旧数据上，用户以为「还没发生」。这是所有失效形态里最难被发现的一种。用例刻意**不碰浏览器上的任何东西**：不刷新、不切页签、不重新导航，唯一动作是一次浏览器之外的 HTTP 写，然后只等界面自己变。**两个方向都打**——只测「新增会出现」的话，一个只做 append 的实现同样能过，所以补了「别处删掉后它必须消失」（否则用户会对着一条已不存在的记录做决定）。变异（`routes/memory.tsx:127` 的 `useMemoryWs()` 注释掉）咬中。能力 gap 610 → **609**，covered 69 → **70** |
| 2026-08-24 | **B20（蒸馏分区权限门）**：新建 `e2e/memory-distill-gating.spec.ts`，两条 P1（MEM-24 / MEM-31）。蒸馏任务面暴露的是**未经人审的模型产出**与它们的失败诊断（stderr 摘录 / 源事件 / 模型会话），由 `memory-distill-jobs:manage` 单点把守。三个面一起锁，少一个就留一条真实口子：①导航里不出现那一格（否则点进去只撞 403 墙）；②`?tab=distill-jobs` 深链回落到默认分区**并说明原因**（深链会留在书签 / 聊天记录 / 旧收藏里，静默显示空白最坏——用户会以为功能坏了去提一个查不出问题的工单）；③整个过程**一个蒸馏请求都不发**（否则服务端日志被必然失败的 403 刷屏，真问题被淹没）。manager 那条正向对照不能省：没有它，「看不见」可能只是「这个分区根本没渲染」。变异（`routes/memory.tsx:83` 的权限查询恒真）咬中。同批**按机制清理了上一笔的一次性 `allowGrowth`**：账本从 612 回落到 610，署名随即过期（RFC-317 T17 的设计就是让它挂不长）。能力 gap 612 → **610**，covered 67 → **69** |
| 2026-08-24 | **REPO-35 覆盖撤回（CI 归因产物）**：`529937e32`（含本轮 B18 的 superseding commit）上 CI 报 `route-not-found: /api/account/code-host-push-credentials/gitlab`，而本机全绿。根因不是判据、也不是平台差异——那条路由所在的 `routes/accountRepositoryTransportCredentials.ts` 是并发 session RFC-321 **尚未上库的未追踪文件**；我的 e2e 二进制从共享工作树构建（含它），CI 从 `main` 构建（不含它）。这是 `docs/dev-gotchas.md:189`「`git ls-files` 型守卫对未追踪文件是盲的」的**镜像形态**：那条讲的是自己的新文件没进 index 导致本地绿 CI 红；这次是**给别人尚未提交的生产代码写了测试**，同样是本地绿 CI 红。按多人协作纪律不代提他人文件，故把 REPO-35 退回 `gap`（`gapSince: RFC-319`），等 RFC-321 落库后由它带上覆盖。账本因此**上涨一条**（611 → 612），按 RFC-317 T17 的定式在 `ledger-baselines.json` 写了一次性署名 `allowGrowth` 并写明原委；下一笔不涨的提交会把它判为过期并强制清理。同批留下的教训：**给一条端点写 e2e 前先 `git ls-files <实现文件>` 确认它在库里**——共享工作树上「能跑通」不等于「已存在」。covered 68 → **67** |
| 2026-08-24 | **B19（会话中途失效）**：新建 `e2e/session-invalidation.spec.ts`，一条 P1（UX-08），两个用例分别锁**必须踢**与**必须不踢**：①凭据本身死了（账号被停用）⇒ 清 token + 回登录（留着它就是 30 秒一次的静默重连循环：页面看着还在、数据永不更新，用户完全不知道自己掉线了）；②凭据仍有效、只是某条通道的门禁不再放行 ⇒ **不许**把人踢去登录（他还能用系统其余部分）。两条必须同时成立才算这个能力做对——只写「会掉线」的话，把 4403 也当 4401 处理同样能通过，而那正是 RFC-312 修掉的那个 bug。**第二条第一版是教科书式的空洞绿，当场作废重写**：原本用 presence 通道构造，可 `ws/registry.ts` 里只有 **task 频道**声明 `rerunUpgradeGate: true`，presence 根本不重跑门禁——用例什么也没观察到，变异下仍绿。改用「把人从任务成员里摘掉」这条真实触发路径（服务端对已连着的 `/ws/tasks/:id` 重跑门禁并以 4403 关闭），变异（把 4403 分支换成 `clearToken()`）即刻咬中。**另附一条操作教训**：还原被变异的源码之后**必须重新 `build:binary:e2e`**——我漏了这一步，于是拿着仍含变异的二进制去跑重写后的用例，红了整整一轮才用 `localStorage.removeItem` 打桩抓到调用栈才认出来。判据没错，跑的东西不对。能力 gap 612 → **611**，covered 67 → **68** |
| 2026-08-24 | **B18（定时任务到点自触发）**：新建 `e2e/scheduled-task-firing.spec.ts`，一条 P1（EVENT-42），覆盖整条链：轮询选中 → CAS 认领并把 `next_run_at` 推到下一槽 → 启动任务 → 回写展示字段。判据挂在 `lastTaskId` 上（那是「真的启动了一个任务」的唯一凭据；只看 `lastStatus` 会把「认领了但启动失败」也算成功），并单独断言**槽位确实前移**——不前移的话同一行会被每个 tick 反复认领，变成每 30 秒开一次任务的无限循环，而 CAS 认领存在的理由正是这个。变异（认领时不推进槽位）咬中。**这条是本 RFC 第一条打 `@nightly` 的用例，理由记在明处**：它按风险是 P1，但实测墙钟 1.5 分钟，且慢在**判据本身**——「到点自己跑」的观察窗口由真实 tick 周期（`SCHEDULE_TICK_MS = 30_000`）与 interval 规格下限（1 分钟）决定，没有任何测试技巧能缩短，除非给 tick 加旋钮，而本 RFC 是零生产改动。放进 PR 腿等于给每次提交加 90 秒，却换不来更早的反馈（这条链一天内不会被改多次）。分档设计存在的理由正是这种「高风险但慢」的用例；`rfc319-ci-topology` 守卫已复核 PR 腿排除 / 夜跑选中两侧都对。能力 gap 613 → **612**，covered 66 → **67** |
| 2026-08-24 | **B17（MCP 撤权与会话终止）**：新建 `e2e/mcp-acl-session-termination.spec.ts`，一条 P1（RES-28）。runtime-test 会话是一个**活着的模型进程**，握着这个 MCP 的完整配置（命令行 / 环境变量 / 远端地址）；把 MCP 从某人眼前收回去却不掐掉他已开的会话，那么「撤销访问」撤的只是**列表里的一行**——撤权的人不会看到异常，被撤权的人也不会。用例：公开 MCP → 普通用户开会话（断言它确实 active，否则「被终止」证明不了任何东西）→ 收回私有 → 从**管理档**读回会话（被撤权者此时连 MCP 都 404，拿他的令牌读只会验到另一件事）→ 断言 `status !== 'active'` **且** `endReason === 'access-revoked'`（只断终止不断原因，事后审计说不清它为什么断）。两处实测契约：建会话的回执只有 `{sessionId, acceptedTurnId}`，状态要另读一次；远端地址指向必然连不上的本地端口不影响会话行的建立——这条用例关心的是生命周期，不是连通性。变异（`transitionMcpAclRuntimeTestsInTx` 的 `endNow` 短路）咬中。能力 gap 614 → **613**，covered 65 → **66** |
| 2026-08-24 | **B16（Webhook 触发规则）**：新建 `e2e/webhook-trigger-matching.spec.ts`，两条 P1（EVENT-18 / EVENT-19）。EVENT-18 用一条规则打满五维：仓库范围（exact 名单外）/ 事件类型（同仓同分支、只把 `status` 换成 success ⇒ `pipeline_succeeded`）/ 分支 glob / 评论命令前缀（note 事件，目标分支设为 main 让它只可能栽在前缀这一维）/ 忽略作者（push 事件）。**第一版把「忽略作者」挂在 pipeline 上，红了，而那条红是对的**：`matchTrigger`（`services/webhook/matching.ts:75`）只对 `AUTHOR_FILTERED_EVENT_TYPES`（push / tag_push / mr_* / note）做作者过滤，**pipeline 类刻意不过滤**——bot 推分支引发的流水线失败必须还能触发「修到绿」，作者身份改为参与熔断重置。这条不对称因此被单独锁成一条正向断言：ignore 名单里的作者发 pipeline 事件**仍然**开工。忘掉它的人会顺手把名单加进 pipeline 分支，而症状是「自动修复再也不启动」。事件类型那一维刻意选 `pipeline_succeeded` 而不是 `tag_push`：后者会先栽在分支过滤上，那样这一条验的就不是事件类型了。EVENT-19 让作者恒在忽略名单里（`evaluateCircuit` 因此不会按「人已介入」清零），连投四次同流事件 ⇒ 出现 `skipped-circuit-open`，同时断言上限之内的那几次确实放行了（否则「熔断生效」可能只是「规则根本没命中」）；再按 streamKey 人工重置，下一条同流事件重新开工——没有这一步，熔断就是个单向门。两处实测契约：`launchPayload.scratch` 与 `autoRegisterRepos` 互斥（`scratch-auto-register-conflict`，事件仓库没注册进平台就没得注册）；无 MR 的事件流键是 `${repoPath}|branch:${branch}`。两个变异（分支过滤失效 / 熔断永不 open）各自咬中对应用例。能力 gap 616 → **614**，covered 63 → **65** |
| 2026-08-24 | **B15（任务启动门与终态删除）**：新建 `e2e/task-lifecycle-gates.spec.ts`，两条 P1（TASK-46 / TASK-28）。TASK-46 三段：不可见工作流与不存在**同形 404**（这条漏掉的症状是**没有症状**——泄漏形式是「任务启动成功」）、公开之后同一个人就能启动（正向对照，排除「他压根启动不了任何东西」）、guest 缺 `tasks:execute` 时是 **403** 而非 404（此时工作流已公开、藏无可藏，能力缺失才是真原因）。TASK-28 四段：非终态拒绝、缺回显 422、回显错 422、回显对才真删且再读 404。**非终态那段用了一个确定性停住的任务**：input → agent → review → output，停在 `awaiting_review` 等人拍板，不依赖任何时序窗口。构造它撞了一条契约——评审节点的 `inputSource` 必须来自 agent 节点的 markdown 产物（直接接 input 被静态校验拒为 `review-input-source-not-markdown`），所以中间那个 agent 是必需的。**变异实证再次照出双层实现**：终态门有两处——`taskDelete.ts:93` 的廉价前置检查与 :174 事务锁内的权威复查；只短路前置那道用例仍绿，两道一起短路才转红。已写进用例注释。能力 gap 618 → **616**，covered 61 → **63** |
| 2026-08-24 | **B14（融合的可见性与决定权）**：新建 `e2e/fusion-access.spec.ts`，两条 P1（INTENT-58 / INTENT-X3）。一次融合会**改写托管技能的正文并递增版本**，而技能正文是往后每次任务都要读的东西——所以「谁看得见」「谁能拍板」是真实边界。INTENT-58 三个读面各锁一次：列表按 owner 过滤、详情与不存在同形（沿用 B13 的 `normalizeRefusal`）、**待审徽标计数**不含他人（这个面最易漏——它只是个数字，错了没有任何症状，只是导航栏上多一个点）。INTENT-X3 三条写路径全拒，且逐条断言拒绝码确实是 `fusion-forbidden` 而不是别的原因，最后回读确认融合没被推进。用例只需要融合**行存在**，不必跑完，隔离面在 `running` 阶段就已成立。**陌生人的角色选择卡了三轮，值得写下来**：`manager` 自带 `resource-acl:bypass`，拿他做陌生人隔离断言恒真；改用显式 `additionalPermissions` 又连撞两次 `user-permission-redundant`——最后核对源码发现 `USER_BASELINE` 是由 `USER_RESOURCE_READS/WRITES` + `USER_EXECUTE` **spread** 组成的，纯 `user` 早就有 `skills:read/update`、`tasks:execute`、`memory:update`，且没有 bypass，本身就是这里要的形状。同一次核对**推翻了 B9 留下的一条注记**：那里写「`user` 角色没有 `agents:create`」——错的，它在 `USER_RESOURCE_WRITES` 里。IAM-32 用例的注释与本表 B9 条目已一并更正为：那半边没断言是因为本用例的原所有者是管理档、自带 bypass 而 bypass 按设计绕开归属，不是因为构造不出普通用户所有者。能力 gap 620 → **618**，covered 59 → **61** |
| 2026-08-24 | **B13（人机门的访问边界）**：新建 `e2e/human-gate-access.spec.ts`，两条 P1（HUMAN-09 / HUMAN-32）。编排沿用 `clarify.spec.ts` 的 stub designer（第一轮抛问题 → 任务停在 awaiting_human），因此门是**真的**——`beforeAll` 里显式断言「任务确实停在反问门上」，否则后面所有隔离断言都会因为「本来就没有东西」而平凡通过。两条各锁三层：读面详情与不存在同形、待办列表不含别人任务的门、写面（作答 / 拍板）拒绝；HUMAN-09 另回读确认门既没被推进也没被污染。**存在性判据被实测收紧了一次**：第一版直接逐字节比较两次拒绝正文，红了——正文里回显了**调用方自己送进去的 id**（`no clarify_round for intermediary node_run <id>`）。那不是泄露（他本来就知道自己问的是谁），所以加了 `normalizeRefusal`：先把各自的 id 归一成同一个占位符再比。这样判据既不误报，也仍然咬得住任何**结构性**差异——两个变异（反问 / 评审各自的 `canViewTask` 短路）分别把对应用例打红，而它们改变的正是 code 与 message 的形状。能力 gap 622 → **620**，covered 57 → **59** |
| 2026-08-24 | **B12（代码平台推送凭据）**：`e2e/repo-governance.spec.ts` 追加 REPO-35。存进去的个人推送令牌**再也读不出来**——写入回执、列表回执两处都逐字节断言不含明文，回读面只有 `tokenHint`（长度恰为 4 且确为原令牌尾 4 位）。**变异实证又一次改写了这条用例的宣称**：把三条路由的 `tokenAccess: 'never'` 全改成 `'allow'`，用例仍然绿——因为 `account:self` 不是可授予的 PAT scope、令牌也不继承它，**权限门总是先触发**，通道门在 HTTP 面根本不可达（与 B4 的 `last-access-administrator-protection` 同一形状）。于是把可证的那道提为显式断言（申请 `account:self` scope 必须 422 `pat-scope-ungrantable`），把三条路由的令牌调用统一断言 **403**，并在注释与标题里写明「通道门由路由注册表单测守着，这条用例不覆盖它」。第一版还犯了个自己刚警告过的错：为绕开 ungrantable 而把 PAT 换成 `agents:update` scope，那样「令牌被拒」就退化成「它缺权限」——现在两层分别点名。另两处实测契约：个人凭据必须挂在**已配置的代码平台连接**上，`connectionGeneration` / `endpointBindingDigest` 要与该连接当前值逐字相等（否则 `code-host-push-credential-stale`）；GitLab 的 baseUrl 必须以 `/api/v4` 结尾。能力 gap 623 → **622**，covered 56 → **57** |
| 2026-08-24 | **B11（运维：本地找回与备份恢复）**：新建 `e2e/ops-local-recovery.spec.ts`，三条 P1（OPS-022 / OPS-020 / CFG-31）。OPS-022 走**真实事故形态**：建一个指向不可解析主机的 enabled 身份源（即「身份源存在但不可用」），据此关掉密码登录并确认登录端点真的 403，停 daemon，用**编译后的二进制**对着**同一个 home** 跑 `auth password-login status|enable`，再重启验证能登进去。用进程内函数调一下只能证明函数存在，证明不了发行的二进制里有这个子命令。顺带锁两条：输出里的 `daemon token remains retired`（本地通道只开门、不发新的最高权限凭据，否则它就成了权限提升通道），以及子命令拼错时给出用法提示并非 0 退出。OPS-020/CFG-31 走完整往返：备份 → 再造一条数据 → 装填 → **取消**（CFG-31 点名的可反悔窗口）→ 重新装填 → 重启 → 两个方向都断言（备份时点那条还在、备份之后那条消失、待生效标记已清）。只测「备份文件生成出来了」等于什么都没测——内容错误或装不回去的 tarball 同样能让那条断言通过。**一处坑值得记**：第一版让 harness 自己建 home，结果 `first.stop()` 因 `keepHome=false` 把整个 home（含数据库）删了，CLI 随即在原地新建空库、`status` 报 `bootstrap: required`——用例会「通过」得毫无意义。改为**由用例自己持有 home** 并两次 `startDaemon({ home })`。另一处是老坑复发：断言消息里 `await res.text()` 之后再 `res.json()` 抛 `Body is unusable`，统一改 `clone()`。两个变异（CLI enable 只读不写 / 启动时不应用待生效恢复）各自咬中对应用例。能力 gap 626 → **623**，covered 53 → **56** |
| 2026-08-24 | **B10（事件响应规则）**：新建 `e2e/event-response-rules.spec.ts`，两条 P1（EVENT-30/31）。EVENT-30 覆盖增删改回环 + 三类拒绝：`subjectMatch` 与 `subjectPattern` 的互斥约束（all 不许带、非 all 必须带）、重复删除必须 404 而非静默成功、以及**模板引用未声明参数在创建时就被拒**（`event-response-template-ref-invalid`）——最后这条是本域最安静的失效形态：放过去的话要等真事件来了才发现启动的是一份参数为空的工作，而那时没有人在看。EVENT-31 锁三档：普通用户读得到但 create/update/delete 三个动作全 403；另一个 manager 有写权限但不是 owner，拒绝形状是 **404 而非 403**（规则 id 的存在性不从错误码泄露），且回读确认规则没被动过；持 `override-owner` 的管理档能跨 owner 改——这条正向对照不能省，否则前两条可能只是「这条规则谁都改不了」。两个变异（模板校验短路 / owner 检查短路）各自咬中对应用例。实测契约：事件类型引用形如 `{id:'code-host.branch.pushed', revision:1}`，从 `/api/event-center/catalog` 的 `eventTypes[].eventTypeRef` 取。能力 gap 628 → **626**，covered 51 → **53** |
| 2026-08-24 | **B9（凭据与归属）**：新建 `e2e/identity-credentials.spec.ts`，四条 P1（IAM-12/13/27/32）。共同特征是「改完之后界面上看不出区别，只有下一个人拿旧凭据敲门时才知道有没有生效」。IAM-13/27 都把判据落在**会话是否真的死了**上（改密 / 重置的唯一目的就是把入侵者踢出去），并各自带正向对照（新密码能登、新签发的票能用）。IAM-27 的「下次必须改密」判据改挂登录回执的 `mustChangePassword`（`auth.ts:121`，前端正是据它跳转），而不是内部列名；顺带锁了 `__system__` 不接受重置（否则平台内部身份会变成可登录账号）。**IAM-12 被真实行为改写了两次，最终判据比原设想强得多**：①原打算直接关掉密码登录，实测被 409 `password-login-requires-enabled-oidc` 拦下——这正是审计条目里「最后一个 provider 保护」那半，遂把它提为第一段断言（没有退路时关掉 = 一次点击锁死整个实例），再建一个 enabled 的 fixture IdP 走完真正的关闭路径，两段互为正反对照；②`oidcDefaultRole` 只收 `guest|user`，原本的回环断言改成**上限断言**：`manager` / `admin` 必须被拒（这个字段决定陌生人从身份源首次登入时的身份，能填管理档就等于把开户即管理员写进配置）。**变异实证暴露了一处判据错位并已写进用例注释**：关掉密码登录后真正拦人的是 `auth/loginPolicy.ts:306`，不是 `routes/auth.ts:85`——摘掉路由那道用例仍绿（它是冗余的第二层），摘掉 loginPolicy 那道才转红。判据落在行为上，所以两层里任何一层还在这条能力就成立。IAM-32 的「原所有者从此不能管」本批没有断言：本用例的原所有者是管理档、自带 `resource-acl:bypass`，而 bypass 按设计绕开归属，拿它做否定断言只会锁死一个错误期望；改用继承人的**前后对照**——转让前连资源都看不见（404），转让后能改它的 ACL——并把标题与注释一并改准。能力 gap 632 → **628**，covered 47 → **51** |
| 2026-08-24 | **B8（仓库 / 仓库组治理）**：新建 `e2e/repo-governance.spec.ts`，三个用例覆盖 REPO-07/13/23/24（四条 P1）。REPO-07 与 REPO-23/24 是同一形状的两条删除挡板——「被引用 → 409 且点名引用者 → force 放行」，两条都补了**正向对照**（强制删除确实删得掉、被拒之后东西确实还在），否则「拒绝」可能只是「这个东西根本删不掉」。REPO-23 额外断言强制删除的回执如实报出连带影响：`detachedReferences ≥ 1`、`archivedMemories == 1`（组被删而挂在它上面的记忆没归档 = 孤儿 scope），并回读父组确认它不再挂着已删子组的 id。**REPO-13 的变异实证纠正了它自己的判据**：第一版注释说它锁的是失败消息脱敏（`clipAndRedact`），实测把那处脱敏摘掉用例仍绿——本机 git 对无法解析的 host 报错时并不回显 URL 里的密码段。真正咬住它的是 `rowToWire` 的两次 `redactGitUrl`（`repoBatchImport.ts:505-506`），改成原样回显立刻转红。已把注释与用例标题一并改准，并写明「失败消息那条仍只有单测守着」，免得下一个人误读这条用例的覆盖面。两处实测契约：仓库组的子组挂载键是 `childGroupId` 而非 `groupId`；任何组都必须显式含一个 `path: ''` 的根节点（缺了报 `repo-group-root-missing`）。能力 gap 636 → **632**，covered 43 → **47** |
| 2026-08-24 | **B7（记忆域）**：新建 `e2e/memory-access.spec.ts`，覆盖 MEM-34/35/36/37（四条 P1：候选未审不可见、逐行管理权、资源 scope 随可见性、repo/global 全员可读仅 bypass 可管）+ MEM-04/08/10/19/48。九条能力、八个用例，全部本机实跑 + 变异实证（四个变异各自只咬中对应用例，其余保持绿——变异归因干净）。能力 gap 645 → **636**，covered 34 → **43**。四处契约是实跑撞出来才写对的，都不在任何文档里：①`POST /api/memories` 的回执是 `{memory:{id,…}}` 而非扁平 `{id}`；②**手工建的记忆初始就是 `candidate`**，而列表对无 `resource-acl:bypass` 者整个滤掉 candidate（`routes/memories.ts:128`）——所以要让普通用户看见必须先 promote，而这条弯路本身把原本排在 P1 却没打算做的 MEM-34 变成了「不用跑蒸馏就能构造」；③`POST /:id/promote` 要带判别体 `{action:'approve'|'approve_and_supersede'|'reject'}`；④`memory:read` 与 `agents:read` 同类，是角色基线权限、不能作为 PAT scope 显式授予（`pat-scope-ungrantable`）。另有两条判据是**先写错、被真实行为纠正**的，比原判据更强：①MEM-10 原打算断言「驳回后普通用户看不到」——实测 rejected 行仍在列表读面上（`dropCandidates` 只挡 candidate）。查过注入侧 `services/memoryInject.ts:143` 只取 `status='approved'`，确认那是审计读面而非注入泄露，遂把判据改成「驳回是终态、不能再被 approve 洗回」；②MEM-19 的 token 回显门原挂在 global 记忆上，实测 PAT 会先撞管理权 403（`resource-acl:bypass` 不是可授予的 PAT scope），根本走不到确认门——那样用例什么也证明不了，改挂到同 owner 的 agent scope 上，并补了「回显正确时确实删得掉」这条正向对照，免得三条拒绝退化成「token 压根删不掉任何东西」 |
| 2026-08-24 | **B4 第二批**：再补 IAM-30（用户访问变更的硬不变量）与 IAM-21/47（令牌只能管自己、且永远够不到归属/授权写面）。能力 gap 660 → **657**，covered 19 → **22**。两条实测结论值得记：①`last-access-administrator-protection` **从 HTTP 面不可达**——任何能 PATCH 用户的 actor 自己就是访问管理员（计数 ≥ 1），而唯一被排除在计数外的 `__system__` 那条路要走 bootstrap 档，那一档在交接完成前对所有业务端点回 `bootstrap-admin-required`（实测 403）。它是 `userAccessPolicy.ts:118-125` 的纵深防御，由那层单测守着；用例里写清了这段，免得下一个人再撞一遍。构造过程中反而照出一条此前同样没有端到端断言的真护栏 `self-disable-forbidden`，一并锁上。②IAM-47 用「**同一个人、同一个资源**，会话能改 ACL、令牌永远不能」作对照，把拒绝精确归因到通道本身而不是权限/可见性/所有权——第一版把资源建在 daemon 名下，PAT 拥有者根本看不见它（404 与不存在同形），「令牌写不了」会因为看不见而成立、证明不了任何针对性。另有三处契约是实跑撞出来的：PAT 的 `purpose` 只接受 `general`/`mcp_only`；`agents:read` 是角色基线权限、不能作为 PAT scope 显式授予；账号面 `/api/auth/me` 对 PAT 也是 `tokenAccess: never`（存活探针因此改用「在自己资源上做一次普通写」）。**端点/路由账本本批未同步**：并发 session 的 `services/gitCredential.ts` 重构中途，`createApp` 起不来（`leasePushCredential` 导出缺失），`allRouteMeta()` 取不到分母。能力账本不依赖它，已同步。端点/路由账本待树能构建时补——期间 nightly 若跑，其「上游没全绿就拒绝对账」的判据会吸收这段不一致 |
| 2026-08-24 | **B4 第一批（身份与访问）**：新建 `e2e/identity-access.spec.ts`，补 IAM-01/03（首次安装的管理员交接：`/setup/admin` 此前是**零自动化**的首屏）、IAM-05/06（密码登录回到原目标页 + 三类拒绝）、IAM-26（停用用户后**已发出的会话**立刻失效）、IAM-43（退出登录：服务端吊销 + 客户端凭据清空 + 回 /auth）。IAM-38 已被上一批的 TASK-32 覆盖，一并销账。四条全部本机实跑 + 变异实证。两处判据是实跑撞出来后改对的：①交接完成后**不能**再查 `/api/auth/bootstrap/status`——它要求 daemon 源身份（`auth.ts:137`），而交接恰恰退役了那张一次性票，所以改成直接断言「那张票不能用了」，判据更强；②登录的三类拒绝里，「不存在的用户」与「错密码」必须**同形**，否则登录页可被用来枚举账号。账本：能力 gap 668 → **660**（8 条晋升为可校验 covered），端点 266 → **264**，路由 18 → **17**（`/setup/admin` 首次被真实加载）。**采集踩了一个坑并已沉淀进 `docs/dev-gotchas.md`**：journal 原本写在 `test-results/` 下，而那个目录归 Playwright 所有、每次 run 开始清空——共享工作树上别人跑一次 e2e 就把它端走了，重播脚本读到 0 份、把账本写成「全都没覆盖」。已把 journal 路径移出 `test-results/`（含 nightly workflow），并确立第三条纪律：**账本重播只能用全绿的那次跑**；这轮全量因并发 session 的在制改动红了 2 条 RFC-310 spec，于是改用「只按正向证据删除、绝不新增」的口径入账 |
| 2026-08-24 | **R3 能力账本落地（T26/T27）+ AC-8**：`architecture/e2e-capability-ledger.json` 播种 820 行。设计上比落档时多了一档状态，理由是**不能把散文原样搬进 JSON**：`gap`（668，只减不增）/ `covered-unverified`（141，审计判定有防护但证据仍是散文 `path:line`，只减不增，**存量专用**——新增行只能是 covered 或 gap，否则它就成了「宣称覆盖但不必证明」的永久后门）/ `covered`（11，证据是 `{file, test}` 且守卫每次验证标题逐字存在）。这样「把散文变成判据」这件事本身也有了一个会下降的数字。两个债务数字通过显式 `gapIds` / `unverifiedIds` 数组接入 RFC-317 的高水位机制（它按数组长度清点，派生计数它数不到），并由守卫钉死两个数组与 rows 派生集合逐条相等，避免冗余漂移。守卫六条负 fixture + 三条变异实证（改 test 标题 → 红；偷偷把 gap 改成 covered-unverified → 红；gapIds 与 rows 脱钩 → 红）。AC-8 同批完成：`e2e/CAPABILITY_COVERAGE.md` 头部加上「这是导读、不是判据」并点名本次抓到的三种失效形态，权威判据指向三份机器账本 |
| 2026-08-24 | **B3 第二批**：T33 / T34 / T36 / T37 完成（T35 泳道拖拽留待下一批——它要走数字员工编排的发布链路，深度另算）。账本再收敛：端点 268 → **266**（新覆盖 `GET /api/scheduled-tasks/:id`、`PUT /api/tasks/:id/members`）。**T33 的根因值得单记**：`rfc232-owner-list.spec.ts` 之所以「全程 admin」，是因为 peer 的会话令牌在 seed 里用完就丢了，浏览器侧根本没有非 admin 凭据可用——把它挂进 `OwnerFixtures` 之后那条边界才写得出来。**T34 撞出一条通用坑（已可复现）**：这条用例单跑绿、全量跑红。两个叠加原因——①`TaskMembersPanel` 的 UserPicker `onChange` 里有 `if (!sessionIsCurrent()) return`，会话未落定时**静默丢弃**用户的选择，症状是「选了但保存按钮一直灰着」；②portal 出来的结果列表**会盖住 Save 按钮**，而列表长度取决于库里累积了多少用户，所以隔离跑时列表短、不覆盖。判据加了两条：用 `members-transfer-owner`（仅 canManage 时渲染）当就绪信号，并断言选择真的落进了 chip；Save 前用 `Escape` 关列表（UserPicker 对 Dialog 内首个 Escape 做过 stopPropagation，正是为此）。**T37 的第一版把邻居弄红了**：插在相机场景中段时，后面 `wrapper.click` 的 `toHaveClass(/selected/)` 变红——那条红与本能力无关，纯粹是我扰动了它的前置相机/选中状态。改放函数末尾；390 窄屏那条腿点不开相机面板，按 `profile` 收窄到 desktop 并写明理由 |
| 2026-08-24 | **B3 第一批 + 代理域 P1**：空洞绿 T30 / T31 / T32 修复完毕，代理域四条 P1（AGENT-01 表单创建落库、AGENT-04/23 富字段保存零丢失、AGENT-07 过期保存被拦、AGENT-35 真实完整性告警）落成新 spec `e2e/agent-authoring.spec.ts`，全部本机实跑通过并逐条变异实证。两处与审计建议不同：①AGENT-31 的修法改为**存在性不可区分**（把「真实但不可见的 id」与「不存在的 id」两次访问的可见文本逐字比较），比「断言进不去」更难写成恒真；②WF-49 的版本 CAS 无法通过 UI 造出来——编辑器通过 WS 立刻同步了外部保存，`expectedVersion` 不会过期（产品做对了），改在合同层断言过期删除被拒 409 且资源仍在。T32 同时纠正了审计没点出的一层：那条唯一的浏览器断言不但被 `RUN_VISUAL_REGRESSION` 关着，而且用 `page.route` 把 `/resource-status` 响应整个换掉，后端完整性计算一行都没跑过；新用例走真实路径（停用被引用的插件）。账本随之收敛：端点 271 → **268**（新覆盖 `DELETE /api/workflows/:id`、`GET`/`PUT /api/plugins/:id`——第一条正是 T31 那条空洞绿此前假装测过的东西），路由 18 不变。`PATCH /api/auth/me/profile` 出现在未覆盖集里但**未写入账本**：它是并发 session RFC-320 尚未入库的新端点，那是他们的债，等它落库后由棘轮向他们提出 |
| 2026-08-24 | **B1 落地**：T10/T11/T12/T14/T15 完成，T13 推迟到第一条 nightly 档后端用例落地时一起做（现在建空目录 + 排除清单，只会得到一条无人验证的配置）。分档用 Playwright 原生 tag：不带 tag = PR 档（今天 340 条一条不动），`@nightly` = 夜跑档。新增 `.github/workflows/e2e-full-nightly.yml`（06:00 UTC，4 分片，**不加任何 grep 过滤**，设 `AW_E2E_ROUTE_JOURNAL`，四分片 journal 汇总后跑两条账本守卫；上游没全绿或分片数不足时**拒绝对账**而不是拿残缺语料比账本）。`rfc319-ci-topology.test.ts` 钉死三处静默失效面，三条变异全部实证转红。`gate:local` 加 `AW_GATE_E2E=1` 可选 Playwright 车道（默认关——门禁慢到让人跳过就等于失效） |
| 2026-08-24 | **B2 第一批落地**：T20/T21/T22/T23/T24/T28 完成，T29 对本批四条守卫做过变异实证。三处与落档设计不同，均已回写 `design.md`：①R1 采集挂在 `AW_E2E_ROUTE_JOURNAL` 开关下（落档时列为退化方案，实做直接采纳——PR 腿行为与今天逐字节相同）；②R2 的分子改用**同一份 journal 里的 SPA 文档请求**，不再需要 T25 的 fixture 迁移作为前提（T25 从必需项降级为增强项）；③R2 的「源码提过」从通过判据降级为纯诊断——实测发现它会把 `/code/*` 十条路由整族漂绿（那些出现全在默认不跑的视觉套件里）。实测值：端点 462 声明 / 191 命中 / **271 从未**；路由 60 / 42 / **18 从未**（其中 14 条「提过却从未加载」）；归一器反方向零失配 |
