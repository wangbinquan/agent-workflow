# RFC-245 设计门记录（2026-08-01）

跑法：`codex exec --sandbox read-only`（companion 路径本机不可用，见 `docs/dev-gotchas.md` §Codex
与个人 memory 的 1.0.6 × 0.146.0 不兼容记录）。范围 = 三件套 + 仓内 ground truth 交叉核验。

**结果：2 P0 / 3 P1 / 4 P2。** 逐条已复核源码后处置如下——**1 条 P0 部分驳回并给出理由，
其余全部折入**。

| #   | 级别 | 结论                                                                        | 处置                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ---- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | P0   | **部分驳回** — 「ULID 最大 ≠ 最新 mint，应先引入持久递增 generation」       | 事实成立但结论不适用：排序权威是 `services/freshness.ts:155-161` 的 `isFresherNodeRun`（RFC-074 PR-C 明定纯 ULID 比较 + baseline 测试锁），调度器 / 上游输入 / 画布状态 / 评审 / 反问全用它。本 RFC 改为**显式镜像该函数**而非自创口径（design D2 重写）；把「id 单调性是全仓前提」登记进 `docs/audit-backlog.md` 作系统级 RFC 处理。按 dev-gotchas §impl-gate 经验，平台级 finding 塞进 UX RFC 会引入更大 regression。 |
| 2   | P0   | **成立** — children 查询可把正常创建的 live child 永久判成缺席              | 复核确认：`useTaskChildren` 的 `refetchInterval` 在 `[]` 时当场关闭；`useTaskSync` 的失效键全是 `['tasks', taskId, …]`，**不覆盖** `['tasks','children',parentId]`。新增 **D9 + T10**：事件失效（`node.status` / 任务终态）+ 父任务活跃期轮询。顺带修好 `ChildTaskLink` 今天会常驻误显「子任务不可见」的同一缺陷。                                                                                                      |
| 3   | P1   | **成立（属 RFC-243 后端）** — retry cascade 不取消下游 call 行的存活子任务  | 复核确认 `services/task.ts:2982-3000` 只取消被直接重试那一行的 child。本 RFC 的**行为仍安全**（最新行无 childTaskId ⇒ 不可点 ⇒ 不误跳），只是 D2 的论据被过度断言 → 已改写为精确表述；后端问题登记 `docs/audit-backlog.md`，本 RFC 不动后端。                                                                                                                                                                           |
| 4   | P1   | **成立** — D1 的失败横幅缓解不足，抽屉是调用节点**唯一**的 Retry / 历代入口 | 复核确认 `NodeDetailDrawer.tsx:113-130,188-210` 的 Retry（含 cascade）与 StatsTab 历代切换；失败横幅只覆盖 `status==='failed' && failedNodeId===该节点`。D1 不变（画布仍不开抽屉），新增 **T9**：「节点运行」表 **call 行**加「运行详情」入口，精确补偿被移除的那一个入口。                                                                                                                                             |
| 5   | P1   | **成立** — 漏了 def-sync `useEffect` 的依赖数组                             | 复核确认 `WorkflowCanvas.tsx:825-845` 的 deps 里有 `reviewNavs` / `clarifyNavs`。只加 ref-guard 不加 deps ⇒ effect 不重跑 ⇒ 提示行与点击行为分叉。新增不变量 **I2b** + 重渲染测试（§7 第 5 组）。                                                                                                                                                                                                                       |
| 6   | P2   | 成立                                                                        | selection 楔子伪代码把签名复位归因给了 `onSelectNodeRun(null)`；实为最先调用的 `clearSelection()`（`:2196-2202`）。注释已改，两个调用职责分清。                                                                                                                                                                                                                                                                         |
| 7   | P2   | 成立                                                                        | D8 症状写错：`NodeDetailDrawer` 在 `run === undefined` 时 `return null`（`:128-129`），不渲染坏抽屉。真实症状是 `taskCanvasLayoutClass` 仍给 `--with-drawer` 留空栏 + 其它 task 作用域状态污染。D8 与验收项已改写。                                                                                                                                                                                                     |
| 8   | P2   | 成立                                                                        | `toFlowNodes` 现为 **13** 个位置参数（proposal 原写 14）；按位置调用 `__testToFlowNodes` 的测试共 **9** 个文件，原计划只列 4 个。已改数字并补全回归备查表。                                                                                                                                                                                                                                                             |
| 9   | P2   | 成立                                                                        | 父任务「存在但不可见」是 **403**（`routes/tasks.ts:810-821`），只有不存在才 404。组件对任意 `probe.isError` 都降级故无泄露，但测试要分别覆盖 403 / 404 两条路径。已写入 §7。                                                                                                                                                                                                                                            |

折入后 RFC 的净变化：新增 D9 / I2b / T9 / T10、改写 D2 与 D8、修正 §3.5 ACL 口径与 §7 测试面、
proposal 增 A10–A12 并调整非目标。两条仓级问题登记 `docs/audit-backlog.md`。
