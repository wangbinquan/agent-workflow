
### 后端｜HTTP 路由与 API 契约

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B1-routes-1` | P0 | happy-path-only | POST /api/auth/sessions/:id/revoke、DELETE /api/auth/identities/:id、DELETE /api/auth/pats/:id 三条 endpoint 的归属校验全部是 route-local 手写代码（查行 → 比 userId → 抛 403），但没有任何用例用「用户 A 的 token 去操作用户 B 的 session/identity/PAT」验证它会被拒。现有 … | 任何登录用户只要拿到别人的 session id / PAT id / identity id（这些 id 会出现在管理页、审计列、日志里），就能把对方踢下线、吊销对方的 CI token、或解绑对方的 OIDC 登录方式（若对方无本地密码即等于永久锁死账号）。一次重构把 `row.userId !== acto… | integration：在 packages/backend/tests/auth-routes.test.ts 里加一个双用户 harness（alice/bob 各自 createUser + createSession），补三条用例：(1) bob 用自己的 token POST /api/auth/sessions/{alice… |
| `B1-routes-2` | P1 | no-test-at-all | routes/fusions.ts 挂了 POST /api/fusions、GET /api/fusions、GET /api/fusions/pending-count、GET /api/fusions/:id、POST /api/fusions/:id/{approve,reject,cancel}，全部 7 条在 packages/backend/tests 下零命中（只出现在 registry.ts 与前端 mock 测… | 普通用户调 GET /api/fusions 会看到全站所有人的 fusion 摘要（含 skillName + intent 文本），或反过来 owner 自己看不到自己的行；GET /api/fusions/:id 的存在性隔离一旦退化成 403，就能被用来枚举他人 fusion id。approve/rej… | integration：新建 packages/backend/tests/fusions-http.test.ts，用 createApp + 双用户 session（alice owner / bob stranger / admin）：(1) alice 建 fusion → 201；(2) GET /api/fusions 分别… |
| `B1-routes-3` | P1 | harness-cannot-express | routes/resourceAcl.ts 用 const path = `${cfg.base}/:${cfg.param}/acl` 后 app.get(path, …) / app.put(path, …) 注册；而 api-contract-coverage.test.ts 的扫描正则 ROUTE_RE 要求 app.get( 后紧跟引号字面量，所以这 12 条（agents/skills/mcps/plugins/wor… | ACL 变更端点（能改 owner、能加/删授权）是全仓权限模型的写入口，却没有一条匿名 401 回归；/api/skills/:name/acl 的 GET/PUT 更是连一条用例都没有——非 owner 改别人技能授权、或匿名读到授权成员名单这类退化不会被任何测试发现。同时契约守卫给出的「登记完整」保证是虚的… | ci-gate：两件事一起做：(1) 把 api-contract-coverage.test.ts 的发现器补一条针对 mountAclEndpoints 的展开——扫 routes/*.ts 里的 mountAclEndpoints(app, deps, { … base: '/api/x', param: 'name' …… |
| `B1-routes-4` | P1 | no-test-at-all | routes/oidc-auth.ts 的 GET /api/auth/oidc/providers、POST /api/auth/oidc/:slug/login/start、GET /api/auth/oidc/:slug/callback 三条在所有测试目录零命中。这三条是**未认证即可访问**的登录入口，路由里手写了大量分支：secretBox 缺失 → 503、provider 不存在或 !provider.enable… | 管理员禁用某个 IdP（enabled=false）后若 !provider.enabled 判断被改坏，被禁用的身份源仍能签发有效 session——等于关不掉的登录后门；state 一次性消费（重放防护）退化也无人察觉。且这些是匿名可达路由，是攻击面最外层。 | integration：新建 packages/backend/tests/oidc-auth-routes.test.ts，用 createApp（带 secretBox）+ 种一个 provider：(1) enabled=false 的 slug POST /login/start → 404 provider-not-found… |
| `B1-routes-5` | P1 | no-test-at-all | routes/oidc.ts 的 GET/POST /api/oidc/providers、GET/PATCH/DELETE /api/oidc/providers/:id、POST /api/oidc/providers/:id/test 六条在 packages/backend/tests 下零命中（只有 packages/frontend/tests/oidc-confirm-dialog.test.tsx 这个 mock … | 若 GET/PATCH 某条路径漏掉 redactedProvider（或 redactedProvider 新增字段时漏改），IdP 的 client secret 会随管理页请求明文回到浏览器并进 devtools/日志；DELETE 的 force 语义再次退化则「强制删除仍在被引用的 provider」静… | integration：新建 packages/backend/tests/oidc-routes-http.test.ts（harness 必须传 secretBox，否则路由不挂载）：(1) POST 建 provider → 201，断言响应 JSON 序列化后的整串**不包含**原始 clientSecret，GET 列表 / … |
| `B1-routes-6` | P1 | cross-module-seam | RFC-099 的五类资源里，只有 agents 与 workflows 在 rfc099-resource-routes.test.ts 有路由级端到端用例（私有资源对陌生人 list 排除 + detail 404 与缺失同形 + 非 owner 写拒绝），workgroups 在 rfc164-workgroups.test.ts:439/461 有同款。skills 与 mcps 两类的路由测试（skills.test.t… | 某次重构在 skills 或 mcps 的某条写路由（save / file PUT / file DELETE / versions restore / rename）上漏掉 requireResourceOwner，任何登录用户就能改写别人私有技能的正文与文件；漏掉 loadVisibleSkill 则私有技… | integration：照 rfc099-resource-routes.test.ts 的 harness（alice/bob/carol/admin 四 token）扩两个 describe：'RFC-099 — skills route ACL' 与 '— mcps route ACL'。每个至少四条：(1) alice 建资源 … |
| `B1-routes-10` | P2 | text-assertion-only | GET /api/tasks/:id/call-targets、GET /api/tasks/:id/nodes/:nodeRunId/stdout、GET /api/tasks/:id/recovery-events、POST /api/tasks/:id/clear-recovery-suspension 四条在所有测试目录零命中。它们各自的 service 层其实测得很扎实（getCallTargets 见 structur… | 隔离检疫解除按钮（RecoverySection 的「清除挂起」）如果路由取错 param 或 service 换签名，界面点了没反应而测试全绿——任务永久卡在 auto-recovery quarantine；stdout 从 text/plain 变 JSON 会让日志面板整片空白；call-targets … | integration：在 packages/backend/tests/tasks.test.ts（已有 HTTP harness）补四条：(1) GET /api/tasks/{id}/call-targets 不带 methodRef → 422 且 body.code==='call-target-method-required… |
| `B1-routes-11` | P2 | no-test-at-all | runtime-routes-registry.test.ts 系统覆盖了 GET /api/runtimes、POST /api/runtimes（含 403 非管理员、409 重名）、PUT /:name、DELETE /:name、POST /:name/enabled（含 403/409）、POST /api/runtimes/probe，rfc135-runtimes-status.test.ts 覆盖 GET /api… | 管理员门若脱落，普通用户可通过该端点触发任意已注册 runtime 二进制的探测执行（旁路命令执行面）并覆写探测缓存；未知 name 的 404 退化成 500 则会在设置页抛未处理错误。它与已测的 POST /api/runtimes/probe（403 已锁）是一对，只有带 :name 的那条漏了。 | integration：在 packages/backend/tests/runtime-routes-registry.test.ts 补两条，复用该文件已有的 fake-binary 与 user/admin token：(1) 普通用户 POST /api/runtimes/opencode/probe → 403；(2) 管理员… |
| `B1-routes-7` | P2 | happy-path-only | 把 routes/*.ts 里所有 ValidationError/NotFoundError/ForbiddenError/ConflictError/DomainError('<code>' 抽出来共 95 个码，逐个 grep packages/backend/tests + packages/frontend/tests + packages/shared/tests + e2e，有 51 个零命中（注意：这些码在 fro… | 任一 zod schema 被放宽（例如某字段从 required 变 optional、strict 变 passthrough）后，非法请求会静默穿过路由进入 service 与 DB，而不是返回 422；前端按 code 做的错误提示分支（i18n errorCodes 命名空间）也会因为 code 改名而… | ci-gate：在 tests/contracts/registry.ts 的 EndpointSpec 上加一个可选 reject: { body?, query?, status, code } 字段，让 api-contract.test.ts 像跑 happy 一样为每条声明生成一条拒绝用例；再加一条与 coverage… |
| `B1-routes-8` | P2 | no-test-at-all | POST /api/users/lookup（RFC-099 的批量 id→公开字段解析，供评审评论 / 反问归属 chip 用）在 packages/backend/tests、packages/frontend/tests、packages/shared/tests、e2e 四处均零命中，其 service 实现 lookupUsersPublic 同样零命中。路由里有三段无人验证的逻辑：非 string 元素过滤、.slic… | 投影列表若被改成 ...r 或补上 email，全站任意登录用户就能用一次批量请求把所有用户的邮箱拉走（这正是 searchUsersPublic 那条「public 5-field view」用例在防的东西，lookup 却没有同款）；200 条上限失效则变成 O(n) 全表拉取的放大器。 | integration：在 packages/backend/tests/users-http.test.ts 里补一个 describe('/api/users/lookup')：(1) 普通用户 token POST { ids:[alice.id, bob.id, '不存在的 ulid'] } → 200，返回 2 条、未知 id… |
| `B1-routes-9` | P2 | no-test-at-all | 该 endpoint 在四个测试目录零命中：users-http.test.ts 覆盖了 /api/users 的 list/create/patch/delete/search，唯独没有 reset-password；users-service.test.ts 测的是 service 函数 resetPassword，users-cli.test.ts 测的是 CLI 子命令。路由自身的三段逻辑（users:write 权限门、… | 权限门若退化（例如误挂成 account:self），任意登录用户可重置任意账号密码并接管管理员；弱密码 / 空密码的 422 拒绝失效则可把某账号密码设成空串。管理员在界面上点「重置密码」是账号接管的最短路径，却是 users 路由里唯一没有 HTTP 用例的动作。 | integration：在 users-http.test.ts 补一个 describe：(1) 管理员 token POST /api/users/{bob.id}/reset-password { password:'newLongPassword' } → 200，随后用新密码 POST /api/auth/login 成功、旧… |

### 后端｜任务生命周期与调度

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B2-lifecycle-1` | P1 | mock-too-deep | `reconcileDeadRunningRuns` 的判据函数是注入式的，生产由 `startOrphanReconcileLoop` 注入真实探针 `runProcessGone`；但四条 reconcile 用例全部注入 `isGone: () => true/false/按 id`，真实探针的三条分支（pid 为 null / pid 不存活 / pid 存活但 `ps` 命令行不含 spawnBinaryPath）零执行… | 在没有 `ps`（或 ps 被沙箱拦截）的部署环境里，每 10 分钟一次的周期回收会把正在跑的 node_run 判成 interrupted，进而把任务翻成 interrupted：用户看到「任务被中断，请恢复」，而 opencode 子进程仍在后台写 worktree；恢复时再叠加一次回滚，等于凭空丢失一段 … | unit：直接对 `runProcessGone` 写单测（不经 reconcile）：① pid=null → true；② 用 `Bun.spawn` 起一个真实短命子进程并 wait 后拿其 pid → true（不存活）；③ 用 `process.pid` + `spawnBinaryPath = process.… |
| `B2-lifecycle-3` | P1 | harness-cannot-express | `setTaskStatus` 的唯一写点里内嵌了运行时长累加逻辑：进入 `running` 开一段（写 runningSince），从 `running` 离开则 `runningMs += now - COALESCE(runningSince, now)` 并清空 runningSince —— 目的是让 awaiting_review/awaiting_human 的等待时间不计入 `maxDurationMs`。`enf… | 记账写反/漏写时测试全绿：漏掉「离开 running 累加」→ runningMs 恒为 0，maxDurationMs 永不触发，跑飞的任务无限烧 token；漏掉「清空 runningSince」→ 任务在 awaiting_human 等人回答期间继续计时，人一回答就被 1Hz 的 enforceLimit… | unit：对 `setTaskStatus` 写纯记账单测（注入固定时钟）：pending→running 只写 runningSince；running→awaiting_human 累加并置 null；awaiting_human→running（经 resume→pending→claim 真实链路）保留旧 runn… |
| `B2-lifecycle-6` | P2 | concurrency-timing | runScope 把自动 commit&push 作为合成 in-flight 项挂进同一张 `inFlight` Map，键是 `commitpush:${nodeId}:${iteration}`。而 RFC-092 audit S-1 的 `dispatchedPendingRowIds` 机制明确允许**同一 invocation、同一 iteration 内**某节点因中途 clarify 回答 / review 决策 … | 被覆盖掉的那个提交会话不再出现在 `inFlight` 里：`inFlight.size === 0` 可能提前为真、runScope 返回、runTask 的 finally 执行 `gcTaskWriteSem` 并丢弃任务 —— 而那个提交会话仍在后台对 worktree 跑 git add/commit/… | integration：构造用例：单个 topLevel 写节点 + autoCommitPush=true，节点首次完成后由测试在同一 iteration mint 一条 pending rerun 行（复用 rfc092/S-1 的手法）触发同节点二次派发+完成，且把 commit&push 打慢（复用该文件已有的 slow com… |

### 后端｜Git / worktree / 隔离 / commit-push / submodule

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B3-git-1` | P0 | no-test-at-all | nodeIsolation.ts:1132 的 fail-closed 闸门（`r.pendingSubResolves.length > 0` ⇒ 判 unresolved、跳过父仓重探）从来没有被非空的 pendingSubResolves 驱动过；配套的持久化函数 isolatedAgentRun.ts:262 `persistPendingSubResolves`（单仓 iso_submodules_json / 多仓 i… | 子仓冲突没人解决时，用户在任务详情点「已解决 / 继续」，父仓层重探看到干净（gitlink 在 theirs 里根本没被改写），任务被判 resolved 并把一个从没人裁决过的 gitlink materialize 进 canonical 工作树——表现是「审查通过了」，实际上另一个节点对子仓的改动被静默丢… | integration：两条用例：①（服务级）构造一个真实带 submodule 的 canon + 两个 iso，让两节点改同一行子仓文件制造 subMerge 冲突，mergeBackNodeIso 后断言 `handle.repos[0].pendingSubResolves` 非空，然后直接调 completeHumanReso… |
| `B3-git-2` | P0 | happy-path-only | captureSubmoduleTopology 用 `poolDir ??= pool`（nodeIsolation.ts:303）只把**第一个**子仓的 module dir 记成 handle 级 poolDir，而 publishSubmoduleHeads（nodeIsolation.ts:572 pushObjectsToPool(r.poolDir, …)）、mergeSubmodulesIntoTheirs（no… | 一个仓库有 vendor-a 和 vendor-b 两个子仓时，vendor-b 的节点提交被 fetch 进 vendor-a 的 module dir，而 canonical 的 vendor-b 只 borrow 自己的 pool → merge-back 时 canonical 读不到那个 commit：… | integration：把现有 canonWithSubmodule fixture 扩成两个并列子仓（vendor-a / vendor-b，各自独立源仓），跑 createNodeIso → 在**两个**子仓里各提交一次 → snapshotNodeIsoFinal → mergeBackNodeIso，断言：(a) handle… |
| `B3-git-3` | P1 | happy-path-only | RFC-210 标题就是「递归 submodule 隔离」，代码里三处专门为嵌套写的逻辑——mergeSubmodulesIntoTheirs 的按深度排序（nodeIsolation.ts:458）、rewriteGitlinkInCommit 对嵌套路径的 `update-index --cacheinfo 160000,<sha>,<subPath>`（gitSubmodule.ts:557）、materializeTree… | 用户仓库是两层 submodule（很常见：vendor/ 下再挂 third_party/）时，内层子仓的合并结果 fold 回 theirs 会失败 → `rewriteGitlinkInCommit` 返回 null → 被当成冲突 → 整仓扣住转人工；或者 materialize 递归把中间层 check… | integration：建 inner→mid→parent 三级 fixture（复用 rfc210-subrepo-snapshot-rollback.test.ts:128 的搭法），跑 createNodeIso → 在 `vendor/nested` 里提交 → snapshotNodeIsoFinal → mergeBack… |
| `B3-git-4` | P1 | no-test-at-all | nodeIsolation.ts:704 新增的 `merge.conflicts.find((p) => r.subBases[p] !== undefined)` 分支——父仓层合并冲突命中已知 submodule 路径时跳过 buildSalvageTree 直接扣住整仓——没有任何用例。现有最接近的 rfc210-recursive-submodule-merge.test.ts:172「同一行冲突扣住整仓」走的是**另一… | 两个并发节点一个删 submodule 一个改它时，canonical 工作树里整个子仓目录被 `rm -rf`（连同用户没提交的内容），任务却报告「部分路径已 salvage 落地」。这是不可回滚的数据丢失，且当前唯一的防线是一条没跑过的 if。 | integration：两条用例：①节点 A 在 iso 里 `git rm` 掉 vendor 并提交、节点 B 在 vendor 里提交内容；A 先 merge-back，B 再 merge-back，断言 `res.clean===false`、`conflicts[0].salvagedPaths` 为空、且 canonical… |
| `B3-git-6` | P1 | happy-path-only | scheduler.ts:1608 `for (const repo of state.repos)` 逐仓建 commit node_run（nodeId 带 repoSlug、baseRef 取 repo.baseBranch）、逐仓跑 commit agent 会话。所有 commit-push 用例都是单仓：scheduler-commit-push.test.ts 里 `repos:` / `repoCount` / `… | 多仓任务开了自动提交推送时，某一仓的认证失败/非快进可能拖垮或静默跳过其余仓；或者所有仓都用了 repo-0 的 baseBranch 去 push。用户看到的是「三个仓只有一个被推上去了，任务却是 done」。 | integration：扩 scheduler-commit-push.test.ts：起一个两仓任务（两个本地裸 remote），仓 1 正常、仓 2 的 remote 设成不可写，断言产生两条 `commitpush:` node_run、nodeId 各带自己的 repoSlug、仓 1 的 remote 真的前进、仓 2 落 c… |
| `B3-git-7` | P2 | text-assertion-only | T28 新增的 dropNodePoolRefs（nodeIsolation.ts:934）本意是「节点级 `refs/agent-workflow/pool/<task>/<run>/<slug>` 随 discardNodeIso 删除，避免跨任务共享池无界增长」，但配套用例 rfc210-iso-submodule-topology.test.ts:187 只断言「discard + gc 之后 landed 对象仍可读」—… | 长跑实例上，宿主仓 `.git/modules/<sub>` 里的 anchor ref 只增不减：每个节点每个子仓一条、每个任务每个子仓再一条，`git gc` 永远收不回这些对象，磁盘持续膨胀且 for-each-ref/gc 越来越慢。属于慢性资源泄漏，不会报错所以不会被发现。 | integration：在现有 topology 用例里补 `git -C <pool> for-each-ref refs/agent-workflow/` 的正负断言：discardNodeIso 后 `pool/<task>/<run>/*` 必须为空、`wt/<task>/*` 必须仍在；再新增一条 GC 用例——runWork… |
| `B3-git-8` | P2 | env-gated-or-skipped | createWorktree 的契约明确写着「submoduleInitOk=false 时调用方应告警但**不得**让任务启动失败」（util/git.ts:487-495），task.ts:1115 只落一条 log.warn 并把 redacted 错误写进 task_repos。但 worktree-submodule-init.test.ts 的三条用例（:92 成功 / :105 mode=never / :119 无… | 私有子仓凭据失效时，可能整个任务启动被判失败（与契约相反），或者反过来——静默启动一个子仓目录全空的 worktree，agent 在空目录上工作、审计基于空内容通过。而且没人验证写进 task_repos 的 stderr 已经过 redact，凭据可能进 DB / 备份。 | integration：在 worktree-submodule-init.test.ts 里加一条：.gitmodules 指向一个不存在的 file:// 路径（本地可控、不需要真网络，可脱离 RUN_GIT_NETWORK 门控），断言 createWorktree **不抛**、返回 submoduleInitOk=false … |
| `B3-git-9` | P2 | no-test-at-all | checkoutMergedGitlinks（util/git.ts:2101）有两条被注释明确定义为「刻意分开」的失败路径：stderr 匹配 `would be overwritten/local changes` 时用 `-f` 重试并只告警（:2126-2136），其余情况抛 `materialize-failed: submodule '<p>' cannot be moved to <sha>`（:2138）。全仓 t… | 用户在 canonical 的子仓里留了未提交编辑时，merge-back 的 materialize 要么把编辑强制冲掉（无告警可见）、要么把整个节点变成 merge-failed。两种表现天差地别却都没被锁定，任何一次重构都能悄悄互换。 | unit：两条 materializeTree 直测：①在 canonical 子仓里写一个未提交改动，再 materialize 一个把该子仓推到新 sha 的 mergedTree，断言不抛、子仓 HEAD 到位（并显式断言用户编辑的去向，把当前语义钉死）；②把 mergedTree 里的 gitlink 指向一个 p… |

### 后端｜运行时执行层（opencode/claude 进程、envelope、capture、fanout）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B4-runtime-1` | P0 | contract-drift | `cwdSlug(cwd)` 只把 `/` 替换成 `-`；本机安装的 claude-code 2.1.215 真实算法是 `cwd.replace(/[^a-zA-Z0-9]/g,"-")` 外加 200 字符截断 + hash 后缀（二进制中 `function PA(e){let t=e.replace(/[^a-zA-Z0-9]/g,"-");if(t.length<=YEt)return t;return`${t.sli… | 用 claude-code 运行时跑的任何节点，SessionTab 的子代理会话树恒为空；用户看不到 subagent 干了什么，也看不到任何失败提示（连 `subagent_capture_failed` 兜底 marker 都不会写，因为不抛异常）。排障时会误以为 claude 没有起 subagent。 | unit：两条：(1) 把 slug 改成与 claude 一致的 `replace(/[^a-zA-Z0-9]/g,'-')` + 200 截断（截断分支需要对齐 claude 的 hash 后缀，或改为扫描 `projects/` 下目录做前缀匹配，避免再次逐字复刻外部私有算法）；(2) 加行为测试：用**含 `.`/… |
| `B4-runtime-2` | P1 | contract-drift | CLAUDE.md 与 RFC-073 把「inline JSON 在 opencode 合并序里最后生效，因此平台的 agent 定义/权限恒胜」当作已解决的开放问题，`buildInlineConfig` 据此注入顶层 `permission {"*":"allow","question":"deny"}` 并剥掉 per-agent 的 `question` 复活键。但全仓没有任何用例在 worktree 里放一个同名 `.… | opencode 任何一次改动合并序（或 repo 里恰好有同名 agent），平台会静默改用用户仓库里的 agent 定义：persona/model 变了、`readonly` 约束失效（本应只读的审计 agent 获得写权限，与并行写节点撞车污染 worktree）、`question:deny` 被复活导… | integration：在 `tests/integration-opencode/` 增两条真进程 case（沿用现有 RUN_OPENCODE_INTEGRATION 门）：① worktree 里写 `.opencode/agent/<same-name>.md`（persona 里要求输出一个特征串），同时用 OPENCODE_… |
| `B4-runtime-3` | P1 | mock-too-deep | `.github/workflows/integration-opencode.yml` 只有 opencode 一条腿（矩阵是 opencode 版本，路径过滤也只列 runner/envelope/protocol/opencode-plugin）。claude 侧的 argv 契约（`-p --output-format stream-json --verbose --permission-mode bypassPermis… | claude 升级掉一个 flag（例如 `--append-system-prompt-file` 改名、stream-json 的 `result`/`usage` 字段调整）后，CI 全绿，但线上每个 claude 节点要么直接 exit≠0 报 unknown option，要么 token 计费恒零 /… | integration：仿照 integration-opencode 建一个 env-gated 的 `tests/integration-claude/`（`RUN_CLAUDE_INTEGRATION=1` + ANTHROPIC_API_KEY 才跑），至少 4 条：`--version` 可解析且 ≥MIN；一次最小 `-p`… |
| `B4-runtime-5` | P1 | no-test-at-all | `buildCommand` 把整个 prompt 作为 `run <prompt>` 的位置参数塞进 argv，而 `buildOpencodeEnv` 还把整份 `OPENCODE_CONFIG_CONTENT`（agent body + 注入记忆 + MCP/插件配置）放进 env —— 两者共享内核的 argv+env 预算。git wrapper 的 `git_diff` 端口是**未截断**的全量 diff（`util… | 某个节点改了 lock 文件或生成了大量代码（几百 KB diff）后，下游审计/修复节点在 Linux 上 100% spawn 失败，用户看到的是 `spawn opencode failed: ...E2BIG` 这类内核错误而非「输入过大」；macOS 上表现为更晚触发的同类失败。任务在流水线中段炸掉且无… | unit：生产侧对最终 prompt 加显式上限（超限走可读的 `prompt-too-large` 失败或对 diff 端口做带提示的截断）；测试侧加两条：① 纯函数断言超限 prompt 被拒/被截并带 notice；② runNode 集成用 200KB+ 的 prompt 跑 mock-opencode（mock … |
| `B4-runtime-6` | P1 | no-test-at-all | `pumpLines` 的 `buffer` 按 `\n` 切行，一条没有换行的巨行会无限累积在内存里；每行都无条件 `db.insert(nodeRunEvents)` 写全量 payload；`agentText` 数组把所有 text 事件文本留到结束再 `join('\n')` 做 envelope 解析。三处都没有字节上限、没有行数上限、没有单行截断。全仓测试里最大的字符串构造是 1 万字符量级（commit-push-… | agent 一次 `cat` 了大文件或工具回显了几百 MB（LLM 失控循环并不罕见），daemon RSS 直线上升直至 OOM 被杀 —— 所有并发任务连同 WS 连接一起死；即便不 OOM，SQLite 也会被灌进巨量大行，events 归档与 SessionTab 查询一起变慢。 | integration：生产侧加三道闸：单行截断（超过 N KB 截断并打标）、单次 run 的 stdout 总字节上限（超限终止子进程并以可读原因 failed）、envelope 解析只保留尾部窗口。测试侧用 mock-opencode 新增旋钮吐出 >上限 的 stdout，断言：进程被终止、节点 failed 且 errorM… |
| `B4-runtime-7` | P1 | text-assertion-only | `probeOpencode` 的 `compareSemver(version, MIN_OPENCODE_VERSION) < 0` 分支（返回 `compatible:false` + `incompatibleReason`）以及 `cli/start.ts` 的 `lock.release(); process.exit(1)` 拒绝启动，全仓没有任何用例真的拿一个「能跑但版本过低」的 stub 二进制走一遍。`open… | 两个方向都会漏：把版本比较改错（或 `ran`/`version` 语义再演进一次）后，过旧 opencode 能启动 → 运行时以各种诡异形态失败（事件流不匹配、envelope 收不到）；反向改错则健康环境被误拒，用户完全起不了 daemon 且只有一行 console 提示。这是保护整个产品的边界策略，却只… | unit：复用 `rfc135-runtimes-status.test.ts` 已有的 `writeVersionBinary` 手法写 stub：打印 `stub-opencode 1.13.99` → 断言 `probeOpencode` 返回 `compatible:false` 且 `incompatibleRe… |
| `B4-runtime-8` | P1 | mock-too-deep | `opencodeSessionWalk` 直接对 opencode 的私有库发裸 SQL（`SELECT id,parent_id,agent FROM session WHERE parent_id=?`、`SELECT id,time_created,data FROM message WHERE session_id=?`、`part` 同理），`resolveOpencodeDbPath` 还硬编码了 `xdg-base… | opencode 任一版本改列名/改存储位置（它已经历过 Effect-TS 重写这类大改），子代理会话捕获全线静默失效：SessionTab 只剩 `subagent_capture_failed` 兜底或空树，RFC-048 实时轮询连续失败后自动禁用，蒸馏也拿不到素材 —— 而 CI 全绿。 | integration：在 `tests/integration-opencode/` 加一条真进程 case：跑一次会拉起 subagent 的最小 prompt，然后用 `resolveOpencodeDbPath` 打开真库执行 `walkOpencodeSessions`，断言能走到子 session 且 message/par… |
| `B4-runtime-9` | P2 | contract-drift | 生产每次 dispatch 都由 `nodeRunMint` 生成 16 位 hex nonce，prompt 里下发 `<workflow-output nonce="…">`，解析器 `envelopeRe(nonce)` **只**认带该 nonce 的开标签（bare/错 nonce 一律不可见）。但唯一跑真模型的 case 用的是 bare 字面量 `<workflow-output><port name="answer… | 若某个模型/opencode 版本对带属性的开标签处理不同（改写属性、加空格、markdown 转义、把长 hex 断行），线上每个节点都以「无 envelope」失败并进入重试→耗尽，而在线漂移哨兵仍然绿——它验的是产品早已不再使用的形态。 | integration：把在线 case 4 改成（或并列增加）nonce 形态：用真 `mintEnvelopeNonce` 生成 nonce、按 `envelopeOpenTag(nonce)` 组 prompt，断言 `detectEnvelopeKind(text, nonce)==='output'` 且 `extractLa… |

### 后端｜认证 / 授权 / ACL / 凭据 / 注入边界

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B5-security-1` | P1 | no-test-at-all | skills.ts 的 18 个路由全部依赖 loadVisibleSkill（不可见 → 404 同形）与 requireResourceOwner（非 owner → 403）两个门，覆盖列表过滤、详情、SKILL.md 内容、文件树、单文件读/写/删、版本历史/diff/回滚。这整条链上没有任何测试用第二个用户跑过：全仓 40 处 `visibility: 'private'` 测试构造里 skill 出现 0 次，30 处… | 若 skills 路由的某个门被重构掉（例如把 loadVisibleSkill 换回 getSkill、或漏掉某个新增子路由的 requireResourceOwner），任意登录用户就能读到他人私有技能的 SKILL.md 正文与 files/ 树下的全部文件（技能正文里常写内部流程、内网地址、示例凭据），甚… | integration：新建 `packages/backend/tests/skills-acl.test.ts`，按 worktree-files-acl.test.ts 的 harness 形制建 owner / grantee / stranger / admin 四个真实用户 + session token，对 skill `… |
| `B5-security-2` | P1 | no-test-at-all | routes/auth.ts 的三个自服务写端点各自手写了归属校验：会话撤销先按 id 查行再比 row.userId、PAT 删除把 userId 拼进 where、identity 解绑先列出本人 identity 再做 some(id)。三处都不是共享原语、各写一遍，但没有任何测试用 A 的 token 去操作 B 的资源。auth-routes.test.ts 只跑了本人 PAT 的 create/list/revoke … | 任一守卫在重构中失效（例如把 PAT 删除的复合 where 拆成按 id 单查、或会话撤销漏掉 row.userId 比较），任意登录用户就能凭猜到/泄露的 id 强制下线他人会话、吊销他人 PAT（拒绝服务），或解绑他人的 OIDC 身份绑定——后者结合 OIDC invited-email 绑定路径还可能演… | integration：在 auth-routes.test.ts 新增一个 `describe('自服务端点的跨用户 IDOR 必须 403')`：建 alice / bob 两个 active 用户各自的 session token 与 PAT，断言 ① bob 用自己的 token `POST /api/auth/sessions… |
| `B5-security-3` | P1 | no-test-at-all | lookupUsersPublic 先 `db.select().from(users)` 拿到含 passwordHash、email、createdBy 等全部列的行，再手工映射成 5 字段的 UserPublic。这个「手工窄化」是唯一的防线，而 `users:search` 属于 USER_BASELINE（任何登录用户都有），端点接受最多 200 个 id 的批量查询。姊妹端点 `/api/users/search` 有… | 任何把 lookupUsersPublic 简化成 `return rows`（或改成复用 findById 之类返回完整行）的重构，都会让每个普通登录用户通过一次 POST 批量拿到他人的 argon2 passwordHash、email、createdBy——离线爆破 + 邮箱枚举一步到位。因为端点零测试，… | integration：在 users-http.test.ts 新增 `describe('POST /api/users/lookup 公开投影')`：用普通用户 token 批量查两个含 password 与 email 的用户，断言 ① 每个元素的 `Object.keys(...).sort()` 恰为 `['displayN… |
| `B5-security-4` | P1 | cross-module-seam | tests/contracts/registry.ts 是全仓唯一会因新路由落地而 CI 变红的结构化清单，但 EndpointSpec 里只有 `public`（是否跳过 multiAuth）这一个安全相关字段，api-contract.test.ts 也只据此生成「匿名 → 401」用例。没有任何字段/断言表达「这个端点需要哪一级授权」，因此一个已鉴权但无权的 actor 打过来会怎样，结构上无人过问。admin-only-g… | 下一个像 worktree-files 那样「挂在 /api/* 下、自己实现资源加载、忘了写授权门」的路由会顺利通过 typecheck + 全部 765 个后端用例 + CI，直到有人再做一次人工权限审计。跨用户越权在这个仓库的历史里已经这样发生过一次，成本是 P0。 | ci-gate：给 EndpointSpec 增加必填的 `authz` 判别式字段（例：`{kind:'permission', perm:'skills:write'} / {kind:'resource-acl', type:'skill'} / {kind:'task-member'} / {kind:'admin'} … |
| `B5-security-5` | P1 | no-test-at-all | mcps.ts / plugins.ts 各自实现了一份 loadVisibleMcp / loadVisibleMcpById / loadVisiblePlugin / loadFreshOwned（不可见 → 404，非 owner → 403），覆盖详情、探针、重命名、升级、删除。但 mcps-http.test.ts 与 plugins-http.test.ts 都用单一 daemon token（→ __system_… | 他人私有 MCP 的 config（command 数组、env 变量名、remote URL、headers）与私有 plugin 的 spec/cachedPath 一旦因门失效而可见，等于把内网服务地址与凭据形状发给所有登录用户；写路径失效则可被任意用户改名/删除/触发升级重装。当前无任何用例能发现这类回归。 | integration：复用 B5-security-1 提议的四角色 harness，新增 `mcps-acl.test.ts` / `plugins-acl.test.ts`：私有后 stranger 的 `GET /api/mcps`、`/api/mcps/probes`、`GET /api/mcps/:name`、`GET/PO… |
| `B5-security-7` | P2 | cross-module-seam | 所有 /api/tasks/:id/... 的成员制可见性是靠 routes/tasks.ts 里注册的两条 app.use 中间件统一兜的（handler 自身不再重复判定）。Hono 按注册顺序组合匹配到的 handler，先注册的 handler 直接返回后中间件永不执行——我在 scratchpad 里用真实 Hono 复现过：同一路径下「注册在中间件之前的 handler」返回 200 LEAKED，「注册在之后的」返回… | 任何新增的 /api/tasks/:id/xxx 路由如果落在 server.ts 第 210 行之前挂载的模块里（workgroupTasks 是现成候选），就会完全跳过成员制可见性门——任意登录用户凭 taskId 即可读他人私有任务的节点输出 / worktree 文件树 / 文件内容。这正是 worktr… | ci-gate：加一条源码级 + 行为级双重守卫：① 源码级——扫描 packages/backend/src/server.ts 的 mountXxx 调用顺序与 packages/backend/src/routes/*.ts 里所有 `/api/tasks` 开头的路由字面量，断言每个声明 /api/tasks 路由的模块… |
| `B5-security-8` | P2 | no-test-at-all | readSkillFile 明确做了两层：safeJoin 做词法遏制，再用 realpathInside 解析真实路径并校验仍在 root 下——源码注释还写清了攻击（skill 目录里放 `secret -> ~/.ssh/id_rsa`）。但 writeSkillFile 与 deleteSkillFile 在 staging 目录里只做了 safeJoin，随后直接 writeFileSync / rmSync，都会跟随符… | 读写两条路径对同一攻击面的处理不对称，且不对称没有被任何测试表达。一旦未来引入任何能在 skill files/ 目录里落 symlink 的入口（zip 导入支持 symlink entry、从外部目录 import、运维手工放置、或 agent 在 managed 技能目录里写文件），攻击者即可用 `PUT … | unit：① 在 skill-file-symlink-containment.test.ts 增补写删两组用例：在 skill files/ 下建 `escape -> <outsideDir>`，断言 `writeSkillFile(name,'escape/pwned.txt',...)` 抛 ValidationE… |

### 后端｜数据层（schema / 迁移 / 事务 / 备份 / 记忆子系统）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B6-data-2` | P1 | harness-cannot-express | drizzle 的 `readMigrationFiles` **只**按 `--> statement-breakpoint` 切分（node_modules/drizzle-orm/migrator.cjs:47-49），每个 chunk 走 `session.run(sql.raw(stmt))` → bun:sqlite `prepare(...).run()`，我实测确认多语句字符串**只执行第一条且不报错**（`db.… | 新增一个手写多语句迁移时忘了写 breakpoint → 只有第一条 SQL 生效，其余（典型是 backfill UPDATE、二次 ALTER、索引创建）**静默丢失**。migrate 不报错、drizzle-kit check 不报错、CI 全绿，但所有用户的库进入「列加了但没回填 / 索引没建」的半应用… | ci-gate：加一条全仓机械守卫 `migrations-statement-breakpoint.test.ts`：readdirSync 遍历 `packages/backend/db/migrations/*.sql`，对每个文件按 `--> statement-breakpoint` 切块，剥掉 `--` 注释后统计每… |
| `B6-data-4` | P1 | concurrency-timing | `startMemoryDistillLoop` 的 setInterval（默认 1000ms）**没有**同侪 ticker 都有的 `let running = false` 重入守卫（对比 `eventsArchive.ts:213`、`gc.ts:428` 都有）。而 `distillTick` 一次 SELECT 最多 `DISTILL_BATCH_LIMIT = 5` 条 pending，然后在 `for (cons… | 同一个蒸馏 job 被并发跑两遍 → 同一批会话生成**重复的记忆候选**（用户在 /memory 页面看到成对的同题候选），并发的 done/failed 写回互相覆盖导致 attempts/lastError 记错，外加双倍 LLM token 花费。当队列里同时有 ≥2 个不同 debounceKey 的 … | integration：测试层：新增 `memory-distill-loop-reentrancy.test.ts`，用一个可控 spawnFn（返回一个由测试手动 resolve 的 promise）种 3 个不同 debounceKey 的 pending job，不等第一个 `distillTick` 完成就发起第二个 `dis… |
| `B6-data-5` | P1 | happy-path-only | `repoCredentials.ts:8` 明确写着整套 at-rest 封存的安全前提是「备份 tarball 装 db.sqlite/config.json/skills/workflows 但**不装 secret.key**，所以封存过的库进备份是安全的」。但这条前提在测试里没有任何断言：`backup.test.ts:102` 的排除用例只检查了 worktrees / runs/ / logs/ / token 四项… | 备份包同时含 `secret.key` 和封存后的 db.sqlite 时，RFC-204 的全部至静密加密等于零——任何拿到备份文件的人可直接解出私有仓库的 access token / OIDC client_secret。用户感知不到（备份照常生成、体积略大），泄露在文件被上传到网盘或交给他人排障时才发生。 | integration：在 `backup.test.ts` 的排除用例里种一个真实的 `${appHome}/secret.key`（以及 `secret.key` 同级的任何未来密钥文件），断言 tar 成员里不含它；再加一条正向断言：种一条带 token 的 cached_repos 行 + 跑封存 + 生成备份，解包后用 `gr… |
| `B6-data-6` | P1 | cross-module-seam | `routes/memories.ts` 有四处 scope ACL 调用：写门 `canManageMemory`（:59/:146）、详情可见性 `canViewMemory`（:56/:122）、列表过滤 `filterMemoriesByScopeVisibility`（:109/:113）。这三个函数名在 `packages/backend/tests/` 全量 grep 下**零命中**，只能靠 REST 用例间接覆盖… | 两个方向都会坏且都无人报警：(a) 收紧方向——`canManageMemory` 一旦被改回 admin-only，普通用户就再也无法编辑自己 agent 的记忆，产品承诺的「资源 owner 管自己的记忆」静默失效；(b) 放开方向——`canViewMemory` / `filterMemoriesBySc… | integration：在 `routes-memories.test.ts` 新增一个 `RFC-099 D12 scope ACL（REST 面）` describe：种 alice 拥有的 private agent + bob 拥有的 public agent，各挂一条 agent-scope 记忆，断言 (1) alice（非… |
| `B6-data-10` | P2 | no-test-at-all | 数据层的四个 `start*` ticker——`startEventsArchiver`、`startWorktreeGc`、`startPluginGenerationGc`、`startMemoryDistillLoop`——在 `packages/backend/tests/` 与 `packages/frontend/src/` 全量 grep 下**全部零引用**（我用脚本对这几个 service 的所有导出符号跑了一… | 重构时把某个 ticker 的重入守卫、`unref`、或 `.catch()` 顺手删掉，测试全绿：结果要么归档/GC 任务重叠执行（重复归档、GC 与正在建的 worktree 打架），要么一次抛错让整个 interval 静默死掉——之后事件表和 worktree 再也不被清理，用户只在磁盘告警时才发现，而… | unit：补一个共用的 `background-tickers.test.ts`，用假时钟（或极小 intervalMs + 手工推进）对四个 ticker 各断言三条契约：(1) 一次 tick 未完成时下一次 interval 触发**不会**重入（喂一个手动 resolve 的慢 run 函数，断言 run 只被调一… |
| `B6-data-7` | P2 | happy-path-only | `archiveOldestForNode` 先 `appendFileSync` 把选中的行写进 `${logsDir}/{taskId}/{nodeRunId}.jsonl`，再 `db.delete(nodeRunEvents)` 删库里的行，两步之间没有事务也没有幂等标记。进程在这两步之间被杀（daemon 重启是常规操作，30s 优雅关闭窗口内 archiveEvents 可能正跑到一半），下一轮归档会把**同一批 id… | 任务日志/stdout 在 UI 上出现整段重复内容（同一批事件被展示两遍），下载的完整 stdout 也带重复；由于 JSONL 是 append-only 且没有去重，重复一旦写进去就永久存在，且每次崩溃叠加一层。 | unit：补 `events-archive-idempotency.test.ts`：(1) 归档一批后，手工把这批行**重新插回** node_run_events（模拟 delete 未生效的崩溃后状态）再跑一次 archiveEvents，断言 JSONL 里每个 id 只出现一次、`readArchivedEve… |
| `B6-data-8` | P2 | no-test-at-all | RFC-193 把每个 path 端口的文件字节不可变副本落在 `${appHome}/runs/{taskId}/ports/{nodeRunId}/…`（`portArchiveRootRel`），设计文档 D-表明确写「归档随 `runs/{taskId}` 目录跟随现行清理策略（本 RFC 不新增清理义务）」。但该清理策略不存在：`runner.ts:1649` 只 `rmSync` 了兄弟目录 `runs/{taskId… | 长期运行的实例上 `~/.agent-workflow/runs/` 无界增长——每个任务的每个 path 端口都留一份最大 2 MiB 的字节副本，外加每个任务一个永不删除的目录。用户表现为磁盘被慢慢吃满、`~/.agent-workflow` 体积远超预期，而 worktree GC 页面显示「已清理」给出误… | integration：先补一条会红的守卫用例把义务显性化：`rfc193-port-archive-retention.test.ts` 造两个 terminal 任务（一个超过 `worktreeAutoGc.olderThanDays`、一个未超），在其 `runs/{taskId}/ports/` 下放归档文件，跑一轮 `sta… |
| `B6-data-9` | P2 | harness-cannot-express | CLAUDE.md 把「迁移 SQL 文件 append-only / 不可变」定为政策（`upgrade-rolling.test.ts:14-18` 的注释还把这条当作测试成立的前提），但没有任何机制执行它。drizzle 的 `readMigrationFiles` 会算每个文件的 sha256 存进 `__drizzle_migrations.hash`，可 `SQLiteDialect.migrate` 只比较 `fol… | 有人为了「修个笔误」改了一条早已发布的迁移 SQL：新装用户拿到修正后的 schema，老用户因为该迁移已记账永不重放、停在旧 schema 上。两拨用户的库结构从此不同，之后任何一条依赖该列/索引的查询在老用户那边直接报 `no such column`，而全套本地测试（都从零重放迁移）恒绿，只有真实升级用户会炸。 | ci-gate：加一个 `migrations-immutability.test.ts`：把每个 `db/migrations/*.sql` 的 sha256 连同 tag 存成一张提交进仓库的清单（如 `db/migrations/meta/_checksums.json`），测试断言当前文件的哈希与清单逐条相等；新增迁移时… |

### 后端｜WebSocket / daemon 运维 / CLI / 定时任务

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `B7-daemon-ws-1` | P1 | cross-module-seam | task 通道被显式设计为「upgrade 时 canViewTask 一次，之后每帧不再过滤」（registry.ts:353-357 无 frameGate）。一旦连接建立，后续无论任务协作者被 updateTaskMembers 移除、用户被 disable、还是 session token 被撤销，服务端都没有任何机制关闭或降级这条连接——全仓不存在按 user/session 关闭在线 socket 的代码（grep p… | 把某人从任务协作者里移除（或禁用其账号 / 撤销其 session）之后，只要对方浏览器标签页没关，他仍然实时看到该任务后续的全部 agent 输出、代码 diff、反问与评审内容，直到自己主动刷新页面为止；UI 上没有任何提示，管理员也无法从服务端切断。 | integration：在 `packages/backend/tests/` 新增 `ws-task-channel-revocation.test.ts`：(1) collaborator 用 session token 建立 `/ws/tasks/{id}` 连接并收到 hello；(2) 调 `updateTaskMembers… |
| `B7-daemon-ws-2` | P1 | no-test-at-all | `startScheduledTaskLoop` 在全仓只被 `cli/start.ts:562` 调用，测试目录里一次都没出现。它承载四项 RFC-159 设计承诺，全部未被防护：① `cfg.scheduledTasksEnabled === false` 的「每 tick 现读」实时总开关（scheduledTaskScheduler.ts:228）；② `SCHEDULE_MAX_IN_FLIGHT - inFlight`… | 管理员在设置里关掉「定时任务总开关」后，定时任务仍然照常触发（或反之，开关误判导致所有定时任务静默不再触发，而 UI 上 next_run_at 仍在推进，看起来一切正常）；积压场景下背压上限失效会让一次 tick 同时甩出上百个 launch 把 daemon 打满；daemon 关闭时 ticker 没停会在… | integration：新增 `packages/backend/tests/scheduled-task-loop.test.ts`，用 `intervalMs: 10` + 可变的 `loadConfig()` 桩驱动真实 ticker：① `scheduledTasksEnabled:false` 时等待若干 tick 断言 `b… |
| `B7-daemon-ws-3` | P1 | text-assertion-only | 启动阶段的 opencode 探针是本仓唯一一条「重启也救不回来」的失败路径（锁已拿到、端口永不监听，再次 start 只会说 another daemon is already running），RFC-208 为此给 `ocDriver.probe` 加了 `BOOT_PROBE_TIMEOUT_MS` 并要求超时后 `lock.release() + process.exit(1)`。但 `rfc208-boot-and-e… | PATH 上的 opencode 是个会挂起的 wrapper（nvm/asdf/mise shim、公司代理脚本）时，daemon 拿着 PID 锁永远不监听端口；用户 `agent-workflow start` 只会得到「another daemon is already running」，只能手删锁文件才… | unit：在 `opencode-version.test.ts`（或新建 `rfc208-boot-probe-behavior.test.ts`）加行为用例，照抄 `opencode-models.test.ts:157` 的形状：写一个 `#!/bin/sh\nsleep 30` 的可执行桩（再加一个 `sh -c … |
| `B7-daemon-ws-7` | P2 | happy-path-only | `doctorCommand()` 有七项检查，但只有两条被测过：`cli.test.ts:88` 测真机 happy path，`cli.test.ts:100` 测 token 文件 mode≠600。纯函数半边（`evaluateMigrationsStatus` / `evaluateLifecycleHealth` / `evaluateGitCheck`）各自有专门文件覆盖得很好，但**带副作用的那一半**从没走过失败… | `doctor` 是用户装不上/起不来时唯一的自助诊断工具；它的失败分支一旦坏掉（比如提示信息里的路径/最低版本号说错，或某项该红却判绿），用户会拿着一份「all checks passed」却起不了 daemon，排障成本直接翻倍。CI 的二进制 smoke 也只跑成功路径，帮不上忙。 | unit：在 `cli.test.ts` 的 `AGENT_WORKFLOW_HOME=tmp` 夹具里补四条：① 写一个 `config.json` 内容为 `{`（坏 JSON），断言 `checks.find(c=>c.name==='config').ok === false` 且整体 `ok=false`、`fo… |
| `B7-daemon-ws-8` | P2 | happy-path-only | `stopCommand` 四种返回态里 `not-running`、`stale-lock-removed`、`stopped` 三种有用例（cli.test.ts:145,160,165），唯独 `timeout`（stop.ts:59-63）没有——而这正是「daemon 卡死」时唯一会走到的分支，且实现上**只是报告超时**：既不升级到 SIGKILL，也不清理锁文件，用户只能手删 `.daemon.lock`。同样地 `… | daemon 因为 `gracefulShutdown` 卡住（DB 被锁、子进程不退）而无法退出时，`agent-workflow stop` 等 30s 后返回一句超时就结束，锁文件仍在，之后每次 `start` 都被拒；用户在文档里得不到「怎么办」的指引，只能自己 kill -9 加删锁。status 侧同… | unit：① `stop` timeout：起一个屏蔽 SIGTERM 的假 daemon（写自身 pid 到 `.daemon.lock` 的 `sh -c 'trap "" TERM; sleep 30'`），`stopCommand({ timeoutMs: 300 })` 断言 `status==='timeout… |

### 前端｜路由与页面级（38 个 route 文件）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F1-routes-1` | P1 | text-assertion-only | 记忆→技能融合的审批闸门页（批准=落盘新技能版本、驳回=带反馈重跑、取消=中止引擎任务）没有任何渲染级测试。具体未防护分支：①`isAwaiting` 才渲染 DiffViewer + 批准/驳回按钮；②驳回对话框提交按钮在 `feedback.trim() === ''` 时必须 disabled；③`refetchInterval` 只在 running/applying 时开 2s 轮询、其余状态必须停；④取消按钮在 don… | 驳回反馈的非空校验若被去掉，用户可以提交空反馈让融合 agent 拿到空指令空转一轮；批准按钮的 isAwaiting 门若失效，用户能在 running 状态点批准把半成品技能版本写进技能库；approve 后漏 invalidate 会让 /skills 页面继续显示旧版本，用户以为批准没生效而重复点击。这些… | integration：新建 fusion-detail-route.test.tsx，按现有 memory-fusion-tab.test.tsx 的 memory-router 套路挂真组件：(a) status='awaiting_approval' → 断言 DiffViewer 出现、驳回对话框里提交按钮初始 disabled… |
| `F1-routes-3` | P1 | text-assertion-only | 删除按钮的 onClick 是一个三元：`item.referencingTaskCount > 0 ? setPendingDelete(item) : remove.mutate({ id })`——有任务引用时弹确认对话框、无引用时直接删；确认对话框的确认键才带 `force: true`（→ `?force=1`）。这个「确认闸门是否生效」的判定没有任何渲染级断言：repos-page.test.tsx 全文只做 `toC… | 若三元被写反或 force 被提到第一次点击上，用户点一下「删除」就会连带清掉仍被 N 个任务引用的缓存仓库（那些任务的 worktree 基线随之失效），而全部测试仍绿——这是一个不可撤销的破坏性操作丢掉了它唯一的确认闸门。反向写错则是有引用时永远删不掉、且不弹窗，用户只看到按钮无反应。 | integration：给 repos-page.test.tsx 补渲染用例（页面无路由依赖，直接 render ReposPage 即可）：列表返回两行，A 行 referencingTaskCount=0、B 行 =3。断言 ①点 A 行删除 → 立刻发出 `DELETE /api/cached-repos/A`（无 `?forc… |
| `F1-routes-4` | P1 | text-assertion-only | `/users` 页的行内角色切换：`<Select>` onChange → `updateRole.mutate({id, role})` → `PATCH /api/users/:id {role}`；两条护栏是 ①当前用户自己那行必须渲染静态 role-chip 而不是 Select（否则管理员能把自己降级、失去撤销权限）②后端 last-admin-protection 返回的错误必须落到该行的 `users-row__… | `isSystem // isSelf` 这个分支条件一旦被改动（例如重构时误写成只判 isSystem），当前登录的管理员就能在自己这一行把 admin 改成 user，刷新后彻底失去 /users 与所有 admin 面板的访问权，且系统里可能一个 admin 都不剩——需要改数据库才能恢复；而三条正则断言的… | integration：在 users-page-actions.test.tsx 里直接复用现有 ROWS/ME fixture 加三条用例：①断言 self 行（me-admin）内 `getByRole('button'/'combobox')` 取不到角色 Select、能取到 `.role-chip`，而 alice 行能取到… |
| `F1-routes-6` | P2 | text-assertion-only | `/agents/by-id/$id` 与 `/workgroups/by-id/$id` 是任务详情里主体链接的落点，负责把冻结的资源 id 解析成当前名字再 replace 跳转。两条路由各有三个互斥分支：404（不存在或无权限，按 RFC-099 要与不存在同形）→ EmptyState `common.resourceUnavailable`；其它错误 → ErrorBanner + Retry；否则 LoadingSta… | 若 `error.status === 404` 这个前置判定被删或写成 `>= 400`，一次后端 500/网络抖动会被当成「资源不可用」显示成终态空态，用户失去 Retry 入口、误以为对方把代理/工作组删了；反向错配则会把「无权访问」的 404 渲染成带原始错误消息的 ErrorBanner，泄漏资源确实存… | integration：把 task-subject-by-id-resolver.test.tsx 的源码 describe 换成渲染 describe：用 memory router 挂真 `Route`（父路由给 `/agents/by-id/$id`，另注册一个 `/agents/$name` stub 记录落点），三条用例——… |

### 前端｜工作流编辑器画布（xyflow）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F2-canvas-1` | P0 | text-assertion-only | 用户在画布上从输出端口拖到目标节点（精确 handle 落点 / 节点 body 落点 / 反向拖 / 点击连线）这一主路径，其编排代码 `handleConnect`(WorkflowCanvas.tsx:851-949)、`handleConnectEnd`(:968-1015)、`isValidConnection`(:1116-1183) 从未被任何测试执行过一行。测试只覆盖了它们调用的下游纯函数（planWorkflow… | 任何一次对连线编排的重构（改分支顺序、把守卫抽成 helper、条件取反、提前 return）都可能让画布上「所有连线操作静默无效」或「反向拖生成非法边」而全套测试保持绿灯。这不是假想：fanout-source-sync-no-op.test.ts 文件头记录 RFC-060 PR-E 的存根误返回 false… | unit：把 handleConnect / handleConnectEnd 的决策部分抽成纯函数（如 `planConnectGesture(definition, gesture, semanticContext)`，gesture 包含 {conn, connState 摘要, pointer, nodeBoxes… |
| `F2-canvas-2` | P1 | cross-module-seam | 两个函数各自独立地做 clarify → cross-clarify → generic 的分类并构造 ConnectionRequest。关键差异：`handleConnect`(:895-925) 在 `connectPointer.current !== null` 时会调用 `resolveDropTarget` 把 mode 从 'new' 翻成 'reuse' 并改写 portName；`isValidConnecti… | 预览显示绿色（合法）但松手后什么都没发生，或者预览红色（非法）却仍然连上了——用户完全无法预测拖拽结果。已有的 canvas-clarify-wiring.test.ts:41 只断言 `classifyClarifyConnection` 在源码里出现 ≥3 次，任何一条链单独漂移（比如只给 handleCon… | unit：抽出单一 `buildConnectionRequest(definition, conn, opts?: {pointer, nodeBoxes})` 供两处共用，opts 缺省即预览模式。写属性式测试：对一组覆盖 clarify / cross-clarify / review / output / fano… |
| `F2-canvas-3` | P1 | no-test-at-all | `wrapSelection`(WorkflowCanvas.tsx:1443-1491) 直接把当前选中的 node id 数组塞进新 wrapper 的 `nodeIds`，不检查这些节点是否已经属于别的 wrapper、是否包含 input/output 节点、是否同时选中了某个 wrapper 及其子节点。菜单项(:1967-1980)只在「选择为空」时 disabled。产出的 wrapper 没有 `size` 字段，… | 用户框选一批已经在某个 git wrapper 里的节点再点「Wrap in Loop」，工作流被保存成一个节点同属两个 wrapper 的图；`innerToWrapper` 后写覆盖先写，调度时该节点归属哪个 scope 取决于 nodes 数组顺序，表现为「循环里的节点被跑了一次就不再迭代」或 git 快照… | unit：两层：(1) 抽 `planWrapSelection(definition, nodeIds, kind)` 纯函数，测试断言正常路径产出的 wrapper 形状（kind / nodeIds / position=min-30 / loop 默认字段），以及非法路径拒绝并返回原 definition —— 选… |
| `F2-canvas-8` | P2 | no-test-at-all | `canvasNotice` state(:337) 由五处写入：copySelection 的 ClipboardInvariantError catch(:1201)、pasteFromClipboard 的 catch(:1224)、duplicateNode 的 catch(:1433)、handleAutoLayout 的三类 warning 映射（cross-scope / cycle-back-edge / size… | 自动布局遇到跨 scope 选择 / 环 / sizeLocked 溢出时静默不动作且不给任何解释，用户以为按钮坏了；剪贴板 fail-closed（buildSlice 抛 ClipboardInvariantError）被 catch 后若文案链路断掉，用户按 Cmd+C 毫无反馈也不知道为什么粘不出来。i1… | unit：抽 `describeLayoutWarnings(warnings, t): string / null`，unit 断言三类 warning code 各自映射到对应 key 与计数参数、多类共存时按顺序拼接、空数组返回 null。再加一条 happy-dom 集成：mount 真实 WorkflowCanv… |

### 前端｜评审 / 多文档 / 结构化 diff / prose

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F3-review-1` | P0 | cross-module-seam | `lib/review/draftStore.ts` 的 getDraft / setDraft / deleteDraft / listDrafts / clearAllReviewDrafts 五个导出没有任何测试 import 过（对照组 `lib/clarify/draftStore.ts` 有完整的 `clarify-draft-store.test.ts`）。更关键的是两者打开**同一个** IDB（DB_NAME =… | 用户只要在这个浏览器里打开过任意一次「反问（clarify）」页面，IDB 就被升到 v2；此后所有评审页 `indexedDB.open('agent-workflow-drafts', 1)` 触发 VersionError → openDb 返回 null。表现为：(1) 选中正文写了一半评论、刷新/切文档… | unit：新建 `tests/review-draft-store.test.ts`：(1) 照抄 clarify-draft-store 的结构补 key 组装 / set-get 往返 / delete 幂等 / listDrafts 前缀过滤 / clearAll 五条；(2) 关键新增一条**共存**用例——先 `… |
| `F3-review-2` | P1 | no-test-at-all | 后端 `POST /api/reviews/:id/decision` 在恢复任务失败时会在 200 响应里带 `resume: { ok:false, code, message }`（RFC-202 T8「伪成功」修复）。单文档路由 `reviews.detail.tsx` 读取并渲染 NoticeBanner 告警；`MultiDocReviewView.tsx` 的 submitDecision 直接 `await api… | 多文档评审 approve 之后，若后端 resumeTask 抛错（worktree 被 GC、spawn 失败等），评审员看到的是一次干净成功 + 被弹回任务详情页，任务实际停在 parked 状态无人知晓，只有 daemon 日志里有一行 warn。单文档那条已实现的告警也随时可能在重构中被删掉而无人发现。 | integration：两条用例：(1) `reviews.detail` render + mock `api.post` 返回 `{ ok:true, resume:{ ok:false, code:'worktree-missing', message:'…' } }`，点 approve → 断言留在当前页且出现 `.revie… |
| `F3-review-3` | P1 | text-assertion-only | `ReviewDocPane` 的四个写入口——popover 提交新评论（POST /comments）、气泡行内编辑保存（PATCH）、删除（DELETE）、复制到剪贴板——从未被任何测试真实点击过。相关「测试」全部是 readFileSync + 正则匹配源码：reviews-detail-layout.test.ts 有 22 处 readFileSync 断言 `<ErrorBanner error={submitCom… | 任何一次重构把 mutation 的 URL、HTTP 方法、payload 字段名或 onSuccess 失效逻辑改坏，全部 616 个前端测试仍然全绿（源码文本断言只要变量名还在就过），而线上表现是「写完评论点提交，气泡不出现」或「编辑保存后旧内容回来」这类核心评审功能直接不可用。 | integration：新建 `tests/review-doc-pane-comment-crud.test.tsx`：直接 render `<ReviewDocPane mode="awaiting" …>`（mock @/api/client），(1) 伪造 window.getSelection 后触发 mouseUp → 填 … |
| `F3-review-4` | P1 | contract-drift | 活跃路径 `rehypeWrapAnchors` 的 collectTextSegments 无条件收集所有 text 节点，没有 pre/code 跳过逻辑；而它取代掉的（现已死代码）`wrapAnchorsInDom` 里有 `SKIP_TAGS = new Set(['PRE','CODE','SCRIPT','STYLE'])`，并且唯一一条 pre/code 跳过的测试还挂在那个死实现上。rehypeWrapAnchor… | 评审员在渲染出来的代码块里选中一段代码写评论：评论能提交、气泡也在侧栏列出，但正文里没有任何高亮，点气泡不跳转（scrollToCommentAnchor 的 querySelector 找不到 mark），scroll-spy 也永远不会选中它。评审员会以为评论没保存而重复提交。同时代码块文本参与了全局 off… | unit：在 `tests/prose-anchors-prop.test.tsx` 补三条：(1) body 含 ```ts fence，anchor.selectedText 只出现在 fence 内 → 断言渲染结果里存在 `mark.comment-anchor[data-comment-id]`（或明确断言「按设… |
| `F3-review-10` | P2 | happy-path-only | `call-chain-entry.test.tsx` 只断言点 ⎇ 会切到第 5 个 tab 并挂上 CallChainView、多仓时入口隐藏。组件内真正的行为——点 ▸ 展开下一级触发新的 `/call-targets` 请求、ancestors 集合逐级累积（环检测的实际输入）、cycle / too-deep / external / unresolved 四种 tag 的渲染、tree↔sequence 分段控件切换后… | 纯函数全绿但接线错了的情况完全不设防：例如 ancestors 忘记累积（`new Set([...ancestors, target.ref])` 写成 `new Set([target.ref])`）会让环检测失效、深层调用链无限展开并对同一个 ref 反复发请求；或 sequence 模式 URL 拼错导致… | integration：新建 `tests/call-chain-view-interaction.test.tsx`：mock api.get 返回分层的 call-targets，(1) 点根节点某个 resolved 子项的 ▸，断言发出第二次 `/call-targets?methodRef=<child ref>` 且子层渲染… |
| `F3-review-5` | P2 | happy-path-only | `ReviewDocPane` 提交评论时把 `docVersionId` 拼进 POST body（`{ ...input, docVersionId }`），这是 RFC-079 多文档场景下「评论归属哪一份文档」的唯一决定点。测试里唯一相关的是 `reviews-multidoc-viewport-fit.test.ts` 的一条源码正则 `docVersionId={activeDocId}`（只证明 prop 传了，不证… | 切到第 3 份文档写的评论如果 body 里带的是第 1 份的 docVersionId（例如未来把 activeDocId 换成 firstDocId、或 memo 依赖写漏），评论会静默挂到错误文档上——评审员在当前文档看不到自己刚写的评论，而另一份文档凭空多出无关评论，且这种错位不会有任何报错。 | integration：在 F3-review-3 的新测试里加一条多文档场景：render `MultiDocReviewView`，先点导航切到第二份文档，再走完 selection→popover→提交，断言 `api.post` 的第二个参数 `docVersionId === 'd1'`（而不是 'd0'）。 |
| `F3-review-6` | P2 | text-assertion-only | pane 内的 keydown handler 只挡了 popover / editingId / INPUT/TEXTAREA/SELECT 焦点，**没有检查 metaKey / ctrlKey / altKey**；而同一页面的多文档快捷键 `multiDocHotkeyAction` 明确「bails on any modifier」并有专门用例。J/K 跳转本身（含 activeCommentIdRef 连点修复、scr… | 用户按 Cmd+J（浏览器下载面板）/ Ctrl+K（很多环境的搜索聚焦）时，评审文档会跟着莫名其妙地跳到下/上一条评论并强制 scrollIntoView，正在读的位置丢失。另外连点 ▲/▼ 的 ref 修复（曾经的「到不了第一条/最后一条」bug）一旦被 refactor 回退成读 state，也没有任何红灯。 | unit：新建行为用例：render ReviewDocPane（3 条评论），(1) fireEvent.keyDown(window, {key:'j'}) ×2 断言 activeCommentId 依次前进（可用 `mark[data-active]` 的 data-comment-id 断言），(2) 连续同步触… |
| `F3-review-9` | P2 | happy-path-only | `StructureTreeToolbar.toggleSev` 有一条刻意的防呆：当用户把三个严重度全部取消勾选时不允许「什么都不显示」，而是重置回全选（`setSevFilter(next.size === 0 ? new Set(ALL_SEVERITIES) : next)`）。`structure-view.test.tsx` 测了默认风险排序、breaking 卡片点击后收窄到 breaking-only，但完全没有触… | 防呆一旦回退，用户把三个勾都取消后结构树整片空白、只剩一句「本文件无符号改动」，看起来像结构化 diff 坏了；而且此时没有任何「清空筛选」的显式入口，用户只能刷新页面。 | unit：在 `structure-view.test.tsx` 的 RFC-088 describe 下加两条：(1) 逐个 uncheck 三个 severity checkbox，断言最后一次 uncheck 之后三个 checkbox 全部回到 checked 且树里的行数恢复；(2) uncheck 掉 safe… |

### 前端｜反问 clarify / 协作 / workgroup

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F4-clarify-1` | P0 | no-test-at-all | clarify.detail.tsx 里 RFC-099 D14 的三段核心并发逻辑完全没有用例：①自动保存只对「与上次服务端 ack 值不同」的题发 PUT（逐题 LWW 的写侧）；②远端草稿合并时「本地用户已改动过该题 ⇒ 不采纳远端值」的本地优先规则；③PUT 失败（403 非成员 / 409 轮已封存）后 serverDraftDisabledRef 熔断、退化为纯 IDB。后端 rfc099-membership-att… | 两人同时编辑同一轮反问时：A 正在输入的自定义文本会被 B 的一次保存整段覆盖（本地优先规则一旦被重构破坏，UI 上表现为「打到一半字被吞」）；或反过来，熔断逻辑失效后前端对着已封存的轮次每 500ms 反复 PUT 打 409。两种都只会在生产被用户实报，本地与 CI 全绿。 | unit：新建 clarify-collab-draft.test.tsx：(a) 给 answersEqual 补真值表单测（乱序 indices 相等、customText 差一字符不等、labels 不同仍相等）；(b) 渲染 ClarifyDetailPage，改一题 → 快进 500ms → 断言只对**该题**… |
| `F4-clarify-2` | P0 | cross-module-seam | RoundAnswerBlock 用 seeded 一次性布种，之后即使 ['clarify','detail',originNodeRunId] 重新拉到新的 draftAnswers 也**永不再合并**；同时集中面板挂载在任务详情页（TaskQuestionList），那里只有 useTaskSync，而 useTaskSync 的规则表里**没有** clarify.draft.updated（只有专用页的 useClar… | 两名成员同时用集中面板回答同一个待指派问题：谁后按提交谁的答案落库，先写的人全程看不到对方的草稿、也没有任何「X 刚刚编辑」提示，提交后才发现答案不是自己写的。而同一对用户换到 /clarify 详情页就能看到彼此——同一功能两条通道行为相反且无人守卫。 | integration：在 centralized-answer-pane.test.tsx 增一组：(a) 面板挂载后让 round detail query 返回更新的 draftAnswers → 断言当前**期望行为**（若产品决定面板也协作，则断言表单值跟进；若有意不跟进，则写成显式「一次性布种」锁并在注释里写明取舍）；(b)… |
| `F4-clarify-3` | P1 | no-test-at-all | WorkgroupRoom 侧栏渲染 data.clarifyStops 的 chip 列表 + 每条一个恢复按钮，点击走 POST /api/tasks/:id/nodes/:nodeId/clarify-directive {directive:'continue', shardKey: askerKey}。RFC-207 design.md 明确要求「恢复入口：房间 clarifyStops 覆盖 leader / asg:… | 工作组里喊停某个 asker 的反问后，恢复按钮是**唯一**的解除入口（普通任务靠画布 toggle，工作组没有画布）。一旦 shardKey 传参、askerKey 归一化（mem:/asg:/leader）或后端路由拼接被改坏，喊停就变成不可逆——该成员/派单再也不能反问，且界面上按钮照常显示、点了没反应。 | integration：新建 workgroup-human-derived-clarify.test.tsx：room 响应带 clarifyStops:[{nodeId:'n1',askerKey:'leader'},{...'asg:A'},{...'mem:M1'}] → 断言渲染三个 chip；点其中一个的恢复按钮 → 断言 … |
| `F4-clarify-4` | P1 | no-test-at-all | clarify.detail.tsx 渲染三处 RFC-099 归属 UI：①每题下方 answerAttributions[q.id] → AttributionChip「最后编辑人」；②已封存轮的 answeredBy → AttributionChip「提交人」；③收到 clarify.draft.updated 时的浮动提示（含「若 editor.userId === 我 则不提示」的自我回声抑制 + 5 秒自动清除 + … | 自我回声抑制一旦失效，用户自己每敲 500ms 就会被自己的编辑弹一次「你 刚刚编辑了…」；归属 chip 接线断掉则审计线索静默消失（谁改的、谁提交的全变空白），而这正是 RFC-099 承诺的「成员制协作可追溯」的唯一 UI 出口。全部只会在人肉点页面时被发现。 | integration：在新的 clarify-collab-draft.test.tsx 里加：(a) session 带 answerAttributions:{q1:{userId,role}} + answeredBy → 断言 clarify-attribution-q1 与 clarify-submitter 各渲染一次且含… |
| `F4-clarify-5` | P1 | no-test-at-all | clarify.detail.tsx 对 canceled/abandoned 轮渲染原因 banner，按后端记录的**转移时刻**原因 sealedCause 三分支取文案：以 'task-' 开头 ⇒ 任务终态封存；'wg-clarify-disabled' ⇒ 工作组已无人工成员而撤销；否则 ⇒ 通用「本轮已封存」。这段带 Codex 实现门批注（「绝不能改用任务当前可变状态判断，否则被取消后重试的任务会把历史轮误标成自动… | 用户打开一条已封存的反问，只会看到一个不能填的空表单加一句含糊「本轮已封存」，不知道是任务被取消了还是工作组把人工成员删光了；更糟的是若有人「顺手」把判据改回读任务当前状态，被取消后重试的任务会把历史轮反标成「无人工成员撤销」，等于把审计记录改错。 | integration：在 clarify-detail-route.test.tsx 加一组 test.each：{sealedCause:'task-canceled'/'task-failed'} ⇒ 断言 clarify.roundSealedByTaskTerminal 文案；{sealedCause:'wg-clarify-… |
| `F4-clarify-6` | P1 | happy-path-only | CentralizedAnswerDialog 的单个提交按钮对 N 个 round 并发 POST（Promise.allSettled），只要有一个 rejected 就整体抛错 ⇒ onSuccess 不触发 ⇒ task-questions / clarify list / node-runs 一律不 invalidate、弹窗保持打开。但**已成功的那些轮的问题其实已经封存了**，用户看到红条后再点一次提交，成功过的轮会… | 用户在集中面板一次答了 3 轮的问题，第 2 轮因为并发被人先答（409）失败，界面报一条错、其余两轮的成功状态完全不可见（列表没刷新，问题还在池子里）；用户重试 ⇒ 第 1/3 轮再撞 409 ⇒ 陷入「怎么点都是错」的死循环，只能刷新页面。 | integration：在 centralized-answer-pane.test.tsx 增：两轮场景，api.post 对 round B 抛 ApiError(409)、round A 成功 → 断言 (a) ErrorBanner 渲染且文案可读，(b) 弹窗仍开着，(c) 至少 ['task-questions', task… |
| `F4-clarify-7` | P1 | happy-path-only | 提交反问答案后，若后端返回 resume:{ok:false, code}，页面应停留原地并渲染 common.resumeFailedAfterSubmit 告警（而不是照常跳去任务详情装作成功）。这段状态（resumeWarning）+ 渲染分支在 tests 与 e2e 全部零命中：resumeFailedAfterSubmit / clarify-resume-failed / crossWaiting 均无引用；现有 d… | worktree 被 GC 之类导致续跑失败时，用户被静默送回任务详情页，看到任务卡在原状态却毫无提示，会以为是平台卡住而反复重试/重启 daemon —— 这正是 RFC-202 T8 要消灭的假成功。 | integration：在 clarify-detail-route.test.tsx 的 directive 提交 describe 里加一条：POST 返回 {resume:{ok:false,code:'worktree-missing'}} → 断言 (a) 不发生导航（navigate spy 未被调用），(b) clarif… |
| `F4-clarify-8` | P2 | text-assertion-only | RFC-182 P1-1（读历史时不得被强行拽回底部 + 「回到最新」浮标）与 D7（drawer 的 runs 必须按成员作用域过滤，否则跨成员串台）都是修 bug 落的代码，但守卫只有 rfc182-room-source-locks.test.ts 的三条 toContain 字符串断言。这类断言只要标识符名字还在就绿：把 atBottom 初值改成 false、把阈值 48 改成 4800、把 drawerRuns 的过滤… | 回归后用户在长房间里往上翻记录会被每秒一次的 tick 拽回底部（完全无法阅读历史），或者点某成员的回合打开 drawer 里混进了其他成员的 run 列表（串台，正是 D7 修的那个 bug）。源码锁一条不红，CI 全绿。 | unit：把 drawerRuns 的 IIFE 抽成 lib/workgroup-room.ts 的纯函数 scopeDrawerRuns(runs, runHistory, drawerRunId) 并补真值表单测（选中 run 恒含、未分类 run 回退全量、跨成员不混入）；滚动锚定用 jsdom 手动设置 scro… |

### 前端｜任务创建 / 启动向导 / 任务详情 / worktree 文件

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F5-tasks-2` | P0 | contract-drift | `repoEntryToSource` 只认 `repoUrl`，认不出就返回 `defaultRepoSource()`（空 URL 空 ref）。而 RFC-175 的 `taskToLaunchPayload` 在 RFC-204 之后**优先**输出 `{cachedRepoId, ref}`（注释明说 repoUrl 是打码的、不可用于 relaunch），后端对每个 URL 仓都写了 `cachedRepoId: so… | 任务详情点「重新启动」（RFC-175 的核心卖点：全参数预填）时，只要任务跑在远程仓（即除 scratch 外的所有正常任务），第 2 步的仓库 URL 和分支都是空的，且没有任何提示说明为什么；用户还拿不到明文 URL（列表里只有打码值），只能靠再从「最近仓库」下拉里重选一次——而重选之后又撞上 F5-tas… | unit：给 `launch-body-to-repo-sources.test.ts` 加 round-trip 用例：`bodyToRepoSources(buildLaunchBodyMultiRepo([{kind:'url',repoUrl:'',cachedRepoId:'cr1',ref:'dev'}], c… |
| `F5-tasks-1` | P1 | no-test-at-all | `buildLaunchBody`（单仓路径）的白名单只写了 `workflowId/name/repoUrl/inputs/ref/gitUser*/extras`，唯独没有 `cachedRepoId`；而 `buildLaunchBodyMultiRepo` 有。RepoSourceRow 的「最近仓库」下拉选中后把 `repoUrl` 设成 **urlRedacted**、把真实来源放进 `cachedRepoId`（RF… | 用户在向导第 2 步从「最近仓库」下拉里选一个用 query token / ssh 密码接入的私有仓（单仓，最常见路径），点启动后任务在 worktree 创建阶段失败，错误里带 `***`；同一个仓改成两行多仓反而能成功。用户完全无法从 UI 推断原因（前端只显示打码 URL，看起来是对的）。 | unit：在 `packages/frontend/tests/launch-body-field-propagation.test.ts` 加一组 cachedRepoId 断言：`buildLaunchBody({kind:'url',repoUrl:'https&#58;//***@h/o/r.git',cachedRepo… |
| `F5-tasks-3` | P1 | contract-drift | `collectAdvanced()` 的返回值以 spread 形式喂给 `buildWorkflowStartBody/buildAgentStartBody/buildWorkgroupStartBody`，而三个 builder + `stampLaunchExtras` + `stampLimits` 是白名单，未登记的键静默消失。问：类型层能兜住吗？实测不能——TS 对**通过 spread 进入对象字面量**的多余属… | 下一个给启动表单加字段（如新的 launch 选项 / ACL 字段 / 新的资源限制）的人，改完 UI + collectAdvanced 后 typecheck 全绿、现有测试全绿，但字段永远到不了后端，功能像「被忽略」一样静默失效；直到线上有人报「我开了开关但没生效」才被发现——历史上已发生两轮（autoC… | unit：加一个穷举锁：在 shared 里把 launch 可选字段定义成一份**常量数组**（如 `LAUNCH_OPTIONAL_FIELDS`），`stampLaunchExtras`/`stampLimits` 改为遍历它，测试里断言「对每个 field 造一个非空值，三个 builder 的输出都必须包含该键」… |
| `F5-tasks-4` | P1 | text-assertion-only | `buildWorkflowStartFormData` 本身有扎实单测，但「向导真的在有 upload 输入时切到 `api.postMultipart`、真的把 `uploads` state 传进去、真的把 immediateGuards 作为 extra 合进 payload、零文件也照样走 multipart 让后端跑 min/max 门」这一整条路由接线，唯一的防线是 launch-route-upload-wirin… | 只要有人把提交分支的条件写反、把 `uploads` 忘了传、或把 extra 参数漏掉（OCC 保护失效），源码里那几个字符串仍然存在于文件中 → 全部测试绿；线上表现是「选了文件但文件没被上传/工作流拿不到附件」，或并发改工作流时不再 409 而是用新快照跑旧参数。 | integration：在 tasks-new-wizard.test.tsx 加一条 W-upload 用例：workflow detail 声明一个 `kind:'upload'` 输入，向导里通过 UploadPicker 的隐藏 input `fireEvent.change` 塞一个 File，走到 confirm 点启动，断… |
| `F5-tasks-10` | P2 | no-test-at-all | 详情页三个查询用回调式 `refetchInterval` 决定何时停轮：task 用 `isTerminal(status) ? false : 3000`，nodeRuns 额外要求 `runs.length > 0` 才停，diff / structuralDiff 各有一套条件。这些条件在前端没有任何用例（全仓 tests 里 `refetchInterval` 只有 rfc108-recovery-section.tes… | 一个终态但没有任何 node_run 的任务（例如调度阶段就失败的任务）详情页会永远每 3 秒打一次 /node-runs；反过来，若有人把条件写成过早停轮，运行中的任务详情就会「卡住不动」，用户必须手动刷新才看到进展——两种回归都不会有用例变红。 | unit：把三处 refetchInterval 的判定抽成纯函数（如 `taskPollMs(status)` / `nodeRunsPollMs(taskStatus, runCount)`）放进 `lib/task-detail-tabs.ts`，在 task-detail-tabs.test.ts 里对全部任务状态… |
| `F5-tasks-11` | P2 | no-test-at-all | scheduled.$id.tsx 的删除是 ConfirmButton → `api.delete('/api/scheduled-tasks/:id')` → invalidate + `navigate({to:'/scheduled'})`。同页的 toggle / run-now / edit-config / degraded banner / history 表都有用例（scheduled-detail-style、… | 删除按钮若回归（例如确认态被跳过、URL 拼错、或成功后不跳转导致停留在 404 详情页），用户可能一键误删定时任务而无二次确认，或删完后卡在一个已不存在的页面上；没有任何用例会红。 | integration：在 scheduled-detail-style.test.tsx 补一条：点删除 → 断言出现确认态（ConfirmButton 的二段式）→ 再点确认 → 断言发出 `DELETE /api/scheduled-tasks/<id>` 且路由跳到 `/scheduled`；同时给 ConfirmButton … |
| `F5-tasks-5` | P2 | happy-path-only | `add()` 在到达 maxCount 时 `break`——多选的文件**无声消失**，没有任何错误提示；同名同大小的文件被静默跳过；`onDrop` 走同一个 add() 但完全不过滤 `accept`；`maxFileSize` 与 `accept` 在组件里只渲染成提示文案，不做任何拦截（真正的拦截在后端 upload.ts）。upload-picker.test.tsx 只测了「渲染文件行」「点删除」「maxCount… | 用户一次选 10 个文件而输入声明 maxCount=3 时，界面只出现 3 行、没有任何「已忽略 7 个」的提示，用户以为都传了；拖入一个超大或类型不符的文件时，前端毫无反应，要等整个 multipart POST 传完才收到后端的 upload-file-too-large / upload-mime-rej… | unit：给 upload-picker.test.tsx 补：(a) maxCount=2 时一次 change 传入 3 个 File，断言 onChange 只收到 2 个且渲染出「已忽略」提示（顺便把提示加上）；(b) 同名同 size 重复选取只保留一份；(c) `fireEvent.drop` 带 dataTr… |
| `F5-tasks-6` | P2 | no-test-at-all | 该组件承载任务协作者的增删、owner 转移、非管理者的只读渲染、以及「dirty 时不被后台 refetch 覆盖」的本地状态守卫，但它的四个 data-testid（task-members-dialog-button / task-members-panel / members-save / members-transfer-owner / members-transfer-confirm）在整个仓库（frontend te… | 任务成员面板一旦回归（例如 canManage=false 时误显示保存按钮、dirty 守卫失效导致用户选的人被后台刷新覆盖、转移 owner 发错 body），没有任何用例会红；而任务成员正是 RFC-099 里「谁能回答评审/反问」的权限边界，坏掉会直接表现为「加了协作者但对方仍看不到/答不了」。 | unit：新建 `tests/task-members-panel.test.tsx`：mock `/api/tasks/:id/members` 返回 `{owner, users, canManage:true}`，断言（a）改动 UserPicker 后保存按钮由 disabled 变可用、点击后 PUT body … |
| `F5-tasks-8` | P2 | no-test-at-all | 向导用分钟做 UI 单位、用毫秒上 wire：提交侧 `Math.round(maxDurationMin * 60_000)`，种子侧 `seed.maxDurationMs / 60_000`。两个方向都只存在于路由文件里，没有被抽成纯函数，也没有任何用例断言它们。builder 层的测试是直接喂 `maxDurationMs: 60_000` 的，跳过了换算；W11 用例只检查非法数值把 Next 灰掉，且用的是 `wiza… | 换算写错（少一个 0、或 relaunch 侧忘了除）会让用户填的「60 分钟」变成 6 分钟或 600 分钟：前者表现为任务被平台无故取消，后者表现为资源限制形同虚设；重启已有任务时还会静默把原任务的限额改写。全绿。 | integration：在 tasks-new-wizard.test.tsx 加一条：展开 advanced fold，往 `wizard-max-duration` 填 1.5，提交后断言 wire body `maxDurationMs === 90000`；再加一条 relaunch 用例，源任务 `maxDurationMs:… |
| `F5-tasks-9` | P2 | cross-module-seam | `taskToLaunchPayload` 的 `spaceResolvable` 纯判定被 task-wizard-builders.test.ts 覆盖得很好（internal / 空 local / worktree-creation-failed 前缀 / scheduler 失败四种情形），但「false 时把 space 换成 `defaultWizardSpace('remote')` 并渲染 `wizard-spa… | 若三元判断被写反或提示被误删，重启一个 fusion/内部空间任务或 materialize 半途失败的多仓任务时，向导会把**残缺的仓库前缀**当完整空间预填并允许提交——用户以为在重放原任务，实际少了若干子仓，且没有任何提示。 | integration：在 tasks-new-wizard.test.tsx 的 relaunch 组里加一条：源任务 `spaceKind:'internal'`（或 status=failed + errorSummary 以 'worktree creation failed:' 开头 + 两个仓），断言 `wizard-spa… |

### 前端｜资源管理页（agents/skills/mcps/plugins/repos/memory/settings/users/home）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F6-resources-1` | P1 | text-assertion-only | ReposPage 渲染表格行（urlRedacted / SubmoduleBadge / RelativeTime / referencingTaskCount / 刷新 / 删除）、以及删除按钮的两条分支——referencingTaskCount === 0 时直接 DELETE 无确认、> 0 时开确认弹窗并以 ?force=1 删除——没有任何渲染级用例。唯一的单测文件是纯 readFileSync 文本断言，且其头注… | 删除按钮的分支一旦接反（例如把 pendingDelete 的判断写反、或 force 参数丢失），有引用的镜像会被无确认直接删掉（后端 409 静默失败、用户看到「点了没反应」），或没有引用的镜像反而弹出吓人的确认框；表格列换字段（urlRedacted → url）也不会被任何用例拦住，会把 Git 凭据渲染… | unit：新增 repos-page-rows.test.tsx：mock GET /api/cached-repos 返回两行（referencingTaskCount 0 和 2），断言 (a) 表格渲染两行且第一列文本等于 urlRedacted、不含 item.url；(b) 点 referencingTaskCo… |
| `F6-resources-2` | P1 | text-assertion-only | FusionDetailPage 的三个 mutation（approve → POST /approve、reject → POST /reject 带 {feedback}、cancel → POST /cancel）、拒绝弹窗的「feedback 为空则禁用提交」门、按状态分段渲染（awaiting_approval 才出 DiffViewer + 批准/拒绝按钮；done/failed/canceled 才隐藏取消按钮；i… | 批准是把 AI 生成的技能改动真正写进技能版本的不可逆动作。一旦接线回归（例如 approve 按钮误绑 reject.mutate、状态门写成 f.status !== 'done' 导致 done 的融合还能再批一次、reject 提交时 feedback 用了闭包旧值发空串），用户会误批或误拒融合结果，且没… | unit：新增 fusions-detail-route.test.tsx（照 distill-job-detail-route.test.tsx 的 createMemoryHistory + 桩路由套路，DiffViewer 可 vi.mock 掉）：mock GET /api/fusions/:id 分别返回 sta… |
| `F6-resources-10` | P2 | ui-visual-or-layout | e2e 视觉回归只截 /settings 的默认 runtime 页签（settings.png）和一张 390 宽的 network 页签手机图，systemAgents / limits / recovery / gc / appearance / rendering / authentication 七个子页签都没有桌面基线。而这几个页签里，recovery 与 gc 的单测本身也只是对 settings.tsx 做 rea… | 改动 .settings-section-panel 系列样式、或在某个子页签里落一个不合公共规范的控件（自写 chrome / 原生 select），既不会被视觉基线拦住，也不会被单测拦住——CLAUDE.md 明确把这类「视觉孤岛」定为产品级 bug，却正好落在这块盲区里。 | e2e：两步：(1) 在 e2e/visual-regression.spec.ts 里对 ?tab=limits / recovery / gc / rendering 各加一张桌面基线（clean fixture，与 settings.png 同 SNAPSHOT_OPTS），把「子页签改动不需要刷基线」这条现状反转… |
| `F6-resources-4` | P2 | no-test-at-all | RenderingTab 是 9 个 settings 子页签里唯一一个既没有渲染用例、也没有 grep 型源码锁的：其 runConnectivityTest 的四条分支（端点为空 → renderingTestEmptyEndpoint；渲染出 .review-diagram__svg → success；渲染出 .review-diagram__error → failure + 拼接错误文本；10s 轮询超时 → fail… | 改 PlantUmlBlock 的 DOM class（.review-diagram__svg / .review-diagram__error）或改 render 签名后，自检按钮会永远走到 10 秒超时分支并报「失败」，即使端点其实是好的——用户会以为自己的 PlantUML 服务坏了，而全部测试仍然绿。 | unit：新增 settings-rendering-tab.test.tsx：直接 render <RenderingTab config={…}/>（照 settings-network-effective-bind.test.tsx 的挂载方式），vi.mock '@/components/review/PlantU… |
| `F6-resources-6` | P2 | no-test-at-all | BackupCard 是一个内嵌在 GcTab 里的独立异步动作：点按钮 → setBusy → POST /api/backup → 成功渲染路径 + MB 大小、失败走 describeApiError + ErrorBanner。这个组件在 tests/ 和 e2e/ 里都没有任何引用（后端 backup.test.ts 只测服务端）。 | 备份是管理员在升级/迁移前的最后一道保险。按钮的 busy 门或错误分支一旦回归（例如 finally 里漏 setBusy(false) 导致按钮永久禁用、或错误被吞成成功态显示一个 undefined 路径），管理员会以为备份成功而实际没有产物。 | unit：新增用例直接 render GcTab（或导出 BackupCard 后单测）：mock POST /api/backup 分别返回 {path,sizeBytes} 与 500，断言 (a) 点击期间按钮 disabled 且文案切到 backupRunning；(b) 成功后渲染 backupSavedAs … |
| `F6-resources-8` | P2 | happy-path-only | agents.new 的创建按钮只判 draft.name === ''，AgentForm 的名称输入只挂了一个 HTML pattern 属性（按钮是 type="button"，不触发表单约束校验），agentForm 的 i18n 命名空间里根本没有 errors.nameInvalid。对比：plugins 有 plugins.errors.nameInvalid + aria-invalid/aria-errormes… | 用户输入「My Agent」「代理1」这类名字时创建按钮仍然可点，请求打到后端拿一个 400，页面只显示一条通用的 agent-invalid 横幅，不指向名称字段也不说明规则（提示只藏在 fieldNameHint 里）；而同一个仓里其它四种资源都会在输入时就红字提示。 | unit：在 AgentForm 的名称 Field 上接 error（新增 agentForm.errors.nameInvalid，zh/en 同步），并把 agents.new 的按钮 disabled 条件从 name === '' 改成 !AGENT_NAME_RE.test(name)。测试：agents-sp… |
| `F6-resources-9` | P2 | happy-path-only | AclPanel 的 save mutation 在 body 里回带 expectedResourceId/expectedAclRevision 做 OCC，onError 时 invalidate ['acl', aclUrl] 让面板刷到最新 owner/grants/revision，同时刻意保留 dirty 草稿让用户复核后重试。现有用例只覆盖成功保存（断言 PUT body 含 expectedAclRevision… | 若 onError 的 invalidate 被删或 useEffect 的 !dirty 守卫被改，409 之后面板要么继续拿着过期 revision 无限重试失败（用户被永久卡住），要么把服务端刚被别人改过的成员列表用本地草稿覆盖回去——即 ACL 静默回滚，正是 OCC 想防的事。 | unit：在 rfc099-acl-components.test.tsx 补一条：GET /acl 先返回 aclRevision:3，改动 visibility 使面板 dirty，PUT 返回 409（ApiError code 'acl-revision-conflict' 之类），断言 (a) 之后自动重发一次 … |

### 前端｜设计系统 / 公共组件 / a11y / i18n / 视觉一致性

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `F7-designsystem-2` | P1 | happy-path-only | `a11y.spec.ts` 的 15 条用例覆盖 /auth、/agents、/agents/new、agents inbox dialog、/workflows、/repos、/memory、/settings、/skills/new、/mcps 详情、/settings 移动暗色、onboarding、首页、/tasks/:id；其余 axe 调用点分散在 workflow-editor / skill-import / a… | 用户在"回答 agent 反问"和"审阅决策"这两个必经的人在环页面上遇到无标签表单控件、无名字的 radiogroup、图标按钮缺 aria-label 时，纯键盘 / 读屏用户无法完成流程，而 CI 全绿。新加的 clarify / review UI 引入 critical 级违规不会被拦。 | e2e：把 `a11y.spec.ts` 的 `expectNoCriticalOrSeriousAxeViolations` 改成对一份显式 ROUTES 常量做 sweep（照抄 `focus-ring-clip.spec.ts:900-921` 已验证的 21 条路由清单 + 种子 fixture），至少补 /cl… |
| `F7-designsystem-4` | P1 | harness-cannot-express | RFC-206 已经证明这类问题 jsdom 物理上测不到，并为焦点环建了 Playwright+CDP 的 21 路由 sweep。但焦点环之外的几何类全部退回源码文本断言：`tasks-list-long-name-truncation.test.ts` 顶注明说契约是"CSS 里有 max-width / nowrap / ellipsis + tsx 里有 title 属性"，`review-bubble-layout.t… | 下拉/弹层被 sticky 表头或工具栏遮住、粘性表头滚动时被行内容覆盖、长任务名/长节点名撑破列宽或被裁到无省略号 —— 这几类是本仓已多次复发的报障形态（长任务名撑列、review 气泡碰撞都是历史 bug），但目前全靠人肉截图发现；改一行 CSS 的 `overflow` 或 `z-index` 不会有任何… | e2e：复用 `focus-ring-clip.spec.ts` 已建好的 CDP sweep 骨架（同一份 ROUTES + 种子 fixture + 单 test 累加 + 覆盖度断言防空跑），新增三条独立审计：①**截断审计**——对所有声明了 `text-overflow: ellipsis` 的元素断言 `sc… |
| `F7-designsystem-5` | P2 | happy-path-only | 视觉门本身在 PR 上是有的（`visual-regression-nightly.yml` 的 `pull_request` 路径过滤命中 `packages/frontend/**`），这点任务描述的前提已过时。真正的洞是**基线选材**：25 个场景里 /auth、/agents、/workflows、/repos、/memory、/settings、onboarding、首页、/tasks 各 1 张桌面 light，in… | 改动共享 CSS token / 布局 class 时，clarify 回答页、评审页、技能/MCP/插件/用户/排程等管理页的视觉回归（面板塌陷、间距崩掉、暗色下文字消失）不会被任何基线捕获；暗色主题几乎是裸奔状态，只有两个断面有像素证据。 | e2e：把 `EXPECTED_VISUAL_SCENE_COUNT` 从"数字棘轮"升级成"路由清单棘轮"：在 `e2e-visual-infrastructure.test.ts` 里维护一份 REQUIRED_VISUAL_ROUTES 常量并断言 spec 源码为每条路由都声明了场景，缺任何一条即红。优先补 /c… |
| `F7-designsystem-6` | P2 | happy-path-only | zh/en 的**键集**同构有两道全量棘轮（`i18n-keys-symmetry.test.ts` + `i18n-batch-extraction.test.ts` 各自 flatten 全树比对），错误词条表也有 `rfc203-l1-completeness.test.ts` 的独立同构锁。但**插值占位符**只有 4 条硬编码 spot-check：`memory.scopeRow.agentCount` 含 `{{n… | 某个键在 en 里写了 `{{count}} items` 而 zh 里漏掉占位符，切到中文时数字凭空消失（用户看到"共 项"）；或占位符名字拼错导致 i18next 原样输出 `{{coutn}}`。两种都能过全部现有测试。 | unit：在 `i18n-batch-extraction.test.ts` 里加一条全量断言：对 flatten 后的每个键，用 `/\{\{\s*([\w.]+)\s*\}\}/g` 抽出 zh 与 en 的占位符名字集合（排序去重后比较），不一致即列出键名失败。我已在 /tmp 验证过这条断言当前全绿（3612 键 … |

### shared 契约 / 前后端 wire 一致性 / 类型边界

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `S1-shared-wire-1` | P0 | no-test-at-all | refineRepoSourceFields 里 `hasUrl && hasQueryCredential(repoUrl)` → 抛 `repo-url-query-credential` 这一分支，是 RFC-204 设计里「在 launch/schedule 门口拒绝」的唯一执行点（schemas/task.ts 是 hasQueryCredential 在生产代码中的唯一调用方）。全仓没有任何测试把一个 `?access… | 任何人重构这段 superRefine（挪位置、把 requireSource 逻辑并掉、把 hasUrl 判定改成先 trim 等）都不会红。一旦这道门失效，`https&#58;//host/o/r.git?access_token=TOKEN` 会重新进入 parseGitUrl → cache slug → ca… | unit：在 packages/shared/tests 新建 rfc204-launch-door-gate.test.ts：对 SENSITIVE_QUERY_KEYS 里每个 key 逐一断言 `StartTaskSchema.safeParse({workflowId,name,repoUrl:'https&#58;//h… |
| `S1-shared-wire-2` | P1 | contract-drift | MemoryDialogShell 没有用 shared 的 `CachedRepo` / `ListCachedReposResponse`，而是本地写了 `interface CachedRepoListEntry { id; url; localPath }`，再用 `api.get<{items: CachedRepoListEntry[]}>('/api/cached-repos')`。RFC-204 已把明文 `url… | 用户在记忆对话框里把 scope 切到 repo 后，下拉里是一串没有文字的空选项，无法判断选的是哪个仓库（只能靠顺序盲选），等于该功能不可用。 | unit：把本地 interface 删掉，改用 shared 的 `ListCachedReposResponse` / `CachedRepo`（label 用 `urlRedacted`），typecheck 立刻会指出 `r.url` 不存在。同时在 frontend/tests/memory-new-dialog… |
| `S1-shared-wire-3` | P1 | contract-drift | `bun run typecheck` = `bun run --filter '*' typecheck`，workspaces 只有 `packages/*`；e2e/ 既不是 workspace 也没有自己的 tsconfig，因此 30 个 spec 里内联的响应类型与断言从不参与任何 tsc。唯一覆盖 e2e 的 gate 是 `lint:repo-ui`（eslint 无 type-aware 配置，纯语法）。更糟的是… | 删/改一个 wire 字段可以穿过 format:check + lint + typecheck + backend/shared/frontend 三套单测，直到 Playwright CI 才红（跑在 build-binary 之后，反馈延迟十几分钟）；而如果这个字段只出现在 `as {…}` 的类型标注里… | ci-gate：加一个 e2e/tsconfig.json（含 playwright.config.ts + e2e/**），并把 `typecheck` 脚本改成 `bun run --filter '*' typecheck && tsc -p e2e --noEmit`；再在 root-test-entrypoint.te… |
| `S1-shared-wire-8` | P2 | happy-path-only | WgAssignmentsPortSchema / WgMessagesPortSchema / WgTasksAddPortSchema 各有一个 per-turn 安全上限（16/16/32，design §12「消息风暴」）。rfc185-leader-fanout.test.ts 锁了 WG_MAX_ASSIGNMENTS_PER_TURN 与协议文案的一致性，rfc164-workgroup-core.test.ts 覆… | 这两条 .max() 被误删或数值被改大后没有信号；一个失控的 leader/成员回合可以一次灌进任意条黑板广播或任务卡，把房间刷爆、把下一轮 prompt 撑到超预算（design §12 明确把它当风暴防线）。 | unit：在 packages/shared/tests 新建 wg-port-caps.test.ts（纯 schema 层，无需 DB）：分别构造长度 = cap 的载荷断言 ok，长度 = cap+1 断言 ok:false 且 errors 里有 root 层 issue；上限值直接引用 WG_MAX_MESSAG… |

### 硬数据｜lcov 覆盖率分析

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `M1-lcov-1` | P0 | no-test-at-all | RFC-036 的 SSO 登录从 `login/start` → IdP → `callback` → code 换 token → id_token 验签（签名/iss/aud/nonce）→ 供给决策 → 建 session → 重定向，整条链路没有任何一个测试执行过。全量重跑覆盖率：`auth/oidc/tokens.ts` 行 7.55% / 函数 0%，`auth/oidc/discovery.ts` 行 10.87%… | 任何人改动 `verifyIdToken`（例如误删 nonce 校验、把 jose 的 issuer/audience 选项传错、把 catch 吞成 return）或 `consumeFlow`（state 变成可重放）都不会有一条测试变红，而 CI 全绿。后果是最严重的一类：攻击者可用伪造/重放的 id_t… | integration：分三层补：① 单元层——`exchangeCodeForTokens` 已经带可注入 `fetcher`（tokens.ts:24），直接注入假 fetch 断言四态：200 正常 / 非 2xx 抛 `token-exchange-failed` / 响应缺 `id_token` 抛 `token-exchan… |
| `M1-lcov-3` | P1 | text-assertion-only | `services/structuralDiff/deep/runner.ts` 全量重跑后行 6.12% / **函数 0%**（未覆盖 29-74，即 `runIndexer` 整个函数体）。它自己的文件头写着"The spawn is injectable so tests drive the four outcomes (ok / non-zero / timeout / garbage output) with a st… | 超时分支（`setTimeout` → `proc.kill()` → 返回 `timeout`）如果失效——例如 `clearTimeout` 顺序写反、`timedOut` 判定被移到 `await proc.exited` 之前、`kill()` 抛错未被 catch——一个卡死的 SCIP indexer… | unit：用它自己预留的 `spawn?: SpawnFn` 注入桩（不需要真 indexer），补四条真调用 `runIndexer(...)` 的断言：① exited=0 且 outPath 写入非空 → `{ok:true, scipBytes}`；② exited=1 → `{ok:false, reason:'… |
| `M1-lcov-4` | P1 | mock-too-deep | `services/orphanReconcile.ts` 行 58.56%，未覆盖 37-46 正是 `runProcessGone` 函数体（`pid===null` / `!isProcessAlive(pid)` / `spawnBinaryPath` 不匹配 三条 OR 分支）。它是生产路径上唯一的判活实现（`startOrphanReconcileLoop` 里 `isGone: runProcessGone`，orp… | 判反的两个方向都很痛：① 过于宽松（比如 `pidCommandContainsBinary` 对回收 pid 的判断退化成恒 true）→ 正在跑的 agent 子进程被判定为"已消失"，daemon 把它的 node_run 翻成 failed，用户看到任务在 agent 还在写代码时莫名失败；② 过于保守（… | unit：直接对 `runProcessGone` 写纯谓词表测试（无需 DB）：`{pid:null}` → true；`{pid: process.pid, spawnBinaryPath: null}` → false；`{pid: <一个刚 kill 掉的真子进程 pid>}` → true；`{pid: proc… |
| `M1-lcov-6` | P1 | cross-module-seam | `routes/fusions.ts` 全量重跑行 20.48% / 函数 10%（未覆盖 35-129，即全部 7 个 handler 的函数体）。机械核对：把 `tests/contracts/registry.ts` 与 `api-contract*.test.ts` 排除后，正则搜索 `/api/fusions` 在 `packages/backend/tests/**` + `e2e/**` 中**一次都不出现**。对照… | 服务层再稳也救不了接线错误：`c.req.param('id')` 写成别的 key、actor 传成 `null`（越权：任何登录用户能 approve 别人的 fusion——权限恰好是在 services/fusion.ts 内按 actor 判的）、reject 的 feedback 字段没解析出来（融合… | integration：照 `tests/routes-clarify.test.ts` 的模式（`createApp` + `app.request` 打真路由）新建 `routes-fusions.test.ts`：POST 创建（合法体 200 + 缺字段 422）、GET 列表（`skillName` 过滤 + 非 owner … |
| `M1-lcov-7` | P1 | no-test-at-all | `tests/contracts/registry.ts` 登记了全部 204 个端点，其中只有 21 条带 `happy` fixture（会真发请求并按 schema 校验 2xx），其余 183 条只享有 `api-contract.test.ts` 的 401 匿名拒绝断言。把这 183 条与"路径字面量是否在其它后端测试/e2e 中出现"求交后，剩 33 条**除 401 外没有任何测试触碰过**。高风险的几条：`POS… | 这批端点大多是"点一下就改状态"的管理动作。缺正向测试意味着：admin 重置密码若把目标 id 取错就会重置到自己/他人（且没有任何用例会红）；技能版本 restore 若把版本号参数取错会把技能回滚到错误快照并覆盖当前内容；`clear-recovery-suspension` 若没做任务成员校验，任意用户可… | integration：分两步且优先第一步：① 对 33 条里"会写状态"的 12 条（reset-password / restore / clear-recovery-suspension / questions/manual / selection / probe / fusions 的 5 条写操作）各补一条 200 正向 + … |
| `M1-lcov-8` | P1 | no-test-at-all | `services/runtime/claudeCode/config.ts` 行 27.08%，未覆盖区间 48-82 恰好是 `bridgeClaudeCredentials` 整个函数：env 认证（`ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` / `CLAUDE_CODE_OAUTH_TOKEN`）存在时短路不桥接、macOS 从 `security find-generic-p… | 两类后果。安全侧：0600 权限位如果在重构中掉了（`writeFileSync` 的 mode 参数被删、`chmodSync` 被移到 try 外后静默失败），用户的 Claude 订阅凭据就以默认 0644 落在 `~/.agent-workflow/runs/{task}/{node}/` 下，同机任意进… | unit：把 `bridgeClaudeCredentials` 的两个外部依赖参数化（`spawnSync` 与 `homedir` 通过可选参数注入，或整体抽成接受 `{platform, readKeychain, homeDir}` 的纯壳），然后在临时目录里断言：① 三个 env 任一存在 → 目标目录**不存在… |

### 元层｜测试框架能力缺口（什么是物理上测不到的）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `M2-harness-1` | P0 | text-assertion-only | `vitest` 用 happy-dom + `css: false`，既无布局引擎也不注入样式表，任何「元素被裁剪 / 内容溢出 / 折行 / 重叠 / 尺寸」的断言在前端单测层物理上不可能成立。三个已上线的真实溢出 bug（任务详情问题卡标题、任务列表工作流列 badge 换行、反问问题标题）修复后各自只留下一条「readFileSync styles.css + toMatch(/overflow-wrap:anywhere… | 用户在 /tasks/:id 的「问题」页签或 /clarify/:id 里放一条长无空格串（如函数签名、URL、路径），标题会顶出卡片框或把 badge 甩到下一行——页面看起来像坏了。整套 616 个前端测试 + PR 上的 chromium e2e 全绿，只有人肉打开页面才能发现。这类 bug 已经用同一形… | e2e：把 `e2e/ux-consistency.spec.ts:96` 的 `expectNoPageOverflow` 抽成共享 helper（放 `e2e/harness.ts` 或新建 `e2e/layout-audit.ts`），再补一条**内容驱动**的通用溢出审计 spec：对每条已 seed 的路由（至… |
| `M2-harness-2` | P0 | env-gated-or-skipped | `integration-opencode.yml`（唯一会起真 opencode 进程 + 真 LLM 调用的套件）和 `git-protocols-e2e.yml`（唯一跑真 Gitea 容器 HTTPS/SSH clone 的套件）的触发器都是 `schedule` + `workflow_dispatch` + `pull_request`（带路径过滤），**没有 `push` 触发**。而本仓的既定开发流程是全部直接提交… | opencode 上游行为漂移（CLI 参数、退出码、config 合并优先级、XML envelope 格式）或本地对 opencode 接口的回归，能带着「全绿 CI」落进 main 并可能被打成 release tag；同理 git HTTPS/SSH 凭据与协议回归也一样。等 nightly 抓到时，问题… | ci-gate：两处都补 `push: { branches: [main], paths: [<现有 pull_request paths 原样复制>] }`（照抄 `visual-regression-nightly.yml:28-44` 的写法，成本 ≈ 10 行 YAML）。同时给四份非 ci.yml 的 workflo… |
| `M2-harness-3` | P1 | harness-cannot-express | `tests/perf/diff.ts` 的判红条件是 `ratio >= 1.2 && deltaMs >= 0.1`（**与**关系）。但 `baseline.json` 里 5 条基准的 median 分别是 0.018 / 0.003 / 0.003 / 0.001 / 0.001 ms。要满足 delta ≥ 0.1ms，`workflow-parse-v3` 需要退化到 ≥0.118ms（6.6 倍）、`envelop… | 任何把 workflow 解析、envelope 抽取、密钥脱敏或路径安全校验拖慢 2-5 倍的改动（例如给 zod schema 加一层 superRefine、给 redact 加一条回溯型正则）都会静默通过 perf 门。而真正会被用户感知的性能问题——1000 事件分页、10 并发任务调度、10MiB d… | ci-gate：三步：(1) 把 `ABSOLUTE_FLOOR_MS` 改成**相对下限**——`max(0.002, baseline.median * 0.1)`，或者干脆按 p95 而非 median 判定并把 floor 降到 0.005ms，让 20% 的 ratio 门重新生效；(2) 在 ubuntu runne… |
| `M2-harness-4` | P1 | harness-cannot-express | `codecov.yml` 把 patch coverage target 设成 70%，作为 CLAUDE.md「test-with-every-change」的机械执行手段。但 lcov 只在 `ci.yml` 的 `test-backend` ubuntu 分片里生成并上传；`test-frontend` job 的命令是 `bun run --filter @agent-workflow/frontend test -- … | 前端有 ~50% 的仓库源码量，其新增 UI 逻辑、hook、lib 纯函数可以完全不带测试落地而 CI 全绿——「每次改动都带测试」这条硬规矩在前端侧只剩人工 review 兜底。shared/src 里的 zod schema 改动同理。这正是本领域其它 gap（例如溢出类只写文本断言）能长期存在的制度性原因。 | ci-gate：给 `packages/frontend` 加 `@vitest/coverage-v8` devDependency，`vitest.config.ts` 里配 `coverage: { provider: 'v8', reporter: ['text','lcov'], reportsDirectory: '… |
| `M2-harness-5` | P1 | contract-drift | 根 `typecheck` 是 `bun run --filter '*' typecheck`，workspaces 只声明了 `packages/*`，所以只有三个包的 tsconfig 生效。逐一核对：`packages/shared/tsconfig.json` 的 include 只有 `src/**/*` → 它的 126 个测试文件**既不 typecheck 也不 lint**（`packages/shared/p… | 删掉或改名一个 wire 字段时，本地三道门（typecheck + lint + test）全绿，直到 Playwright 真跑起来才在 CI 里炸（这条已经发生过）。更隐蔽的是 shared 的 126 个测试文件：其中任何一处 `as` 误断言、或跟着 schema 改名后本该编译报错的地方，都会静默通过… | type-system：(1) 给 `packages/shared/tsconfig.json` 的 include 加 `tests/**/*`、lint 脚本改 `eslint src tests`；(2) 在仓根加 `tsconfig.repo.json`（include: `e2e/**/*`, `scripts/**/*.t… |
| `M2-harness-7` | P1 | contract-drift | `tests/contracts/registry.ts` 登记了 204 个端点，`api-contract-coverage.test.ts` 强制新路由必须进 registry（这层是扎实的）。但真正验证**响应体形状**的 `happy` fixture 只有 23 条，且其中 `schema:` 字段有 21 条——逐条看：`z.array(z.any())` 出现在第 123/155/170/184/218/291/3… | 改一个响应字段名（或把 `nullable` 变成 `optional`、把数组元素少一个键）时，后端测试、契约测试、typecheck 全绿——前端只有编译期类型，运行时不校验，页面会静默渲染出 undefined/空白。这条已经以另一种形态发生过（launch body 白名单丢字段）。目前 97% 的端点在… | integration：把 registry 从「可选 happy」翻成「默认必须有 schema」：给 `EndpointSpec` 加必填的 `responseSchema`，对暂时无法构造 happy 请求的端点必须写显式 `skipHappy: '<原因>'`（现在这个字段定义了但 0 处使用），再加一条元层测试断言 `skip… |
| `M2-harness-10` | P2 | harness-cannot-express | `test-suite-policy.test.ts` 是全仓最有价值的元层守卫——用 TS AST 精确清点 `.only/.skip/.todo/.fixme/.fail` 并把每条 skip 锁进 `ALLOWED_SKIP_COUNTS`。但它的 `TEST_ROOTS` 只有四个目录：`packages/backend/tests`、`packages/shared/tests`、`packages/frontend/t… | 如果有人在 `packages/frontend/src/**/__tests__/` 下提交一个 `describe.only`，该文件里其余用例会静默不跑，而全仓最强的 `.only` 守卫检测不到——CI 全绿。同一位置也可以塞进不受管的 `.skip`，绕过那份被精心 review 的 11 条 skip… | ci-gate：把 `TEST_ROOTS` 从「目录白名单」改成「按包扫描 + 排除 node_modules/dist」：加入 `packages/frontend/src`、`packages/backend/src`、`packages/shared/src`、`tests`、`scripts`，让扫描范围与 vites… |
| `M2-harness-11` | P2 | no-test-at-all | `package.json:29` 定义了 `depcheck` 脚本跑 dependency-cruiser，`.dependency-cruiser.cjs` 注释里写「Any future PR that accidentally imports across the wrong seam fails `bun run depcheck` immediately」。但全仓搜索 `.github/workflows/` 无任何… | 新增一个 shared 导出并被两侧循环引用时，typecheck 绿、全部单测绿，直到 build-binary job 里编译出的二进制在启动时因模块初始化顺序拿到 undefined 而崩——报错信息不指向真正的环。这条已经以 RFC-079 的形态发生过一次。此外 `no-services-to-rout… | lint-rule：(1) 在 `.dependency-cruiser.cjs` 的 forbidden 里加标准的 `no-circular`（severity error，`to: { circular: true }`），并把 `exclude` 里的 tests 保留（测试里的环不影响运行时）；(2) 把 `bun run… |
| `M2-harness-12` | P2 | concurrency-timing | 后端的运行方式是 `bun test --isolate --randomize`——每文件一个新进程、**串行**执行。CI 注释明确说明 `--parallel` 会 DEADLOCK 整个套件（多个文件会起真 daemon 抢单实例 flock），所以分片是靠跨 runner 的 VM 隔离实现的，「不改变任何并行安全性」。`sqlite-concurrency-fuzz.test.ts` 用 fast-check 打状态机… | 两个写者交错造成的状态机破损（例如调度器把 node_run 推进 done 的同时用户点了 cancel，或 daemon 重启后的 orphan reap 与人工 retry 撞车）只能靠 CAS 谓词的正确性保证；一旦某条路径漏写 CAS，没有任何测试能复现。表现为任务卡在不可能的状态、UI 显示 runn… | integration：不追求「真并行跑测试套件」（那会撞 flock，成本收益不划算）。改为定向补两类：(1) **单进程内的 await 交错**——写一个 helper 让两条服务调用在受控的 await 点交错（用可解析的 promise 手动排定顺序），针对 `cancelTask` × 调度器推进、`resumeTask` … |
| `M2-harness-8` | P2 | concurrency-timing | `playwright.config.ts:67` 设置 `retries: process.env.CI ? 1 : 0`。任何 e2e 用例第一次失败、第二次通过，Playwright 报 flaky 但 job 退出码为 0，CI 显示绿。这与 CLAUDE.md 里「发现某测试间歇性失败，先确认是不是真 bug……绝不允许『重跑就过了』作为通过依据」的规则相悖——现在这个「重跑就过了」是**自动**发生的，人根本看不到。团… | 真实的时序竞态（例如任务状态 WS 推送与列表刷新的赛跑、编辑器自动保存与导航的竞速）以 flaky 形态存在于 CI 中，被自动重试掩盖成绿色，直到用户在真实环境里稳定命中。因为没有 flaky 统计，也无法回答「这条用例最近重试了多少次」，问题不会被排上日程。 | e2e：保留 `retries: 1`（CI 冷启动确有不可控抖动），但把 flaky 变成**可见且有上限**：(1) CI 的 e2e job 加 `--reporter=blob` 并上传 blob 报告为 artifact；(2) 加一个后置步骤解析报告，若本次运行 flaky 用例数 > 0 就在 job su… |
| `M2-harness-9` | P2 | ui-visual-or-layout | `playwright.config.ts` 只注册两个 project：`chromium`（默认、PR/push 门）与 `webkit`（需 `PLAYWRIGHT_WEBKIT=1`，仅 nightly 用）。全仓搜索 `firefox` 零命中——Gecko 引擎从未被任何自动化触碰过。WebKit 侧则更弱：它只在 `e2e-webkit-nightly.yml` 的 schedule/dispatch 下跑（无 pu… | Safari 用户会遇到 chromium 上不存在的 focus 环被裁、`overflow: clip` 行为差异、`:has()`/`subgrid` 支持差异等问题；Firefox 用户遇到的任何 Gecko-only 问题（flex/grid 细节、input 默认样式、滚动条占位）则永远不会被任何自动… | e2e：(1) 给 `e2e-webkit-nightly.yml` 加 `push: { branches: [main], paths: ['packages/frontend/**','e2e/**'] }`，让 WebKit 至少随前端提交跑（和 visual-regression 一致）；(2) 把 `focu… |

### 元层｜CI 门禁缺口（什么改动能穿过所有门）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `M3-ci-gates-1` | P0 | no-test-at-all | release.yml 的 build job 只做 checkout → setup-bun → bun install → bun run build:binary → upload，没有 needs 指向任何测试/lint/typecheck job，也没有 ci.yml 里那步 `"$bin" version` / `"$bin" doctor` 冒烟。任何人 `git push origin vX.Y.Z` 就会把该 c… | 用户从 Releases 下载 linux-arm64 版本，可能拿到一个启动即崩（bundle 断裂 / 原生绑定缺失 / 架构相关的 embed 资源路径错）的二进制；或者拿到一个在红 CI commit 上打包的版本。两种情况平台都会先向所有 arm64 Linux 用户发货再发现。 | ci-gate：两件事：(1) release.yml 的 build job 加 `needs` 或在 job 内前置 `bun run typecheck && bun run lint && bun run test` 中至少一条硬门，最低限度把 ci.yml:445-450 那段 `"$bin" version` / `… |
| `M3-ci-gates-2` | P0 | happy-path-only | tests/perf/diff.ts 判定回归要求 `ratio >= 1.2 && deltaMs >= 0.1ms` 两个条件同时成立。而 baseline.json 里五条基线的 median 分别是 0.018 / 0.003 / 0.003 / 0.001 / 0.001 ms。要凑够 0.1ms 的绝对增量：workflow-parse-v3 需要退化到 0.118ms（6.5 倍），envelope-extract … | 热点函数（workflow 解析、envelope 抽取、密钥脱敏、路径 safe-join）退化到 5 倍慢也不会被拦。密钥脱敏与 safe-join 是每条日志/每次文件访问都要过的路径，5 倍退化在真实任务里会体现为整体吞吐掉一个量级，而且只有用户在生产上跑大任务时才发现。 | ci-gate：把绝对下限改成与基线相关的相对量（例如 `max(0.02ms, baseline.median * 0.5)`），或对亚毫秒基准改用 p95 + 更高迭代次数把噪声压下去再取消绝对下限；同时在 diff.ts 里加一条硬校验：baseline.platform 必须等于 current.platform，不等直… |
| `M3-ci-gates-5` | P0 | harness-cannot-express | codecov.yml 把 patch 目标设为 70%，注释里写明这是 CLAUDE.md「test-with-every-change」的机械执行手段。但 codecov 的 patch/project status 是 PR 级别的检查——本仓的实际开发方式是全部直推 main（git log 近 30 条全是直接 commit，仅有的 2 个 merge commit 是 `Merge remote-tracking br… | 「新代码必须带测试」这条项目铁律在自动化层面完全没有执行力：一次纯前端（React 组件 / hook / 路由）零测试的大改动可以一路绿灯落到 main。后端也一样——只要不开 PR，patch status 根本不生成。 | ci-gate：两条路二选一：(a) 承认工作流是直推 main，把覆盖率门改成 CI 内可执行的形式——在 test-backend 里用 `bun test --coverage --coverage-threshold=<line>` 之类的硬阈值（或写一个读 lcov 的对比脚本，与上一次 main 的 lcov 比对，… |
| `M3-ci-gates-10` | P1 | happy-path-only | CI 侧的迁移门只有 `drizzle-kit check`（scans job），它验的是 migrations 目录内部一致性——无孤儿 SQL、journal 同步、无版本冲突——完全不看 SQL 做了什么。测试侧 upgrade-rolling.test.ts 从 journal idx 1/13/19 三个冻结点前滚到 HEAD，但冻结点上的库是空库（freezeAt 只是把 journal 截断后 migrate 一个… | 一条给既有表加 NOT NULL 列而回填写错、或者 UPDATE ... WHERE 条件写反的迁移，能通过 drizzle-kit check（journal 没问题）和 upgrade-rolling（空库前滚没问题），在用户机器上第一次 `start` 时把历史任务/评审/记忆数据改坏或抹掉——这是本地优… | ci-gate：加一条策略测试（仿 test-suite-policy 的白名单模式）：枚举 db/migrations/*.sql，要求每个 tag 要么有对应的 migration-<n>-*.test.ts / 在测试源里被 tag 名引用，要么出现在一份显式的 `PURE_DDL_NO_DATA_IMPACT` 白名单里… |
| `M3-ci-gates-3` | P1 | no-test-at-all | diff.ts 计算了 `missing` 行（missing-baseline / missing-current）并把数量写进 PERF_SUMMARY，但退出码只看 `regressions.length > 0`。因此：把 run.ts 里某条 bench 删掉或改名，perf job 打印 `missing=1` 然后 exit 0——门禁静默减少一条覆盖面。同时整个 tests/perf/ 目录（run.ts / di… | 任何人（包括为了让 CI 变绿的人）可以把某个变慢的基准从 run.ts 里删掉，perf 门照常绿灯，而且日志里那行 missing=1 没人会看。覆盖面净减少不留痕。 | unit：(1) diff.ts 把 `missing.length > 0` 也纳入非零退出（missing-baseline 可留作显式 opt-in 的新基准，但 missing-current 必须 fail closed）；(2) 新建 tests/perf/diff.test.ts（挂到 shared 或 ba… |
| `M3-ci-gates-4` | P1 | no-test-at-all | .dependency-cruiser.cjs 定义了三条 severity: error 的跨包/跨层禁令（no-frontend-to-backend / no-services-to-routes / no-shared-to-app），package.json 也有 `depcheck` 脚本，但全仓搜索 .github/、scripts/、packages/*/package.json 之后，`depcheck` / `… | services 层反向 import routes 会形成模块初始化环——这正是本仓已经踩过的「单二进制构建时模块 init 循环，typecheck 和 bun:test 都测不出来」那类事故；等到 build:binary 或者运行时才炸，而 build:binary 在 ci.yml 里是 needs 测… | ci-gate：在 ci.yml 的 scans job 里加一步 `bun run depcheck`（<10s，无需 opencode）。同时在 root-test-entrypoint.test.ts 里加断言：scans job 的正文必须包含 `run: bun run depcheck`，避免这一步日后被悄悄摘掉——… |
| `M3-ci-gates-6` | P1 | env-gated-or-skipped | integration-opencode.yml 与 git-protocols-e2e.yml 的 `on:` 只有 schedule + workflow_dispatch + pull_request(paths)，没有 `push:` 触发（对照 visual-regression-nightly.yml 是有 `push: branches: [main]` + paths 的）。既然本仓一律直推 main，这两套的「路… | 「我们的框架对着真实 opencode 回归了」和「Gitea HTTPS/SSH clone 路径坏了」这两类线上级故障，最快也要 24 小时后才在一个没人订阅的夜间面板上亮红；实际上没有任何机制把红色送到人眼前，可以红几周而无人察觉，直到用户报「任务起不来 / 仓库拉不下来」。 | ci-gate：给这两个工作流补 `push: branches: [main]` + 同样的 paths 过滤（与 visual-regression-nightly.yml 保持一致的形状），让改到那几个文件的直推 commit 立刻触发；再给所有 schedule 工作流补一个 `if: failure()` 的 `gh … |
| `M3-ci-gates-7` | P1 | concurrency-timing | ci.yml 的 concurrency group 是 `${{ github.workflow }}-${{ github.ref }}`，在 main 上所有 push 共用同一个 group 且 cancel-in-progress: true。共享 main 的多 session 开发模式下（本仓常态），后一个 commit 的 push 会取消前一个 commit 正在跑的 run——被取消的那个 commit 自此没… | 一个引入回归的 commit 可以永久停留在 main 上且从未被任何测试执行过——CI 面板上它既不是绿也不是红，是 cancelled。后续 commit 的绿只证明「最终状态可用」，不能定位是谁引入的；如果最后那次 push 也被取消，整段区间就是无判决的。 | ci-gate：main 上不做取消：把 concurrency 改成条件式——`cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}`（PR/分支仍取消以省资源，main 上每个 commit 都跑完）。在 root-test-entrypoint.test.ts… |
| `M3-ci-gates-8` | P1 | contract-drift | `bun run typecheck` = 三个包各自 `tsc --noEmit`，而三个 tsconfig 的 include 分别是 backend `[src, tests, db, drizzle.config.ts]`、frontend `[src, tests, vite.config.ts, vitest.config.ts]`、shared **只有 `src/**/*`**。仓库根目录没有覆盖 scripts/… | 改 wire 字段名时，e2e/ 里内联的响应类型和断言不会被本机任何门发现（本仓已实证：本机全绿、Playwright CI 才红）；shared 测试里的类型错误同样只在运行时暴露。build-binary.ts 里的类型/逻辑错误要等到 CI 最后一个 job 甚至 release 时才炸，而 releas… | type-system：(1) packages/shared/tsconfig.json 的 include 补上 `tests/**/*`，与另外两个包对齐；(2) 新增 tsconfig.repo.json（extends base，include `scripts/**/*`、`tests/perf/**/*`、`e2e/**/… |
| `M3-ci-gates-9` | P1 | harness-cannot-express | test-suite-policy.test.ts 是很强的元层守卫，但它锁的是三件事：禁止 only/todo/fixme/fail、每个 skip 必须在 ALLOWED_SKIP_COUNTS 白名单里、每个 RUN_* 开关必须有 CI 激活路径。它不统计测试文件数、不统计 test() 数量、不看断言内容。全仓也搜不到任何「测试数量/断言不许净减少」的守卫（唯一沾边的是 api-contract-coverage.tes… | 回归防护网可以在一次「清理无用测试」的 commit 里被大面积拆掉而 CI 全绿；之后真正的回归再进来时没有任何东西会红。这在一个把「测试随每次改动落地」写进 CLAUDE.md 的仓库里，是规则与执行力之间最大的落差。 | ci-gate：在 test-suite-policy.test.ts 里加两条低成本硬锁：(a) 四个 TEST_ROOTS 下的测试文件总数不得低于一个提交在文件里的下限常量（如 `MIN_TEST_FILES`，取当前值再留 2% 余量，下调时必须在 diff 里显式改这个数字 → 强制评审）；(b) 同理锁 `test(… |
| `M3-ci-gates-11` | P2 | no-test-at-all | scans job 跑 `bun audit --audit-level=high --ignore GHSA-w7jw-789q-3m8p`：所有 moderate 级别公告一律放过（注释自述这是为了滤掉 esbuild 之类 dev-only 噪声，但同一条规则也放过了会进单二进制的 moderate 运行时依赖）；那条 --ignore 是无到期日的永久豁免；没有任何测试或门禁盯着这个 ignore 列表的长度——再加三条 … | 一个进入单二进制的 moderate 级 RCE/路径穿越依赖公告可以长期存在而 CI 全绿；而永久 --ignore 一旦成为惯例（注释里已经写了「不要降级成 continue-on-error」，却没写「不要让 ignore 列表变长」），安全门会随时间静默失效。 | ci-gate：(1) 分两档：对运行时依赖用 moderate 阈值，dev 依赖保留 high；bun 没有可靠 --prod 时改用一个读 `bun audit --json` 的小脚本按 package 归属分档。(2) 在 root-test-entrypoint.test.ts 里断言 audit 步骤的 `--ig… |
| `M3-ci-gates-12` | P2 | harness-cannot-express | test-suite-policy.test.ts 的 TEST_ROOTS 是 backend/tests、shared/tests、frontend/tests、e2e 四个目录，不含 packages/frontend/src。但 vitest 的默认 include 会捡起 src 下的测试，实际有 3 个测试文件住在 `packages/frontend/src/**/__tests__/`（都是溢出/截断类的布局回归）… | 这三个文件锁的是文本溢出/列宽截断类的视觉回归；被 skip 掉后 CI 依旧全绿，而对应的 UI 回归（长任务名撑破布局之类）会直接漏到用户界面上。范围小但性质与 M3-ci-gates-9 同类：防护网可以被静默关闭。 | ci-gate：把 `packages/frontend/src` 加进 TEST_ROOTS（listTestFiles 已经是递归实现，加一行即可，且当前这 3 个文件无 skip，加进去不会引入红）。更彻底的做法是把这 3 个测试挪进 packages/frontend/tests 与其余 616 个对齐，并在策略测试里断… |

### 元层｜测试自身的质量（防护是不是真的在防）

| id | 严重度 | 逃逸类别 | 未防护的是什么 | 生产后果 | 建议防护层 |
| --- | --- | --- | --- | --- | --- |
| `M4-test-quality-1` | P0 | text-assertion-only | 不变量「任何一次 session 拆除都必须清空 React Query 缓存 + IDB clarify/review 答案草稿」只被 `logout-clears-cache.test.ts` 的 3 条 grep 防护，而 grep 只检查 `queryClient.clear()` / `clearAllClarifyDrafts` / `clearAllReviewDrafts` 三个字符串**在 UserMenu.ts… | 共享浏览器上，用户 A 的会话过期（401）或用 orphan 兜底按钮登出后，用户 B 登录时 React Query 的 stale-while-revalidate 会先渲染 A 的私有数据（任务列表/详情、ACL 成员名单、资源列表）再被 refetch 的 403 冲掉；IDB 里 A 未提交的 cla… | unit：两层补：(1) 行为层——把清理逻辑收敛成 `stores/auth.ts` 里的单一 `teardownSession()`（clearToken + queryClient.clear + 两个 draftStore clear），写 vitest：预先往 QueryClient 塞一条 `['tasks']… |
| `M4-test-quality-2` | P1 | no-test-at-all | CLAUDE.md「test-with-every-change」的机械执行手段是 codecov 的 patch 70% 状态检查，但 lcov 只有 backend 的 ubuntu 分片上传。前端的 vitest 与 shared 的 bun:test 在 CI 里都不带 `--coverage`，`@vitest/coverage-v8` 根本没装（bun.lock 里只是 vitest 的 optionalPeer，不是… | 前端新增一个路由 / 组件 / hook 完全不写测试，CI 全绿、codecov 状态检查绿（因为这些行不在任何报告里），reviewer 也没有任何数字提示。前端是 UI 回归最高发的地方（本仓已有 3 个已知 flaky + 大量源码文本兜底断言），却恰恰是唯一没有覆盖率数字的一层。长期结果是前端真实覆盖率… | ci-gate：装 `@vitest/coverage-v8`，在 packages/frontend/vitest.config.ts 里加 `coverage: { provider: 'v8', reporter: ['text','lcov'], reportsDirectory: './coverage' }`，CI … |
| `M4-test-quality-5` | P2 | env-gated-or-skipped | `test-suite-policy.test.ts` 是本仓防「测试静默失效」的核心元护栏，用精确相等断言锁住全仓 skip 清单（:265）。但它的 `TEST_ROOTS` 只有 4 个目录：backend/tests、shared/tests、frontend/tests、e2e。而 `packages/frontend/vitest.config.ts` 没有配 `include`，走 vitest 默认 glob，因此… | 在 `packages/frontend/src/**/__tests__/` 下提交一个 `describe.skip` / `test.skip`，`test-suite-policy` 的精确清单断言不会红、CI 全绿，覆盖率却静默消失——正是这条元护栏文件头（:1-7）声称要防的场景，只是发生在它自己的盲… | lint-rule：把 `TEST_ROOTS` 从「枚举 4 个 tests 目录」改成「枚举 3 个 package 根 + e2e，递归找所有 `*.{test,spec}.*` 并排除 node_modules/dist」，让扫描面与各 runner 的实际 include 面一致；再补一条自校验断言：策略扫到的测试文件总数… |