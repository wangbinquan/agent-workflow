# RFC-258 实现门记录(2026-08-06)

评审方式:对抗子代理(全新上下文,~21 分钟,68 工具调用;Codex 本机组合仍处
RFC-259 记载的不可用状态,按 RFC-255/257/259 先例替代)。判定:3 P0 + 9 P1 +
14 P2,逐条核实全部属实零驳回。处置如下;核对通过项清单见评审原文(F-03/F-04/
F-05/F-07/F-10/F-11/F-12/F-13/F-14/Panel F-16 等抽查均属实)。

## 已修(P0 全部 + P1 全部 + P2 十项)

| # | 处置 |
|---|---|
| P0-1 | focus effect 改按值依赖 + 同值只滚一次 ref 守卫(宿主内联对象不再触发视口回跳/闪烁重放) |
| P0-2 | externalFile 分支保留根仓 `''`,query 恒经 `repoKeyWire` 发送(多仓 root diff 外点击不再 400) |
| P0-3 | snapshotDigest 改 `--porcelain -uall`(未跟踪目录内文件参与内容指纹)+ 回归测试 |
| P1-1 | SourcePane codeIntel key 注入 contentDigest 快照提示 |
| P1-2 | structural-diff deep **单仓 task-scope** 接入 indexCache(cacheScope 注入;测试 seams 走直接路径);多仓/node-scope 保持直接路径——design §2.3 口径同步订正为「单仓已接,其余 follow-up」 |
| P1-3 | 缓存 entry 记 probe 版本,hit 经 probe-memo(5min TTL)比对版本,升级 indexer 失效旧图且不产生 per-click which-spawn |
| P1-4 | splitStructuralPath 对 structural-only miss 走「显式 repoKeys ∪ files.repoKey」最长前缀匹配(仍零路径形状猜测) |
| P1-5 | 影响面 caller 不在 diff 内时落 externalFile 只读视图(deep caller 常在未变更文件) |
| P1-6 | FileSymbolAnchorBar:全文档符号锚点条 + `status≠ok` 完整性徽标(F-09 呈现面补齐;AC-2 键盘保证路径落地) |
| P1-7 | 时序图参与者头接 onOpenSource(role=button + Enter) |
| P1-8 | push 采集当前视图 scrollTop,pop rAF 恢复(F-17 快照兑现) |
| P1-9 | 七项测试锁全部补齐:①viewport transform 逐字节不变 ②kind→null 重置回归 ③hunk-owner 单动作(code-intel 零调用) ④版本守卫内容交换断言(并订正测试头虚假声明) ⑤调用链树 external 无入口 ⑥indexCache 权重逐出 + 64MB 拒载 ⑦file-symbols go/rust/java/cpp/scala 矩阵 |
| P2-1 | SymbolMenu error 态文案(焦点管理/外点关闭记档见下) |
| P2-2 | impact 叶名提取 `#private` 安全(首 `#` 后取段 + 剥 `:kind:line` 尾) |
| P2-3 | hunkPointToFilePoint 以 old/new 计数耗尽封顶,不再越过下一 `@@` |
| P2-5 | file-symbols base 侧补 1.5MiB 上限(与 file-content 语义对齐) |
| P2-6 | porcelain -z 的 R/C ORIG-path 记录成对消费 |
| P2-7 | line/col 非整数(NaN)命中 `code-intel-missing-params` |
| P2-8 | baseline references 去重 |
| P2-9 | jumpToFile 清 externalFile |
| P2-11 | SourcePane:空 qualifiedName 直接展示文件;迟到的符号解析不再 clobber 已导航位置 |
| P2-12 | 关系图 caller 成员行也获得 ‹› 入口 |

## 记档不修(已知限制,follow-up 候选)

- **P2-10 renamed 文件 base 侧 basePath**:code-intel/file-symbols/CodeViewer 均按新
  路径读 base——改名文件的 base 定义跳转显示「该侧不存在此文件」;base 侧 in-diff
  跳转走 externalFile 分支被标「任务外文件」徽标(徽标语义偏窄)。改动面涉及三层
  参数扩散,留 follow-up。
- **P2-1 后半**:SymbolMenu 无外点关闭/首项 autofocus/方向键管理(role=menu 键盘
  语义不完整;Esc 可关、条目为真按钮 Tab 可达)。
- **P2-4 残余**:useLineHtml key 已含 (task,repo,path,side) 身份,同一文件刷新后同
  长度不同内容仍有一次渲染窗口的旧行(react-query 内容到达即收敛);BUDGET_BYTES
  以 UTF-16 length 近似字节数。
- **P2-13**:负缓存 key 不含 digest——修好源码后冷却窗内(≤5min)仍降级;口径:
  负缓存针对 indexer 可用性而非内容,记档为有意行为。
- **P2-14**:`langIdForPath` 是后端 EXT_RESOLUTION 的手工镜像(当前 20 扩展逐一
  核对一致);后端表消费 shared 常量留 follow-up。
- **P2-8 后半**:`CodePosition.preview` 后端未填充(菜单显示 file:line,无行预览)。
