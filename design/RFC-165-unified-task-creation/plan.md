# RFC-165 统一创建任务 —— plan

任务编号 `RFC-165-T*`；3 个 PR。**全部实现排在 RFC-164 PR-3 提交之后**。migration ×2（0085 含 `space_kind`/`source_agent_name`/`workspace_pruning_at`/`workspace_pruned_at`/DROP recent_repos；0086 含 `launch_kind`），journal 计数锁各 +1。**每个 PR 合入后 main 必须可部署可用**。每个 PR：四件套全绿 + shared 导出变更跑 `bun run build:binary` + push 后查 CI + Codex 增量审查。测试编号引用 design.md §11。

## PR-1 `feat(shared,backend,frontend): RFC-165 执行空间收敛——契约 v2 + scratch 模型 + path 公开面下线 + 旧 launcher URL-only 化`

| #   | 任务                                        | 说明 / 验收                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1  | shared 契约 v2                              | `StartTaskSchema` 删三字段/加 `scratch`/repos url-only；`rejectRetiredStartTaskKeys`（F1）；`applySpaceFields`（F2）+ workgroup candidate 接入；`TaskSchema`/Summary 增列；`taskExecutionKind`。测试 §11.1/.2/.6/.13。                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T2  | scratch 物化 + 生命周期                     | `initScratchRepo`（AW_INTERNAL_GIT_IDENTITY 身份，N2）+ **`materializeSpace` tagged result（F3-r3 多仓形状：`canonicalWorktreePath`+`repos[]`+lease；失败臂 `failedStage`/`partial`）** + multipart 统一协议 + 禁用面；**lease 由外层 launch try/finally 持有（F9-r3）** + 孤儿扫描；GC 扩展：**两阶段墓碑 pruning/pruned + 超时重认领 + 多仓逐仓删（R3-1）** + **revive 全路径（resume/retry/sync/lifecycle repair）统一 CAS 条件（R3-2）** + **candidate 排除 `internal`（R3-4）** + **iso GC 同认领 + 多仓 onlyMerged 全仓判定（D1/D3）** + **legacy 双 NULL 行 startup reconcile + 物化失败行原子补墓碑（R3-2-r4）**。测试 §11.3/.4/.5。 |
| T3  | migration 0085                              | `space_kind`（local/remote/internal——**fusion 工作流名精确收窄 + 排除 workgroup_id 非空，F4-r3**；EXISTS 混合多仓 F20）+ `source_agent_name` + **`workspace_pruning_at`/`workspace_pruned_at` 两列** + DROP recent_repos；statement-breakpoint；journal bump。测试 §11.18。                                                                                                                                                                                                                                                                                                                                                                  |
| T4  | scheduled backfill + tolerant read          | path→`pathToFileURL` 保真改写；**file cache key dual-read/校验后 rekey（F19-r4：旧 key lossy 碰撞需复核原 url 新 key 一致；old/new 双锁 CAS 仅更新 url_hash）**；file 源 ref 权威解析（cold syncBranches）+ 源/源分支失效**硬 fail**；**`fetchBeforeLaunch:true` 行禁用+语义提示（不静默删），false 行自动转**；healer 时点 DB open→heal→HTTP serve；**逐字段三态 degraded DTO（launchPayload/scheduleSpec 各自，含坏 shape，N3-r3）+ raw PUT 修复**。测试 §11.14/.15。                                                                                                                                                                      |
| T5  | path 公开面下线 + 内部面迁移                | recent 端点/`upsertRecentRepo`/path fetch/multipart path 分支删；`resolveRepoSourceSingle` 对外收内；`internalSource`（互斥/可单独或组合 preCreatedWorktree+路径一致断言/deps 边界）+ fusion 迁移 + `space_kind='internal'`；recent shared DTO/导出退役（F7）。先 grep 全量盘测试锁；存量用例迁 file:// 或 internalSource。banned 锁**三键**精确 allowlist（F22-r3：符号/锚级，非整文件）§11.20；fusion 回归 §11.7。                                                                                                                                                                                                                         |
| T5b | 旧 launcher URL-only retrofit（F6，closed） | 删 recent import/query/种子、path 分支+`RepoSourceTabs`、`buildLaunchFormData.ts`；`bodyToRepoSources` v2（legacy→空 URL+修复横幅）。PR-1 出口冒烟：前端 build + URL JSON/upload 启动 + legacy schedule 修复链。测试 §11.24/.26/.27。                                                                                                                                                                                                                                                                                                                                                                                                        |
| T6  | PR-1 门禁                                   | 四件套 + build:binary + CI + Codex。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

依赖：T1 → T2/T3/T4/T5/T5b。

## PR-2 `feat(shared,backend): RFC-165 单 Agent 启动路径 + 定时任务三主体`

| #   | 任务                             | 说明 / 验收                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T7  | 宿主 + 合成快照 + 统一校验上下文 | `agentLaunch.ts`：`__agent_host__` 懒播种 + `buildAgentHostSnapshot`；`buildWorkflowValidationContext(db)`（plugins 必传）副作用前 parse+validate；**同 PR 替换全部生产 caller（JSON launch/multipart/sync apply/preview，R3-3）+ 一致性锁**。形状锁 + `.ok` + 负例矩阵 §11.8。                                                                                                                                                                                                                                                                                                                                                         |
| T8  | 启动服务 + 路由 + 守卫           | `StartAgentTaskSchema` + `startAgentTask`（404 同形 + `assertNotBuiltin` + `applySpaceFields` + **`dbTxSync` 单事务收拢 insert+agent 重检（F17-r3）**）+ `POST /api/agents/:name/tasks` 挂 `tasks:launch`（F15）；守卫 `taskExecutionKind` 化——**agent：resume+node retry 放行；workgroup：resume 与 node retry 均 403 锁 + lifecycle repair 列表/apply/auto 对 workgroup 拒绝 resumeAfterApply/节点复活选项（F13-r4）+ boot auto-resume 候选排除 workgroup（r5）**；sync 统一 422；agent rename/delete 守卫 `dbTxSync` 化 + 非终态 409；accepted limitation（终态 retry/resume 遇删=agent-not-found）文档+语义锁。测试 §11.9/.11/.12。 |
| T8b | optional clarify 指令（F12-r3）  | `'optional'` 进 `ClarifyChannel.directive` 联合（runner 去布尔化透传、follow-up renderer 双 envelope 含纠错轮）；clarify 节点 `clarifyMode:'optional'` 字段 + **`undefined` 兼容缺省（R3-5）**；scheduler 局部变量 `clarifyMode`→`channelKind` 改名；优先级 stopped>optional>mandatory/suppressed；答复 rerun 保持 optional。测试 §11.10。唯一 scheduler/runner 触点。                                                                                                                                                                                                                                                                  |
| T9  | 定时三主体（D11）                | 0086（journal bump）；`scheduledPayloadSchemaFor(kind)` 接 save/edit/fire/run-now；工厂按 kind 分派；自动 fire 阈值禁用 vs run-now 不动 cadence（F21，closed）；kind 不可变；**`tasks:launch` 完整规则（N1-r3）：create/payload 更新/enable/enabled 态 spec 变更/run-now gate，disabled spec-only 等放行**。测试 §11.16/.17/.19。                                                                                                                                                                                                                                                                                                       |
| T10 | PR-2 集成测试 + 门禁             | 全链矩阵；四件套 + build smoke + CI + Codex。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

依赖：PR-1；T9 依赖 T8。

## PR-3 `feat(frontend): RFC-165 四步创建向导 + 入口全集 + 旧 launcher 下线`（吸收/取代 RFC-164 T22）

| #   | 任务                       | 说明 / 验收                                                                                                                                                                                                                                           |
| --- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T11 | Stepper 公共原语           | `components/Stepper.tsx` + 样式 + a11y + 单测 §11.21。                                                                                                                                                                                                |
| T12 | `/tasks/new` 四步向导      | 三分支 + gating + 确认页/回跳 + 深链落 Step 2 + `?schedule=1` 主次互换 + `editScheduled` 三 kind + Step 1 就绪度/launchability 过滤（§4 统一 ctx 投影）。测试 §11.22/.25。                                                                            |
| T13 | 三 body builder + 定时封套 | 三 builder + `stampSpace`；「存为定时任务」复用产物 + kind 封套（`ScheduleDialog` 三 kind 泛化）。字段显式断言 §11.23。                                                                                                                               |
| T14 | 入口全集 + 下线            | 首页快速操作、`/tasks` 新建、`/scheduled` 新建定时+错误/迁移徽标、agents 详情+列表行、workgroups 详情、编辑器/任务详情/定时详情深链、redirect、`workflows.launch.tsx` 整页删除；源码文本锁 §11.26；164 `plan.md` T22 + design.md 定时排除条代改标注。 |
| T15 | i18n + 视觉自查            | `taskWizard.*` zh-CN/en；截图（light+dark）对齐；不动视觉基线。                                                                                                                                                                                       |
| T16 | e2e                        | §11.28/.29：三条即时链到终态 + 一条定时链。                                                                                                                                                                                                           |
| T17 | 收尾                       | `design/plan.md` 索引 Draft→Done、`STATE.md` 已完成表、四件套+CI+Codex 实现门。                                                                                                                                                                       |

依赖：PR-1 + PR-2。

## 验收清单（对 proposal §6 逐条回验）

- [ ] 首页 / `/tasks` / `/scheduled` + 三资源页深链（含 agents 列表行）可进向导；三方式建任务到**终态**（e2e ×3）。
- [ ] 单 Agent：`{{description}}` 注入；optional 反问四向 + directive 全链透传（纠错轮双 envelope）+ `clarifyMode` 缺省兼容；关反问=无 clarify；builtin agent 403；`tasks:launch` PAT 矩阵；`dbTxSync` 单事务重检。
- [ ] 临时空间：三方式可建；diff 契约（UTF-16 截断口径）；禁用面双层；tagged result（多仓部分失败）+外层 lease+两阶段墓碑+revive 全路径 CAS+internal 排除，竞态测试绿。
- [ ] path 公开面无残留（raw-key 混合体 422、三键 allowlist banned 锁、源码文本锁）；file://（dual-read/rekey、未推送分支冷启动、源失效硬 fail）；fusion internalSource 回归绿。
- [ ] 存量定时任务：path→file:// 保真迁移；`fetchBeforeLaunch:true` 行禁用+提示；目录缺失禁用；legacy/坏 JSON/坏 shape 行逐字段 degraded 可读可修可删。
- [ ] 定时三主体：三分支可存定时；fire 阈值禁用 vs run-now 分离；kind 不可变；`tasks:launch` 逐操作矩阵（含 enabled 态 spec 变更）；编辑按 kind 反填。
- [ ] 宿主任务：agent resume/node retry 放行；**workgroup resume/node retry 均 403 锁**；sync 422；旧路由 redirect + `editScheduled` 等价。
- [ ] 每 PR 合入后 main 可部署（PR-1 出口冒烟）；门禁四件套 + build smoke + CI 两 OS + Codex 增量审查。
