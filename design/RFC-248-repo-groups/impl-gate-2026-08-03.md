# RFC-248 实现门（2026-08-03）

**跑法**：Codex `review --base ad5d2963`，从 pin 到自己 commit `cf56f888` 的**分离
worktree**（`git worktree add --detach`）跑——共享 `main` 上并发 diff 会吞掉 review
（`docs/dev-gotchas.md` §Codex）。覆盖本 session 的三个 commit（PR-4 + PR-5a + PR-5b）、
约 60 个文件。

**用时 ~80 分钟**，stdout 工具调用日志约 100 KB，全程实读源码（`repos.tsx` /
`task-wizard.ts` / `changeReview.ts` / `scheduler.ts` / `scheduledTasks.ts` /
`assemble.ts` / `mcp/tools.ts` / migrations…）——**不是空洞通过**。

**判定：not ready to merge，9 × P1 + 11 × P2。逐条核实后全部属实、全部修复。**

---

## P1（9 条，全部已修 + 带回归锁）

| #   | findings                                                                      | 核实结果                                                                                                                                                                                              | 修法                                                                                             |
| --- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 1   | 挂根成员的文件在评审侧栏里**整批消失**                                        | **属实**。`splitByRepo` 把 `.` 解析成 `''` 后，`ChangeReviewPanel` 6 处仍用 `label === undefined ? p : label+'/'+p` ⇒ 拼出 `/src/a.ts`，与结构化侧 `src/a.ts` 逐字符不等                              | 新增**唯一** key 构造器 `changeEntryKey`（`undefined`/`null`/`''` 三态等价），6 处全部改用它       |
| 2   | 只读脏计数只在 `autoCommitPush=true` 时才写                                   | **属实**。`maybeRunCommitPush` 由 `state.task.autoCommitPush && topLevelIds.has(nodeId)` 触发 ⇒ 默认配置任务、失败 / 取消任务永远 NULL                                                                | 抽出 `inspectReadonlyRepos`，挂到**任务终态收尾**（done/failed/canceled/awaiting_\* 全覆盖）      |
| 3   | `sourceTaskId` 重放绕过 `canViewTask`                                         | **属实**（安全）。id 完全由调用方控制，`loadFrozenLayout` 直接读 `task_repos`，泄漏私有 / 已撤权任务的仓库构成，且形式是「任务成功启动」                                                              | 新增 `assertCanReplaySourceTask`（不可见与不存在同形 404），接到 4 个启动入口                     |
| 4   | 重启多仓任务在向导里变成空白行且自称「可解析」                                | **属实**。`taskToLaunchPayload` 发 `sourceTaskId`，而 `payloadToWizardSeed` 不认它 ⇒ 落进 `bodyToRepoSources` 产出空 remote 行                                                                        | `WizardSpace` 新增 `{kind:'replay'}` 一档：seed / body / 卡片 / 摘要全链路                        |
| 5   | 组的 OCC 栅栏形同虚设                                                         | **属实**。`UpdateRepoGroupSchema = CreateRepoGroupSchema` ⇒ 非 strict zod **静默剥掉** `expectedVersion`，路由也没传第四参 ⇒ 并发编辑 last-write-wins                                                 | Update schema `.extend({expectedVersion})` + 路由转发                                             |
| 6   | 删组无确认、409 后无出路                                                      | **属实**。一键抹掉并归档绑定记忆；被引用时 409 且 UI 没有 `force` 路径                                                                                                                                | `ConfirmDialog` 确认 + 409 后切成「强制删除」重试 + 回报归档/摘除/停发条数                        |
| 7   | multipart 预物化缺 `secretBox`                                                | **属实**。组成员一律按 `cachedRepoId` 解析，私有仓 URL 是封存的 ⇒ `cached-repo-credential-unavailable`；等价的 JSON 启动却成功                                                                        | 两处预物化都透传 `deps.secretBox`                                                                 |
| 8   | 上传目录按**仓数**判定                                                        | **属实**。单成员 + sparse 的组 `repos.length === 1` ⇒ 上传物落进那个仓的工作树、进它的审计 diff、被自动推送                                                                                           | 两处改判 `space.kind === 'group'`；排除计划的 `includeUploadDir` 恒 true                          |
| 9   | 空组可启动                                                                    | **属实**。展平 0 仓时建出无 `task_repos` 的组根、`repoCount=1`，任务跑在**非 git 目录**里                                                                                                              | 服务端 `assertNonEmptyLayout` ⇒ 422 `repo-group-empty`                                            |

**P1-8 连带暴露一个真交互**：`includeUploadDir: true` 后，sparse 成员的仓根
`.gitignore` 落在 sparse 集之外，`git add` 直接拒。但这条 `.gitignore` 恰恰**必须**
写——未跟踪文件不受 sparse 规则约束，不排除上传目录就会被 `add -A` 吞掉。改用
`git add --sparse`（sparse-checkout 是工作树概念，提交里带集合外路径完全正当）。

## P2（11 条，全部已修）

preview `.parse()` → 500 改 `safeParse` 422 · 布局树改用共享 `mountDepth`/`isUnder`
（自写的区分大小写 + 挂根深度算错）· 编辑器补粘 URL 入口（否则 API 的 `repoUrl`
形态与 `pendingImports` 在 UI 上够不着）· 向导选组后预览布局树 · 任务详情溯源按
`repoGroupName` 判定而非 `repoCount > 1`（单成员组也是组）· 任务详情改用共享树 ·
MCP `resource_write` 加 `force` 输入并映射成 `?force=1` · 记忆 `SCOPE_TABS` 补第 5
档 · 组分段补搜索工具条 + noMatches 空态 · 保存组时一并失效 `repo-group-layout`
缓存（否则展开的树显示改前布局）· `.info-box--warn` 变体（复用既有 `--warn-*`
令牌，不新造颜色）。

## 本人自查独立命中（gate 之外）

Codex 探 `scheduledTasks.ts` 那次 grep 让我发现一个**我自己引入**的缺口：把顶层
`repos` 加进 `RETIRED_START_TASK_KEYS` 后，存量的多仓定时任务 payload 会开始被
RFC-165 自愈扫描捡起来，但那段扫描只认识 `repoPath` / `baseBranch` /
`fetchBeforeLaunch` ⇒ 每轮删掉几个**别的**键、写回、计一次 converted，`repos` 还在
⇒ **永远清不干净**，而计划一直启用着、每次触发 422。正是设计第 10 行要防的烂账，
只是从存量 payload 那一侧进来。修法：1 条 `repos` 摊平进顶层（语义上就是单仓）、
≥2 条**停发**并给出「建个组再重存」的可操作原因；`rfc248-scheduled-payload-heal.test.ts`
锁住幂等收敛。
